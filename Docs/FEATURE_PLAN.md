# MicrobeLab — Feature Plan

> **Twenty-ninth-pass rule-restatement summary** (top-of-doc per the canonical-invariant tier codified 2026-06-12; verbatim user-direct, now repeated TWENTY-NINE times spanning five calendar days — passes twelve through eighteen landed at PRs #93 → #121 (2026-06-13), passes nineteen + twenty landed at PRs #122 → #132 (2026-06-15), passes twenty-one → twenty-six landed at PRs #133 → #176 (2026-06-16; SIX same-calendar-day restarts forming a same-day chain), twenty-seventh landed at PRs #177 → #182 earlier today (2026-06-17; single-day gap from the twenty-sixth, separated by the PR #176 merge), twenty-eighth landed at PRs #183 → #186 earlier today (2026-06-17 — SECOND same-calendar-day restart on 2026-06-17, separated from the twenty-seventh by the PR #182 merge), and the twenty-ninth pass opens this round (2026-06-17 — THIRD same-calendar-day restart on 2026-06-17, separated from the twenty-eighth by the PR #186 merge that closed the twenty-eighth-pass round rollup — ELEVENTH observation of cadence-persistence, demonstrating the same-day-chain property scales MONOTONICALLY within a single calendar day past the initial 2-pass restart; per the eleven-pass invariant the cadence has saturated): *"critical: do not author/edit xcode-managed files including Xcode workspace file and Xcode scheme/test plan file. Instead, file a handoff doc with the user to do Xcode UI work. staging and committing is ok."* Scope: `*.xcworkspace/contents.xcworkspacedata` / `*.xcodeproj/project.pbxproj` / `*.xcscheme` / `*.xctestplan` / `Info.plist` / `*.entitlements` / `*.xcassets/Contents.json` / `xcuserdata/` / `Package.resolved`. No checkbox below implies an edit to any of those paths; items that need them ship via `Docs/HANDOFF_TO_USER_<TOPIC>.md`. Per the **eleven-pass canonical-invariant tier**: the cadence has saturated — twelfth- through twenty-eighth-pass restatements add reinforcement frequency but no new binding tier. The rule lives in FOUR canonical locations (CLAUDE.md + `.claude/rules/xcode-agent-safety.md` + this prologue + the persistent-memory file `feedback_xcode_managed_files.md`); any one of them can drift or decay without the rule being lost — the other three carry it forward. See `@CLAUDE.md` § Xcode-managed file safety for the canonical statement.
>
> Phased delivery roadmap. Mirrors the engineering breakdown in `@Docs/TECHNICAL_DESIGN.md`. Implementing sessions check off boxes as work lands; do not collapse phases — the per-phase exit criteria gate ship readiness.
>
> **SPM folder convention** (per `@CLAUDE.md` § SPM Folder Convention): new feature surfaces with ≥ 3 files (view + machine + service) land in a per-feature subdirectory under their target (e.g., `AppFeature/Onboarding/`, `AppFeature/Engagement/`). Add subdirs as the codebase grows; the flat-root pattern stays canonical SPM regardless.
>
> **Xcode-managed file constraint** (per `@CLAUDE.md` § Xcode-managed file safety): no item in this plan implies edits to `*.xcodeproj/*.xcscheme/*.xctestplan/Info.plist/*.entitlements/Package.resolved`. Items that need them ship via `Docs/HANDOFF_TO_USER_<TOPIC>.md` describing the Xcode GUI steps.

## Phase 1: Microscope Foundations (MVP)

Core microscope-zoom loop, 12-character microbe cast, freshwater microbiome simulator, innate-immunity minigame, and AI Socratic mentor (Vee). NGSS MS-LS1-1 baseline + portfolio safety/COPPA bedrock.

### Scaffolding

- [x] Create Xcode project with thin app shell (`MicrobeLab/MicrobeLabApp.swift`)
- [x] Create `Packages/Libraries/Package.swift` with 6 targets (Models, Services, SharedUI, GameEngine, AIMentor, AppFeature)
- [x] Add ForgeKit dependency (remote GitHub URL, `from: "0.99.0"`)
- [x] Create stub source files for all targets
- [x] Verify build succeeds with zero warnings (via `swift build` — no Xcode reload)
- [x] Add `Packages/Libraries` to workspace (PR #14 — workspace + test plan now wire the 4 SPM test targets)

### Data Layer

- [x] Define value types: `MicrobeCharacter`, `MicrobiomeState`, `ZoomTier`, `GutSlot`, `FeedingMode`, `AntibioticState`, `GrowthRate`
- [x] Define SwiftData models: `PersistentMicrobeSession`, `PlayerProgress`, `EncounterLog`, `JournalEntry`
- [x] Create `VersionedSchema` (V1) with all models — `SchemaV1`
- [x] Create `SchemaMigrationPlan` (V1 only — start early) — `MicrobeLabMigrationPlan`
- [x] Bundle 12 microbe character catalog as JSON in `Services/Resources/microbes.json`
- [x] Create `MicrobeCatalogService` to load + query microbe data
- [x] Create value-type cache structs (`PlayerProgressData`, `EncounterLogData`, `JournalEntryData`, `MicrobeSessionData`)

### Microscope Engine

- [x] Implement `ZoomTier` enum + transition rules (1× → 100× → 1000× → 10000×)
- [x] Implement `MicroscopeScene` skeleton (SpriteKit) with lazy visual setup + `.resizeFill` scaleMode
- [x] Implement pinch-to-zoom gesture handler + tier boundary snap (via `ZoomMachine` + `MicroscopeScene.handlePinch(delta:)`)
- [ ] Implement LOD sprite atlas + per-tier sprite swap (asset-blocked on microbe portrait pack)
- [x] Implement `ZoomMachine` view-local state machine
- [x] Implement microscope HUD overlay (tier badge + zoom level) — `MicroscopeHUD` in SharedUI, wired into ExploreView with mentor-cue refresh on snap

### Microbiome Simulator

- [x] Implement `MicrobiomeState` value type (populations + feeding mode + antibiotic state + tick count)
- [x] Implement `FeedingMode` enum (fiber / sugar / balanced / none) + per-microbe growth-rate effects
- [x] Implement `AntibioticState` enum (none / active / recovering) + recovery-curve mechanics
- [x] Implement `MicrobiomeSimulator` per-tick update logic
- [x] Implement `GutSlot` ecology zones (oralCavity / stomach / smallIntestine / largeIntestine / colon + skin + soil)
- [x] Implement `SimulationMachine` view-local state machine
- [x] Implement deterministic seedable RNG for reproducible test states (`SeededRNG` splitmix64; `tick(_:using:)` overload threads jitter)

### Immune Response Engine (innate path)

- [x] Implement `MacrophagePacmanScene` skeleton (SpriteKit) — Pac-Man-style consume-pathogen loop
- [x] Implement pathogen spawn + wave progression (pure-value logic in `MacrophagePacmanScene` + `PathogenState` / `PathogenKind` in Models)
- [x] Implement macrophage movement + boundary handling (`moveMacrophage(by:)` clamps to scene bounds; `consumePathogensInRadius()` awards points)
- [x] Implement scoring + 5-wave Phase-1 progression (logic-level: `recordConsume(_:)` + `clearWave()`)
- [x] Wire scene into `GameEngine` target with lazy visual setup

### 12-Character Microbe Cast

- [x] Define 12 named microbes (Lacto / Yeast / Photo / Net / Spore / Guard canonical DN cast + Bifido / Akker / Strep / Coli / Rhino / Deino codex-supporting)
- [x] Bundle character JSON metadata in `Services/Resources/microbes.json` (v2 schema includes voiceLines)
- [ ] Bundle character portrait WebPs in `Resources/Cast/<slug>.webp` (12 portraits; await hub distribution per `.claude/rules/forgekit.md` § Asset generation ownership)
- [/] Wire `ForgeIllustrations.IllustrationRegistry` registration — **chapter content axis SHIPPED PR #94**; portrait-asset axis still awaits the labsmith portrait pack. `ForgeIllustrations` is now declared in `AppFeature` per `Packages/Libraries/Package.swift`; `MicrobeChapterStore` (Services/) loads the 6 bundled DN-S chapter MDs (Lacto / Yeast / Photo / Net / Spore / Guard) from `Bundle.module`; `ChapterReaderSheet` (AppFeature/Engagement/) renders the chapter title + opener illustration (via `ForgeIllustrations.ChapterPortraitView` — gracefully renders `Color.clear` when chapter WebPs are absent, so the surface is correct today + auto-upgrades when assets ship) + markdown body in a kid-readable reading column. The `IllustrationRegistry` full registration step (per-microbe portrait + per-chapter opener/spot manifest) lands when the labsmith asset wave ships.
- [x] Implement per-character voice lines per DN voice register card — `voiceLines: [String]` on `MicrobeCharacter`; `VeeMentor.voiceLine(for:rotation:)` rotates lines deterministically; backward-compatible Decoder default for v1 catalogs

### SwiftUI Views

- [x] Create 5-tab `TabView` (Explore / Codex / Microbiome / Progress / Profile) shell + bundled-catalog bootstrap
- [x] Build `ExploreView` wrapping `MicroscopeScene` via `SpriteView` (tier-badge HUD + mentor bubble)
- [x] Build `MicrobeCodexView` (12-microbe grid; locked entries show "???" until discovered) — **ecology surfacing landed PR #60**: `MicrobeKnowledgeGraph` (Services/) wraps `ForgeKnowledgeGraph.KnowledgeGraph` with shared-habitat edges (symmetric, `.recommended` strength) derived from `MicrobeCharacter.preferredEnvironment`. The view builds the graph once on appear; each discovered card surfaces up to 2 already-discovered ecology neighbors via a small "Lives near: X, Y" caption. Trauma-informed posture: neighbors are filtered to the kid's discovered set so the codex never hints at undiscovered microbes (no shame-mining "you haven't met X yet" framing). 9 unit tests pin graph construction + neighbor ordering + limit + exclude-self + unknown-slug handling.
- [x] Build `MicrobiomeView` wrapping simulator with feeding-mode + antibiotic controls
- [x] Build `ImmuneGameView` wrapping `MacrophagePacmanScene` — reached via NavigationStack toolbar in MicrobiomeView; ships trauma-safe off-ramp + score HUD
- [x] Build `ProgressTabView` with XP / streak / codex grid
- [x] Build `ProfileView` with `ForgeAvatar.AvatarStudioView(.lite)` sheet — `AvatarStudioSheet` seeds the ForgeID via `getOrCreateForgeID(displayName:)` in `.task` before the editor opens (R489 gotcha), reads back `currentForgeID()?.avatar` on appear so the row shows the saved look
- [x] Build `SettingsView` with parental gate (`AppSettings` + `AppSettingsStore` in Services; `ParentalGateView` math gate; sensory toggles kid-accessible, content gate + session cap parent-gated)
- [x] Build `QuizView` for question kits — wired from MicrobeCodexView toolbar Menu; `QuestionKit` + `Question` in Models; `QuestionKitService` loader; `QuizMachine` in SharedUI; kit 01 (microbiology basics, 5 questions) bundled

### AI Mentor (Vee — Socratic)

- [x] Create `VeeMentor` class with lazy `LanguageModelSession`
- [x] Implement `MicrobeFact` `@Generable` with curriculum-guarded fallbacks for all 12 microbes
- [x] Implement `ZoomCue` `@Generable` for tier-transition Socratic prompts
- [x] Implement `EcologyHypothesis` `@Generable` for microbiome puzzle scaffolding
- [x] Implement static fallbacks for every `@Generable` per `.claude/rules/foundationmodels.md`
- [x] Create mentor speech-bubble UI component — `SharedUI.MentorBubble` with thinMaterial backdrop + combined accessibility label
- [x] Wire mentor to events: microscope tier-up, microbiome milestone, immune wave-clear — `ExploreView` refreshes cue on tier snap; `MicrobiomeView` refreshes on feeding-mode change + every 5th tick milestone; `ImmuneGameView` surfaces a wave-clear cue on bubble (run-end + per-wave variants)

### Gamification

- [x] Integrate ForgeGamification `XPEngine` for leveling (wrapped in `GamificationService` MainActor `@Observable`)
- [x] Integrate `StreakManager` for daily engagement (`recordSession()` async surface; `currentStreak` / `longestStreak` mirrored to the `@Observable`)
- [x] Integrate `AchievementEngine` with first 10 Phase-1 achievements (`MicrobeLabAchievements.phase1` set defined; `evaluateAchievements(with:)` grants + auto-XP-awards)
- [x] Wire question kits 01-04 via `Bundle.module` (microbiology basics / microbiome / immune defense / beneficial microbes) — all four phase-1 kits ship via `QuestionKitService.phase1KitSlugs`
- [x] Implement XP awards for: first microbe discovered, microbiome stable for 5 ticks, immune wave cleared, etc. — quiz-completion XP + first-quiz / quiz-perfect achievements wired; microbiome (`fiberPioneer` / `sugarTrial` / `microbiomeSteady`) + immune (`immuneRookie` / `immuneRunner`) achievements now auto-evaluate via `MicrobiomeView` + `ImmuneGameView` calling `GamificationService.evaluateAchievements`, which auto-awards XP per achievement definition

### Adventure Mode

- [ ] Wire Level 1 config from `spark-anvil-hub/Resources/HubContributions/microbelab.json` (Life Zone contribution) — labsmith-side handoff filed at `Docs/HANDOFF_FROM_APP_FORGEADVENTURE_LIFE_ZONE_PROPOSAL.md` (eighth-pass round, PR #74); waits on labsmith to author the canonical baseline JSON + ship the `ZoneID.lifeZone` case
- [x] Implement `MicrobeLabHubContribution` Level 2 Swift overlay in `Packages/Libraries/Sources/AppFeature/HubContribution/` — **shipped PR #74** (eighth-pass auto-cycle sweep). `MicrobeLabHubContribution` (`nonisolated public struct`) conforms to `ForgeAdventure.HubContribution` with `themeAccent` = `#33CCBB` hero color per `Docs/TECHNICAL_DESIGN.md`, `mentorPersona` = `.microbeLabCilia` (`MentorPersona` extension with Cilia's canonical system-prompt header), `kitResources` = the 4 Phase-1 question kits already bundled via `QuestionKitService.phase1KitSlugs`, `engineCopy` carrying trauma-informed protection / agency / discovery copy for `.simulation` / `.defense` / `.quest`. `MicrobeLabHubChallengeAdapter` (SwiftUI adapter) renders a hub-friendly per-engine placeholder surface (icon + completion message + back-to-hub + wrap-up-today affordances). Targets `.scienceLabs` until labsmith ships `lifeZone` per the cross-repo handoff above
- [/] Register mode-cards in `AdventureView` — partial: `MicrobeLabHubRegistrar.register(into:)` ships the async registrar helper AdventureHub-side integration calls when the hub orchestration lands. AdventureHub owns the canonical registry instance; this item closes fully when AdventureHub-side integration calls the registrar at hub startup
- [ ] Wire `ForgeProgressionManager` gating across mode-cards — awaits hub-side ProgressionManager integration; gating semantics live in the Level 2 contribution's `kitResources` Bloom-band + grade-band metadata (already supplied). **Twenty-first-pass round PR #137 shipped IN-APP `ProgressionService` (Services/Engagement/) wrapping `ForgeProgressionManager` for Phase 3+ in-app gates** (disease-story-immune / disease-story-microbiome / global-microbiome-tour) — this is DISTINCT from the hub-side mode-card gating which this checkbox tracks; hub-side wiring still blocked.

### Onboarding

- [x] Create 5-step onboarding flow (welcome, first zoom-in, meet first microbe, first observation, first quiz) — `MicrobeLabOnboardingFlow` wraps `ForgeUI.ForgeOnboardingFlow`; `OnboardingMachine` value-type state + `OnboardingStore` UserDefaults persistence; gated at `AppRootView` before the tab shell
- [x] Implement aha moment: first microscope zoom reveals a character introducing themselves — onboarding step 3 introduces Lacto verbatim ("one of trillions of tiny lives that help you digest food"); `ExploreView` mentor-bubble cue refreshes on tier snap (the in-app aha continues after onboarding completes)
- [x] Implement progressive disclosure (Session 1: microscope + codex only) — `SessionCountStore` (UserDefaults monotonic counter) + `TabDisclosure` (pure mapping: count < 2 → Explore + Codex; 2-3 → + Microbiome; 4+ → full chrome incl. Progress + Profile); wired into `AppRootView.tabShell`; increment fires only after onboarding completes so the 5-step flow isn't counted as session #1
- [x] Implement parent handoff flow (30s setup) — `ParentHandoffFlow` (AppFeature/Onboarding/) wraps a 4-step value-type `ParentHandoffMachine` (welcome → content comfort → daily session cap → ready). Persists choices into `AppSettings` (disease-story gate + daily cap); flips `ParentHandoffStore.hasCompletedHandoff` (Services/) on completion. AppRootView gates the kid-facing `MicrobeLabOnboardingFlow` behind the parent handoff so a grown-up confirms preferences ONCE before handoff. No PII captured (binary completion flag only) per `.claude/rules/age-assurance.md` § 2026 FTC COPPA.
- [/] Apple Declared Age Range API gate (iOS 26+) — **scaffold landed**. `Services/AgeAssuranceService.swift` (MainActor `@Observable`) holds the most-recent verification result + the `AgeAssuranceCapability.isDeclaredAgeRangeAvailable` entitlement probe (reads `Bundle.main`'s `Entitlements` dict). `SettingsView` → About surfaces a passive readout ("Math gate — Apple gate pending entitlement" until the entitlement lands; flips to "Declared Age Range API ready" after). ForgeKit's `ForgeSystemAgeGate` already implements the system request + math-gate fallback. **Entitlement provisioning** is documented in `Docs/HANDOFF_TO_USER_XCODE_GUI_TASKS.md` § 6b — agent cannot author `.entitlements` files from disk (per CLAUDE.md § Xcode-managed file safety). The `requestSystemVerification(...)` method is a stub that no-ops + records `.notAttempted`; replace with the live `await requestAgeRange(...)` call only when the COPPA consent + retention surface ships (per `.claude/rules/age-assurance.md` — receiving "Under 13" creates **COPPA actual knowledge**, all consent flows immediately apply). 9 unit tests pin the scaffold (init defaults / recordResult / unavailable path / entitlement probe / key constant / result equality).

### Quality

- [x] Unit tests for microbiome simulator (feeding modes / antibiotic shock / recovery curve) — `MicrobiomeSimulatorTests` + `MicrobiomeStateTests` + `SimulationMachineTests`
- [x] Unit tests for zoom-tier transitions — `ZoomMachineTests` + `ZoomTierTests` + `MicroscopeSceneTests`
- [x] Unit tests for cast catalog loading + integrity — `MicrobeCatalogServiceTests` (bundled-load + canonical DN cast presence + beneficial-microbe foregrounding + unique-slug guard + voice-line integrity)
- [x] UI tests for microscope + codex flow — **shipped PR #99** (fourteenth-pass auto-cycle round). Smoke-level coverage added to the existing pbxproj-tracked `Apps/MicrobeLab/MicrobeLabUITests/MicrobeLabUITests.swift` (modifying the existing file does NOT require pbxproj edits per `.claude/rules/xcode-agent-safety.md`). New tests: `testAppLaunchesWithoutCrashing` (baseline `.runningForeground` state), `testFirstLaunchPresentsOnboardingGate` (accepts either `MicrobeLabOnboardingFlow` OR `ParentHandoffFlow.step.*` identifier surface depending on simulator UserDefaults state), `testTabShellShowsExploreAndCodexAfterOnboarding` (drives past the gate via the `advancePastOnboardingIfPresent` helper that taps through "Next" / "Get Started" / "Continue" / "Done" / "Looks good" / "Save" affordances bounded by a 15-iteration cap, then asserts the Explore + Codex tab bar buttons are visible). The helper relies on Tabs exposing their label as accessible buttons on the tab bar per SwiftUI's `Tab(label, systemImage:, value:)` semantics. The existing `testLaunchPerformance` is preserved.
- [x] UI tests for microbiome puzzle — **shipped PR #99** (same fourteenth-pass auto-cycle round). `testMicrobiomeTabHiddenOnFirstSession` verifies the progressive-disclosure gate behaves correctly on the canonical first-launch state — Microbiome tab MUST be absent at `sessionCount < 2` per `TabDisclosure.from(sessionCount:)`. Deeper flow coverage (entering the puzzle, exercising feeding-mode toggles, running the simulator forward) waits on a future launch-argument-driven session-count bypass — the bypass would also unlock the Progress + Profile tab coverage. The gate-behavior test is itself meaningful coverage because it pins the progressive-disclosure invariant a regression in `TabDisclosure` would silently break (e.g., if someone hardcoded `sessionCount: 5` while debugging and forgot to revert, this test would fail).
- [x] Accessibility audit (VoiceOver / Dynamic Type / color contrast) — read-only audit at `Docs/AUDIT_ACCESSIBILITY_PASS_2026-06-12.md`. Covers all SwiftUI surfaces shipped through PR #50; documents PASS / GAP per surface. Verdict: **PASS WITH GAPS** — Dynamic Type + color contrast PASS portfolio-wide (semantic font tokens, lowest contrast = `Color.primary.opacity(0.08)` solid fallback ≥ 4.5:1); 3 prioritized gaps identified (GAP 1: SpriteView hosts lack top-level VoiceOver labels — ~1h fix; GAP 2: 2 not-yet-swept thinMaterial sites in `ProgressView` + `QuizView` content cards — defer per Liquid Glass content-card policy; GAP 3: ParentHandoffFlow card material — bundle with next AppFeature touch). Recommendation: land GAP 1 + GAP 3 ahead of App Store submission; GAP 2 + SpriteKit action-duration clamp on Reduce-Motion can defer to Phase 2. Reviewer XCUITest `performAccessibilityAudit(for:)` coverage lands when the UI test surface lands. **GAP 1 closed PR #69**: `ExploreView` / `MicrobiomeView` / `ImmuneGameView` each carry a top-level `.accessibilityElement(children: .contain) + .accessibilityLabel(...) + .accessibilityValue(...)` envelope on the `SpriteView` host so VoiceOver lands on a named canvas + hears dynamic state (current tier / feeding mode + tick / wave + score + pathogens-remaining) on every snap. Asset-blocked items (LOD sprite per-microbe labels) still inherit when the portrait pack ships.
- [x] Performance profiling (microscope tier transition < 16ms; simulator tick < 8ms) — **signpost probes shipped PR #56 + bench-harness assertions shipped this round**. `PerfSignpost` (Models/) wraps `OSSignposter` with 3 portfolio-canonical channels (`zoom` / `simulator` / `immune`). Wired into `MicroscopeScene.handlePinch(delta:)` + `MicroscopeScene.snapToTier(_:)` (tier transition), `MicrobiomePuzzleScene.advanceOneTick()` (per-tick budget), `MacrophagePacmanScene.advancePathogens(by:)` (immune wave). The signposts are zero-overhead in release builds (Apple's unified logging absorbs them) so they stay safe to leave in production. Open Instruments → Points of Interest → filter `com.microbelab.app` to verify the budget. **Bench-harness assertion gate** lands via `Tests/GameEngineTests/PerformanceBudgetTests.swift` — 6 `@Test` cases drive the pure-value surfaces (`MicrobiomeSimulator.tick` + `MicrobiomeSimulator.tick(using:)` + `ZoomMachine.applyPinch` + `ZoomMachine.snap` + `MacrophagePacmanScene.advancePathogens` + `MacrophagePacmanScene.moveMacrophage`) across N=200 iterations using `ContinuousClock.measure`, assert the average per-iteration cost stays under a generous budget (4-8× headroom over the canonical 8ms / 16ms targets) so noisy CI doesn't flake the gate. Steady-state on a Mac runner: every surface lands under 0.1ms per iteration with full headroom remaining. Real-device Instruments capture for the 60fps on-device frame budget still pending — the bench harness covers the algorithmic regression class (a future change that 10×'s a per-tick cost surfaces immediately).

**Exit criteria**: first session reaches aha moment in ≤ 60 seconds; microbiome simulator stable across 100 ticks; immune Pac-Man playable end-to-end; 4 question kits ship; 12 cast members visible in codex.

---

## Phase 2: Adaptive Immunity + Microbiome Expansion

Adaptive B-cell antibody-matching minigame, expanded microbiome ecology (oral + skin + soil microbiomes), and 4 more question kits.

- [x] Implement `BCellAntibodyMatchScene` (SpriteKit) — pattern-match antibodies to antigens. **Skeleton + pure-value gameplay logic shipped fifteenth-pass auto-cycle round (PR #105); UI integration into `ImmuneGameView` shipped sixteenth-pass auto-cycle round (PR #109); per-pedagogy-beat mentor cues wired sixteenth-pass auto-cycle round (PR #111)**. `BCellAntibodyMatchScene` (`GameEngine/`) follows the `MacrophagePacmanScene` lazy-visual-setup pattern. Pure-value gameplay state: `AntibodyShape` enum (4 cases) + `AntigenState` + `BCellState` + `MemoryRecord`. 5-wave Phase-2 default `[3, 4, 5, 6, 7]`. Scoring: `baseMatchPoints` (2) on first-encounter; `memoryMatchPoints` (4) on memory-aided recognition. **UI integration (PR #109)**: `ImmuneGameView` gains a mode picker (segmented `.innate` / `.adaptive`) gated by `AdaptiveImmunityUnlock` from PR #108; locked state surfaces warm explainer copy ("2 more innate runs to meet the B-cell library"). Adaptive surface ships parallel SpriteView host + score HUD + antibody-shape segmented picker (kid picks the complement shape) + Step/Reset controls. Per-wave + full-run celebration + sensory haptics layered. **Mentor cues (PR #111)**: Begin-wave tap fires `.firstEncounter`; matched shape NOT in memoryCells fires `.matchedShape`; match landing on a shape already in memoryCells fires `.recallFromMemory` — scenario routing via memoryCells-before-match snapshot, no async hop. **Trauma-informed register**: no warfare lexicon anywhere; stoplist pinned by `ImmuneMode` + `VeeMentor` + `BCellAntibodyMatchScene` parameterized tests.
- [x] Implement adaptive-immunity progression curve (innate-first → adaptive unlocks) — **shipped sixteenth-pass auto-cycle round (PR #108)**. New `AdaptiveImmunityUnlock` (Services/Engagement/, pure nonisolated `Sendable` value type) is the canonical derivation: `from(innateRunsCompleted:perfectInnateRuns:simplifyChallenge:)` returns `.locked(progress:runsRemaining:)` | `.unlocked`. Standard path unlocks at 3 cumulative innate runs; fast-track unlocks at 1 perfect innate run (the pedagogy reward for a kid who nails the innate surface first try). Parent-gated `simplifyChallenge` toggle bypasses the gate entirely. Persistence via `AdaptiveImmunityProgressStore` (Services/Engagement/, MainActor `@Observable` UserDefaults-backed counter). `ImmuneGameView` records every completed innate run via `recordRunCompleted(perfectRun:)` in the `surfaceWaveClearCue(finished:)` finished branch; guarded by `hasRecordedInnateRunCompletion` so re-taps at the wave-5 boundary don't double-count. **Trauma-informed posture**: the locked-state explainer copy frames remaining work as discovery sequencing, never as failure. Copy stoplist (`failed` / `behind` / `should` / `must` / `almost` / `fell short` / `compared` / `better than` / `lost` / `not yet ready` / `haven't` / `didn't`) pinned by parameterized test. 20 unit tests across 2 suites (`AdaptiveImmunityUnlockTests` 11 + `AdaptiveImmunityProgressStoreTests` 9).
- [x] Expand `GutSlot` to include oral / skin / soil ecologies — **CLOSED via `Docs/AUDIT_GUT_SLOT_EXPANSION_CLOSURE_2026-06-13.md`** (seventeenth-pass round). `GutSlot` ships all 7 cases (`Packages/Libraries/Sources/Models/MicrobeCharacter.swift:23-31`): `.oralCavity` / `.stomach` / `.smallIntestine` / `.largeIntestine` / `.colon` / `.skin` / `.soil`. The audit walks per-case validity for the Phase 2 scenes that will consume each ecology + Codable raw-value forward-compatibility + the trauma-safe authoring constraints inherited by `.skin` (eczema-safe framing) and `.soil` (decomposer ecology bridge). PR #114 (seventeenth-pass round Option 2) `MicrobeKnowledgeGraphTests` explicitly use `.skin` + `.soil` + `.oralCavity` for cross-slot cohort traversal, so the cases are validated downstream. Dedicated scene-authoring checkboxes (oral / skin / soil) below remain open as separate items.
- [x] Bundle 8 additional microbe characters (20 total) — **shipped eighteenth-pass auto-cycle round (PR #119)**. `Packages/Libraries/Sources/Services/Resources/microbes.json` extended from 12 → 20 with 8 new entries (Sebu / Demi / Halo / Pylo / Sweet / Nodu / Therm / Loam) covering Phase 2 ecology gaps: `.skin` +2 (Sebu Cutibacterium-style sebum commensal, Demi Demodex face-mite framed as "quiet cleaner"), `.soil` +4 (Halo halophilic archaea, Nodu Rhizobium nitrogen-fixer, Therm thermophilic archaea, Loam saprophytic decomposer fungi), `.stomach` +1 (Pylo Helicobacter pylori framed as "most are quiet"), `.oralCavity` +1 (Sweet Streptococcus-mutans-style sugar-loving oral commensal). Trauma-informed posture preserved: zero new pathogenic entries (still 0 pathogenic / 7 neutral / 11 beneficial = 55% beneficial-first); skin entries use eczema-safe authoring (sebum framed as needed, pimples framed as "skin at every age"); stomach H. pylori framed as "most people who carry me never know I'm here"; oral Sweet framed as "I'm not the bad guy. I just like sugar a lot." Names checked against the existing MicrobeLab cast for slug uniqueness; UUIDs follow the `9DB2A1F0-3333-4XXX-...` Phase-2 prefix so the V1 SwiftData schema's `MicrobeCharacter` value-type Codable surface forward-decodes the new rows without migration. `MicrobeCatalogServiceTests` extended: count assertion bumped 12 → 20; new `phase2CastExpansionPresent` test pins per-slug presence; new `phase2EcologyCoverageExpanded` test pins per-ecology floors (skin ≥ 3 / soil ≥ 6 / oral ≥ 3 / stomach ≥ 1) so future edits don't silently regress the Phase 2 prerequisite for oral / skin / soil microbiome scene authoring; `beneficialMicrobesFlaggedCorrectly` floor raised 4 → 10 + the trauma-informed posture pinned by `pathogens.isEmpty` invariant. Build green via `swift build --target Services`. **Codex completion target auto-rescales** — `MicrobeCodexView` passes `catalog.microbes.count` to `MasteryMomentDetector.recordCodexDiscovery(totalDiscovered:totalAvailable:)`, so the codex-master moment now fires at 20/20 instead of 12/12 without a code change.
- [x] Implement oral-cavity microbiome scene (plaque ecology) — **shipped nineteenth-pass auto-cycle round (PR #124)**. `Models/OralMicrobiomeState` (pure value type wrapping `MicrobiomeState` + new `OralSugarLoad` enum with `.water` / `.fruit` / `.sugarSnack` / `.brush` cases) maps to underlying `FeedingMode` (`.balanced` for water/fruit, `.sugar` for sugarSnack, `.none` for brush — mirrors "the community settles" register). `GameEngine/OralMicrobiomeScene` mirrors `MicrobiomePuzzleScene` lazy-visual-setup; reuses shared `MicrobiomeSimulator` engine; warm cream background (no clinical white, no warning red). `AppFeature/OralMicrobiomeView` SwiftUI host (segmented sugar-load picker + tick/undo/reset + per-load mentor cue refresh) wired into `MicrobiomeView`'s NavigationStack via new toolbar item gated on optional `catalog:` parameter. **Trauma-informed posture**: brushing as care NOT moral test; cavities as ecology NOT blame; per-load mentor copy stoplist-pinned. **Pedagogy invariant**: brushing → `.none` feeding mode → ecology doesn't bloom under sustained brushing (verified by `brushSugarLoadCollapsesGrowthOverTicks` test). 15 new tests across 2 suites (`OralMicrobiomeStateTests` 7 + `OralMicrobiomeSceneTests` 8 @MainActor with `nonisolated deinit {}` per testing.md).
- [x] Implement skin-microbiome scene (eczema-safe framing per `.claude/rules/trauma-informed-content.md`) — **shipped twentieth-pass auto-cycle round (PR #130)**. `Models/SkinMicrobiomeState` (pure value type wrapping `MicrobiomeState` + new `SkinCareLoad` enum with `.gentleWash` / `.barrier` / `.scratch` / `.restRecover` cases) maps to underlying `FeedingMode` (gentle care → `.balanced`, scratching → `.sugar` = disturbance flora, rest → `.none` = community settles). `GameEngine/SkinMicrobiomeScene` mirrors `OralMicrobiomeScene` lazy-visual-setup; reuses shared `MicrobiomeSimulator` engine; warm peach/sand background. `AppFeature/SkinMicrobiomeView` SwiftUI host (segmented care-load picker + tick/undo/reset + per-load mentor cue refresh + `skinKindnessChampion` predicate wiring at `stableRunThreshold = 8`) wired into `MicrobiomeView`'s NavigationStack via new toolbar item gated on optional `catalog:` parameter. **Trauma-informed posture (eczema-safe)**: scratching acknowledged as kid lived-experience NOT failure; per-load copy frames consequence as ecology NOT blame ("The skin gets itchy. The neighborhood gets jumbled — that's ecology, not blame."); display labels stoplist-pinned (no `should` / `must` / `dirty` / `gross` / `ashamed` / `ugly` / `acne` anywhere on the surface); achievement name "Skin Garden Tender" recognizes care NOT victory. Cast wiring inherits Sebu (sebum commensal) + Demi (Demodex face-mite "quiet cleaner") + Guard (defensin-producing commensal) from PR #119. 20 new tests across 2 suites (`SkinMicrobiomeStateTests` 13 + `SkinMicrobiomeSceneTests` 7).
- [x] Implement soil-microbiome scene (decomposer ecology bridge to bioforge/ecosphere) — **shipped twentieth-pass auto-cycle round (PR #131)**. `Models/SoilMicrobiomeState` (pure value type wrapping `MicrobiomeState` + new `SoilMoistureLoad` enum with `.moist` / `.compost` / `.saturated` / `.drought` cases) maps to underlying `FeedingMode` (moist → `.balanced` = every guild thrives, compost → `.fiber` = decomposer feast, saturated → `.sugar` = anaerobic stew, drought → `.none` = dormancy + extremophile persistence). `GameEngine/SoilMicrobiomeScene` mirrors `OralMicrobiomeScene` / `SkinMicrobiomeScene` lazy-visual-setup; reuses shared `MicrobiomeSimulator` engine; warm forest-floor brown background. `AppFeature/SoilMicrobiomeView` SwiftUI host (segmented moisture-load picker + tick/undo/reset + per-load mentor cue refresh + `soilDecomposerWhisperer` predicate wiring at `stableRunThreshold = 8`) wired into `MicrobiomeView`'s NavigationStack via new toolbar item. **Trauma-informed posture (decay as participation, NOT death-anxiety)**: decay framed as "decomposers return material to the soil" NOT death-vocabulary; drought framed as "everyone slows down — extremophiles wait it out" NOT catastrophe; saturated framed as "only the anaerobic specialists thrive — that's ecology, not failure"; cross-portfolio bridge to bioforge/ecosphere — soil is a thriving system NEVER "dirt"; display labels stoplist-pinned (no `dirty` / `gross` / `nasty` / `rot` / `dead` / `doom`). Cast wiring inherits 7 soil microbes from PR #119: Loam (saprophytic decomposer fungi) + Nodu (Rhizobium nitrogen-fixer) + Therm (thermophilic archaea) + Halo (halophilic archaea) + 3 pre-existing soil microbes. 22 new tests across 2 suites (`SoilMicrobiomeStateTests` 14 + `SoilMicrobiomeSceneTests` 8). **Phase 2 ecology scenes ARE FULLY SHIPPED — all 3 of 3 (oral PR #124 + skin PR #130 + soil PR #131) closed.**
- [x] Integrate question kits 05-08 (adaptive immunity / oral biome / skin biome / soil biome) — **closed nineteenth-pass auto-cycle round (PR #123)**: kits 06 (oral) + 07 (skin) + 08 (soil) shipped alongside kit 05's adaptive-immunity surface. `QuestionKitService.phase2KitSlugs` extended `["adaptive-immunity"]` → full 4-slug Phase-2 set; `loadAllPhase2Kits()` returns all 4. New JSONs: `kit_06_oral_microbiome.json` "The Mouthful Neighborhood" (6 Q, NGSS MS-LS2-2; S. mutans as sugar-loving not villain; brushing as care; cavities as ecology not blame); `kit_07_skin_microbiome.json` "The Skin Garden" (6 Q, NGSS MS-LS2-2; C. acnes + Demodex framed gently; eczema framed with care not shame); `kit_08_soil_microbiome.json` "The Quiet Underground" (6 Q, NGSS MS-LS2-3; decomposers + nitrogen-fixers + thermophiles; ecology bridge to cross-portfolio cast). Trauma-informed register (warfare + body-shame + blame stoplist) pinned by parameterized test across all 3 ecology kits. 8 new test cases. **Distractor authoring discipline** codified for future kits: trauma-informed register applies to FULL choices array, not just correct answer + body — even wrong-answer framings register with kids. (Previous PR #120 sub-text:) **kit 05 (Adaptive Immunity) shipped eighteenth-pass auto-cycle round (PR #120)**. New `kit_05_adaptive_immunity.json` (title: "The Library of Shapes") ships 6 questions calibrated to NGSS MS-LS1-3 (immune system as subsystem) at the adaptive-immunity layer. Pedagogy beats mirror PR #104's `AdaptiveImmuneScenario` (.firstEncounter / .matchedShape / .recallFromMemory): Q1 contrasts adaptive vs innate (slower, remembers); Q2 introduces B-cells as antibody-producers; Q3 introduces antibody shape-matching ("key fitting a lock"); Q4 introduces memory B-cells (second meeting is shorter); Q5 introduces vaccines as adaptive-immunity primer; Q6 reinforces that most-microbes-are-friendly even at the adaptive layer. `QuestionKitService` extended with `phase2KitSlugs = ["adaptive-immunity"]` + `allKitSlugs` union accessor + `loadAllPhase2Kits()` mirror. Trauma-informed register: no warfare vocabulary anywhere in the kit (pinned by parameterized `kit05TraumaInformedRegisterStoplist` test against `fight` / `attack` / `destroy` / `kill` / `war` / `enemy` / `battle` / `weapon` / `soldier` / `warrior`); the body LEARNS and REMEMBERS — never fights. Question IDs in `AAAAA005-0000-4005-8000-...` namespace; uniqueness pinned across all shipped kits via the new `questionIDsAreUniqueAcrossAllShippedKits` test. Kits 06-08 (oral / skin / soil microbiome) wait on the corresponding microbiome scenes — Phase 2 cast expansion (PR #119) unblocked the casts those scenes will consume.
- [x] Add 8 Phase-2 achievements — **fully closed nineteenth-pass auto-cycle round (PR #125)**: PR #110 shipped the adaptive-immunity quintet; PR #125 adds the per-ecology trio: `oralBalanceKeeper` (80 XP, "Mouthful Steadier" — kid kept the oral neighborhood steady under water + fruit + gentle brushing; pairs with oral scene PR #124), `skinKindnessChampion` (80 XP, "Skin Garden Tender" — gentle washing kept the skin community in balance), `soilDecomposerWhisperer` (100 XP, "Underground Steward" — decomposer trio thriving). `MicrobeLabAchievements.phase2.count` 5 → 8. Trauma-informed register pinned: titles + descriptions name care + recognition, NEVER victory / "you finally" / "at last" framing (parameterized `phase2EcologyAchievementsTraumaSafeRegister` stoplist test). XP-band invariant pinned (50-150). Criteria-predicate wiring lands per-scene-PR; skin + soil scenes follow in future rounds; oral lands with a tick-tracking extension to MicrobiomeView. (Previous PR #110 sub-text:) **5 of 8 shipped sixteenth-pass auto-cycle round (PR #110)**. New `MicrobeLabAchievements.phase2` set covers the now-shipped adaptive surface: `firstShapeMatch` (40 XP, first antigen shape recognized) / `memoryAwakened` (60 XP, body re-recognizes a known shape — `memoryCells.recognitionCount ≥ 2`) / `adaptiveRookie` (50 XP, first adaptive wave cleared) / `adaptiveRunner` (140 XP, every adaptive wave cleared) / `librarianOfShapes` (100 XP, all four `AntibodyShape.allCases` recorded in memoryCells). Remaining 3 wait on the oral / skin / soil microbiome scenes. New `MicrobeLabAchievements.allDefinitions` aggregate accessor (Phase 1 + Phase 2 = 15 definitions); `GamificationService.evaluateAchievements` now feeds the full set; `ProgressView` "Achievements" card surfaces `allDefinitions`. `ImmuneGameView.evaluateAdaptiveAchievements` is pure-derivation against the new `@State` flags (`hasMatchedAnyShape` / `hasTriggeredMemoryRecall` / `hasClearedFirstAdaptiveWave` / `hasClearedAllAdaptiveWaves`) + the canonical `librarianOfShapes` predicate against the B-cell scene's `memoryCells` shape-set. 5 new tests in `GamificationServiceTests` (`phase2SetHasFiveAchievements` / `allDefinitionsCoversBothPhases` / `phase2IDsAreUnique` / `allDefinitionsHaveUniqueIDs` — load-bearing: `AchievementEngine` coalesces id collisions silently / `phase2AchievementsAwardXP`).
- [x] Add ForgeAdventure mode: Antibody Lab (timed antigen-matching) — **Hub mode-card wired (seventeenth-pass round Option 4)**. `MicrobeLabHubContribution` (`AppFeature/HubContribution/MicrobeLabHubContribution.swift`) extends `supportedEngines` default to `[.simulation, .defense, .quest, .puzzle]` and adds a `.puzzle` row to `canonicalEngineCopy` with trauma-informed copy: title "Antibody library", tagline "Help the B-cells remember the shape", completion message "Memory cells locked in — your library grew today." `.puzzle` is the natural `ForgeAdventure.GameModeType` case for shape-pattern matching pedagogy (`.defense` stays reserved for the innate macrophage Pac-Man so the two surfaces stay distinct in the hub picker). `MicrobeLabHubChallengeAdapter.engineIconName` extends with `"key.viewfinder"` for the new mode-card. Per `.claude/rules/forgekit.md` § Maximize-ForgeKit-integration interaction note: ForgeAdventure module wiring is SPM-only — no entitlement provisioning required. The hub-orchestrated surface inherits the same `AdaptiveImmunityUnlock` innate-first progression curve as the in-app surface (PR #108). Underlying gameplay (BCellAntibodyMatchScene shipped PR #105 + AdaptiveImmunityUnlock shipped PR #108 + adaptive UI integration shipped PR #109) is tested via `BCellAntibodyMatchSceneTests` (16) + `AdaptiveImmunityUnlockTests` (11) + `ImmuneModeTests` (6); the hub contribution layer's pure-value extension is verified via `swift build --target AppFeature` (green).
- [x] Wire `MicrobeKnowledgeGraph` cross-microbe ecology edges — **closed twenty-second-pass round (PR #140)**. The graph already shipped three edge classes (shared-habitat + same-role + same-kingdom) with `related(toSlug:)` / `relatedByRole(toSlug:)` / `relatedByKingdom(toSlug:)` / `allRelated(toSlug:)` accessors PRs #60 + #113; this checkbox close adds the **codex-view consumer wiring** for the kingdom edge class. `MicrobeCodexCard` (SharedUI) gains an optional `sameKindDisplayNames: [String]` slot rendered as a "Same kind: A, B" caption below the existing "Lives near" line (with a `circle.hexagongrid` glyph to visually distinguish from the `link`-glyph habitat line). `MicrobeCodexView` (AppFeature) computes the new slot via `sameKindDisplayNames(for:)` which filters `graph.relatedByKingdom(to:limit: 4)` to the kid's discovered set + **excludes any name already on the "Lives near" line** so the two captions don't repeat each other (a microbe in the same habitat AND same kingdom only appears once, on the stronger-relation habitat line). 3 new `MicrobeCodexCardTests` pin the default-empty / store / both-cohorts-coexist invariants. Accessibility label preserves the per-piece structure ("Lacto, beneficial microbe. Lives near Bifido. Same kind Akker") so screen readers surface both relations without confusion. Trauma-informed posture preserved: cohort names are filtered to the kid's discovered set so the codex never hints at undiscovered microbes (no shame-mining "you haven't met X" framing).
- [x] Expand mentor `@Generable` set with adaptive-immunity hypotheses — **shipped fifteenth-pass auto-cycle round**. New `AdaptiveImmuneHypothesis` `@Generable` in `MentorGenerables.swift` (observation + memoryHypothesis property pair, property-order-matters per `.claude/rules/foundationmodels.md` so the model writes `observation` first + the testable `memoryHypothesis` references the same shape-matching language without re-deriving it). New `AdaptiveImmuneScenario` enum (`.firstEncounter` / `.matchedShape` / `.recallFromMemory`) covers the canonical pedagogy beats the mentor surfaces during the Phase 2 B-cell antibody-matching arc. New `VeeMentor.fallbackAdaptiveImmuneHypothesis(for:)` + `VeeMentor.adaptiveImmuneHypothesis(for:)` mirror the existing `EcologyHypothesis` pattern (synchronous static fallback per `.claude/rules/foundationmodels.md` § Session Management — always available; async LLM accessor falls back on unavailable / paused / call-fail). **Trauma-informed register** (per `Docs/TECHNICAL_DESIGN.md` § Trauma-Informed Design Posture — adaptive immunity surfaces as the body's library of shapes, NEVER as warfare): authored fallback content pinned by parameterized test stoplist (`fight` / `attack` / `destroy` / `kill` / `war` / `enemy` / `battle` / `weapon` / `soldier` / `warrior`); LLM prompt explicitly forbids the same tokens (defense-in-depth). Hedging-language invariant + shape-matching-register invariant also pinned. 5 new `VeeMentorTests` cover scenario coverage + warfare stoplist + hedging language + shape-matching register + raw-value stability (load-bearing for LLM prompt interpolation).

**Exit criteria**: adaptive minigame integrated; 20-microbe cast complete; 8 question kits live; cross-microbiome navigation works.

---

## Phase 3: Disease Stories + Vaccines

Disease-narrative arcs (always trauma-informed; COVID-sensitive framing), vaccine mechanics, and historical-microbiology context cards.

**Phase 3+ gate scaffolding shipped (PR #137, twenty-first-pass round)**: `Services/Engagement/ProgressionService` wraps `ForgeProgressionManager` with three canonical Phase 3+ gates (`disease-story-immune`: 5 sessions + 3 immune runs; `disease-story-microbiome`: 5 sessions + 5 microbiome scene visits; `global-microbiome-tour`: 8 sessions + 4 distinct ecologies). Trauma-informed register pinned. Consuming surfaces (the disease-story arc views, the global-tour view) wire to `ProgressionService.isUnlocked(_:)` / `.unlockHint(for:)` / `.unlockProgress(for:)` when authored.

- [/] Author 4 disease-story arcs (chosen for kid-developmental safety; coordinate with SAMHSA register per `.claude/rules/trauma-informed-content.md`) — **scaffold landed PR #141; consumer view layer shipped PR #179** (twenty-seventh-pass round). New `AppFeature/Engagement/DiseaseStoryArcView` renders each arc's `ArcPresentation` with warm intro + status chips + per-presentation affordances (`.ready` placeholder body / `.authoringPending` "Coming soon — a reviewer is reading this story now." / `.gatedBehindProgression` ProgressionService.unlockHint copy / `.gatedBehindConsent` "A grown-up turns these stories on in Settings"). All copy stoplist-reviewed for trauma-informed register. Surfaced via `MicrobiomeView` toolbar. Prose body remains reviewer-blocked per ADR-016; the `.ready` branch swaps to render real body content when reviewer-signed-off prose ships. `Models/DiseaseStoryArc.swift` defines the 4 canonical arcs (`.handwashing` / `.vaccinePriming` / `.antibioticStewardship` / `.outbreakRecovery`) with per-arc `displayTitle` + `primitive` + `gateID` (vaccinePriming rides `disease-story-immune`; the other 3 ride `disease-story-microbiome`). `DiseaseStoryAuthoring` enum (`.placeholder` / `.draftAwaitingReview` / `.reviewerSignedOff`) gates prose visibility; consuming views render `"Coming soon"` until reviewer-signoff per `.claude/rules/trauma-informed-content.md` + ADR-016. `Services/Engagement/DiseaseStoryService.swift` (MainActor `@Observable`) computes per-arc `ArcPresentation` (`.ready` / `.authoringPending` / `.gatedBehindProgression` / `.gatedBehindConsent`) by composing the catalog's authoring state with `ProgressionService` gate state + `ParentalConsentService.diseaseStoryArcs` opt-in. Canonical catalog ships all 4 arcs as `.placeholder` in gentlest-first order. 11 unit tests pin catalog order / per-arc gate routing / presentation switch / trauma-informed register stoplist (display titles + primitives must avoid `fight`/`attack`/`war`/`battle`/`weapon`/`kill`/`destroy`/`enemy`/`germ`/`scary`/`fear`). **Prose authoring remains reviewer-blocked**; view-layer consumers + per-arc reviewer-signed-off prose land in a future round per the trauma-informed-content rule.
- [/] Implement vaccine-mechanism mini-explainer (antibody-priming visualization) — **scaffold landed PR #154 + consumer view shipped this round** (twenty-eighth-pass round). New `AppFeature/Engagement/VaccineExplainerView` SwiftUI List renders the canonical 4-beat catalog with per-beat affordances: `.ready` placeholder body until reviewer signoff lands / `.authoringPending` "Coming soon — a reviewer is reading this beat now." / `.gatedBehindProgression` `ProgressionService.unlockHint(...)` copy with per-step warm fallback when ProgressionService isn't threaded / `.gatedBehindConsent` "A grown-up turns this explainer on in Settings." Surfaced via `MicrobiomeView` toolbar next to the disease-story-arcs item (adjacent placement makes the adaptive-immunity → vaccine pedagogy bridge visually contiguous). `AppRootView` instantiates canonical `VaccineExplainerService` `@State` + threads it through. Status chip: book/hourglass/leaf/people. All copy strings stoplist-reviewed for trauma-informed register (no warfare / shame / threat / fear-induction lexicon). 4-beat sequencing pedagogy-aligned: introduction → antibodyPriming → memoryFormation → boosterRationale. Prose body stays reviewer-blocked per ADR-016; the `.ready` branch swaps to render real body content when reviewer-signed-off prose ships. (Previous scaffold PR #154:) `Models/VaccineExplainerStep` (4 pedagogy beats: `.introduction` / `.antibodyPriming` / `.memoryFormation` / `.boosterRationale`) + `VaccineExplainerAuthoringState` (`.placeholder` / `.draftAwaitingReview` / `.reviewerSignedOff`) + `VaccineExplainerStepRecord`. `Services/Engagement/VaccineExplainerService` (MainActor `@Observable`) composes the 4-step catalog with `disease-story-immune` gate state + `ParentalConsentService.diseaseStoryArcs` opt-in to surface a `StepPresentation` switch. Mirrors PR #141 DiseaseStoryService pattern.
- [/] Bundle historical context cards (Pasteur / Koch / Salk / portfolio cast) — **scaffold shipped PR #164 + consumer view shipped this round** (twenty-eighth-pass round). New `AppFeature/Engagement/HistoricalContextCardsView` SwiftUI List renders the canonical 4-card catalog in canonical order (methodology spine Koch → vaccine arcs Pasteur + Salk → kid-scientist register Marshall) with per-card `CardPresentation` affordances mirroring the `VaccineExplainerView` + `DiseaseStoryArcView` pattern. Per-card surfaces: name + era + contribution + bridge chips (cast: Marshall ↔ Pylo from PR #119; cross-portfolio: Pasteur ↔ labsmith, Marshall ↔ curiosityquest) + status chip + presentation body. Per-card `.gatedBehindProgression` warm fallback when ProgressionService isn't threaded. Wired into `MicrobiomeView` next to the vaccine-explainer + disease-story toolbar items (adjacent placement makes the pedagogy bridge methodology-spine → vaccine-arcs → kid-scientist visually contiguous). `AppRootView` instantiates canonical `HistoricalContextService` `@State` + threads it through. All copy strings stoplist-reviewed for trauma-informed register + anti-credentialism gate per CQ CONTENT_STYLE_GUIDE.md § 4.5 (no hero-myth / mortality / warfare lexicon; figures framed as patient observers). Prose body stays reviewer-blocked per ADR-016; the `.ready` branch swaps to render real body content when reviewer-signed-off prose ships. (Previous scaffold PR #164:) `Models/HistoricalContextCard.swift` defines 4 canonical figures (`.koch` / `.pasteur` / `.salk` / `.marshall`) with per-figure metadata. `Services/Engagement/HistoricalContextService` (MainActor @Observable) mirrors DiseaseStoryService pattern with `CardPresentation` switch (.ready / .authoringPending / .gatedBehindProgression / .gatedBehindConsent).
- [ ] Implement disease-story narrative scenes (chunky-cartoon register; no graphic illness imagery)
- [x] Add 4 disease-themed achievements — **shipped PR #152** (twenty-third-pass round). New `MicrobeLabAchievements.phase3` set with 4 entries paired 1:1 with disease-story arcs from PR #141: `handwashHero` (60 XP, "Hands That Care") / `vaccinePrimer` (80 XP, "Library Primer") / `antibioticSteward` (100 XP, "Quiet Steward") / `outbreakHelper` (120 XP, "Helper at the Window"). Names recognize care + curiosity + stewardship per SAMHSA TIP 57 register, NEVER victory / failure / threat framing. XP band 60-120 matches Phase 2 spread. `MicrobeLabAchievements.allDefinitions` now returns 10 + 8 + 4 = 22 achievements. Trauma-informed stoplist pinned (warfare + shame + threat lexicon forbidden). Criteria-predicate wiring lands per-arc-PR (gate fires when each DiseaseStoryArc prose ships reviewer-signed-off).
- [/] Integrate question kits 09-12 (vaccines / herd immunity / hygiene / public health) — **scaffold landed PR #155** (twenty-third-pass round). 4 placeholder kit JSONs (`kit_09_vaccines.json` / `kit_10_herd_immunity.json` / `kit_11_hygiene.json` / `kit_12_public_health.json`) each shipping `"authoring": "draftAwaitingReview"` with deterministic "Reviewer-signoff pending" 1-question stub. New `QuestionKitAuthoring` enum + `QuestionKit.authoring` field (Codable defaults missing values to `.reviewerSignedOff` so Phase 1 + Phase 2 kits surface unchanged). New `QuestionKitService.phase3KitSlugs` + `loadAllPhase3Kits()` (filters out non-`.reviewerSignedOff` kits so draft content NEVER reaches kid surface) + `loadAllShippedKits()` (canonical cross-phase accessor). Trauma-informed register preserved in summary copy. Real content reviewer-blocked per ADR-016; loadAllPhase3Kits() currently returns empty array until reviewer signoff lands.
- [x] Implement crisis-resource surfacing if despair signals detected (per portfolio trauma-informed gate) — **shipped PR #167** (twenty-fifth-pass round). New `Services/Engagement/DespairSignalDetector.swift` (pure-value, `nonisolated`) implements a conservative phrase-level stoplist drawn from SAMHSA TIP 57 + Crisis Text Line training material. Three-tier severity (`.calm` / `.elevatedDistress` / `.elevatedCrisis`); crisis wins on mixed text. Phrase matching is diacritic + case + whitespace-insensitive. `DespairSignalSurface.presentation(for:)` returns a trauma-informed `Presentation` (header + hedge + canonical 3-resource list) when severity warrants surfacing; nil for `.calm`. Reuses the portfolio-canonical `CrisisResources.all` (988 / Childhelp / Crisis Text Line) — surface IS the same one Settings already exposes. `ReflectionEntryStore.append(_:)` screens incoming `.text`-modality entries through the detector + publishes `pendingDespairPresentation` for view consumers; `acknowledgeDespairPresentation()` clears it. **Privacy posture** (per `.claude/rules/age-assurance.md`): detection runs fully on-device; free text passes through + is discarded — only the categorical severity result reaches the consumer. **Trauma-informed posture** (per `.claude/rules/trauma-informed-content.md` § SAMHSA TIP 57 validate-then-inform register): conservative phrase-level matching so ordinary game language ("the pathogens die") doesn't trip; header validates the kid's experience FIRST before surfacing support; hedge names what the app can + can't carry; copy stoplist (`emergency` / `danger` / `should` / `must` / `failure` / `wrong` / `broken` / `fix you`) pinned by parameterized test. 26 new test cases across 2 suites (15 `DespairSignalDetectorTests` + 7 `ReflectionEntryStoreTests` extensions + 4 PR #168 + #170 test-throws backfill).
- [x] Expand mentor `@Generable` with public-health framing — **shipped PR #153** (twenty-third-pass round). New `PublicHealthHypothesis` `@Generable` in `MentorGenerables.swift` (observation + healthHypothesis property pair, property-order-matters per `.claude/rules/foundationmodels.md`). New `PublicHealthScenario` enum (`.handwashing` / `.vaccinePriming` / `.antibioticStewardship` / `.outbreakRecovery`) aligned 1:1 with the disease-story arcs from PR #141 + achievements from PR #152. New `VeeMentor.fallbackPublicHealthHypothesis(for:)` synchronous + `VeeMentor.publicHealthHypothesis(for:)` async LLM accessor mirroring the AdaptiveImmuneHypothesis pattern. Per-scenario register cue: handwashing → care; vaccine → library; antibiotic → patience; outbreak → community. LLM prompt explicitly forbids warfare + shame + threat lexicon (21-token stoplist; defense-in-depth alongside the parameterized fallback stoplist test). 5 new `VeeMentorTests` pin coverage + stoplist + hedging language + care-community register + raw-value stability (load-bearing for LLM prompt interpolation).
- [/] Add parent/educator explainer at the disease-story phase boundary (opt-in) — **scaffold shipped PR #168 + consumer view shipped PR #180** (twenty-seventh-pass round). New `AppFeature/Settings/PhaseBoundaryExplainerView` SwiftUI Form renders the canonical 3-note catalog (disease-story / historical context / global tour) with per-note `BoundaryPresentation` affordances + "Reset acknowledgements" + warm parent intro. Surfaced from `SettingsView` "For parents" section as NavigationLink behind the existing parental-gate math wall. `SettingsView` + `ProfileView` + `AppRootView` extended with matching optional `phaseBoundaryExplainer:` + `progressionService:` parameters. Prose authoring stays reviewer-blocked per ADR-016; the `.readyToInvite` rows currently surface "Coming soon" until reviewer-signed-off prose lands. New `Models/PhaseBoundaryNote.swift` value-type cluster: `PhaseBoundaryNote` enum (3 canonical notes: `.diseaseStoryArcs` / `.historicalContextCards` / `.globalMicrobiomeTour`) + `PhaseBoundaryAuthoring` enum (placeholder / draftAwaitingReview / reviewerSignedOff) + `PhaseBoundaryNoteRecord` with `requiresConsent` axis (disease-story + historical context true; global tour false per ADR-016 ecology+adaptation framing). New `Services/Engagement/PhaseBoundaryExplainerService.swift` (MainActor `@Observable`) — `BoundaryPresentation` 4-case switch (.notReached / .awaitingConsent / .readyToInvite / .alreadyAccepted) composing gate state + consent state + per-note authoring; `acknowledge(_:)` / `resetAcknowledgements()` for non-consent notes. Canonical catalog order: disease-story → historical context → global tour (lowest session-day floor first). Mirrors the DiseaseStoryService / VaccineExplainerService / HistoricalContextService scaffold pattern shipped PRs #141 / #154 / #164. Prose authoring stays reviewer-blocked per ADR-016; view consumer + per-note reviewer-signed-off prose lands in a focused future round. 15 new test cases.

**Exit criteria**: 4 disease arcs ship; vaccine mini-explainer playable; crisis-resource surface implemented; SAMHSA register reviewed.

---

## Phase 4: Microbiome Worlds + Classroom

Cross-microbiome global tour, classroom integration, and App Store submission readiness.

**Phase 4 feature-flag scaffolding shipped (PR #136, twenty-first-pass round)**: `Services/Engagement/ExperimentsService` wraps `ForgeExperiments.ExperimentAssigner` with two pilot experiments — `progressive_disclosure_v2` (staged-unlock cadence; `earlyUnlockTicks: 2` parameter) and `seasonal_content_gate` (Phase 4 seasonal pack gate). Both default 100% control / 0% treatment until a focused Phase 4 round flips the variant weights. Deterministic SHA256(seed|experimentID) bucketing — same kid sees the same variant across launches without persisting.

**Phase 4 seasonal-event consuming surface shipped (PR #143, twenty-second-pass round)**: `Services/Engagement/SeasonalEventService` (MainActor `@Observable`) wraps `ForgeEvents.ForgeEventEngine`. Default packs are culturally neutral (`.seasons` + `.globalCelebrations`); region/religion packs ship opt-in only via `setEnabledPacks(_:)` after a parent-gated handoff. Surface API: `registerBuiltIns()` / `refresh(on:)` / `hasActiveEvent` / `streakEmoji` / `currencyEmoji`. Pairs with the `ExperimentsService.seasonal_content_gate` pilot — the experiment flag flips a treatment weight; the service is the consuming surface that views read.

- [x] Implement global-microbiome tour (Yellowstone hot-spring / deep-sea vent / human gut comparison) — **service scaffold shipped PR #162** (twenty-fourth-pass round) + **view layer shipped PR #169** (twenty-fifth-pass round). `Models/GlobalMicrobiomeTourStop.swift` defines 4 canonical stops (`.yellowstoneHotSpring` / `.deepSeaVent` / `.humanGut` / `.soilUnderground`) with per-stop `displayTitle` + `primitive` + `gateID` (`global-microbiome-tour` from PR #137) + `featuredMicrobeSlugs[]` (Crenarch+Therm / Crenarch+Baro / Lacto+Akker+Bifido / Loam+Nodu+Halo) referencing the 24-microbe cast. `GlobalMicrobiomeTourAuthoring` enum gates prose visibility. `Services/Engagement/GlobalMicrobiomeTourService` (MainActor @Observable) computes per-stop `StopPresentation` (.ready / .authoringPending / .gatedBehindProgression). Distinct from disease-story arcs in NOT requiring parental consent — ecology + adaptation framing. Order is load-bearing: wonder-forward → familiar → expansion (Yellowstone → deep-sea → gut → soil). **Cultural-respect gate** (load-bearing): Yellowstone primitive descriptor explicitly surfaces Indigenous TEK credit per `.claude/rules/distributed-narrative.md` § cultural-sensitivity gates; test pins the credit surface. **View layer (PR #169)**: `AppFeature/GlobalMicrobiomeTourView.swift` SwiftUI List per-stop card surface (title + primitive + featured-microbe row + status badge + body-copy switch on StopPresentation) + Indigenous TEK credit footer rendered at every authoring state. Wired into `MicrobiomeView` via new toolbar item ("Global tour" / `globe.americas.fill`) + optional `globalTour:` + `progression:` init parameters; `AppRootView` instantiates the canonical service + progression `@State` and threads them through. Per-stop reviewer-signed-off prose lands in a focused future round per ADR-016.
- [x] Add extremophile microbe characters (4 more — 24 total) — **shipped PR #151** (twenty-third-pass round). `Services/Resources/microbes.json` extended from 20 → 24 with 4 new neutral extremophile entries (Crenarch / Acido / Cryo / Baro) bridging the Phase 4 global-microbiome tour to named cast members for the four canonical extremophile habitats: hot springs + deep-sea vents (Crenarch — archaea), acid mine drainage + stomach (Acido — bacteria), sea ice + glaciers (Cryo — bacteria), deep-sea sediment under Mariana-Trench pressure (Baro — archaea). UUID namespace `9DB2A1F0-4444-...` (Phase 4 prefix). All 4 ship as `.neutral` — cast stays at 0 pathogenic entries (invariant: 11 beneficial / 11 neutral / 2 opportunistic at 24-microbe scale). Trauma-informed register pinned by parameterized stoplist test (no `scary` / `dangerous` / `deadly` / `kill` / `horror` / `doom` / `suffer` / `violent` / `attack` anywhere on the surface); extremophiles framed as wonder + adaptation pride ("The pressure suits me." / "Frozen is fine. I'll wait."). Codex completion target auto-rescales to 24/24 via `catalog.microbes.count`. 2 new tests (`phase4ExtremophilePackPresent` + `phase4ExtremophilesTraumaSafeRegister`).
- [x] Implement seasonal-microbiome simulation (winter cold + spring allergy seasons — trauma-safe framing) — **state types scaffold shipped PR #161 + scene + view shipped PR #178** (twenty-seventh-pass round). New `GameEngine/Scenes/SeasonalMicrobiomeScene` mirrors the OralMicrobiomeScene lazy-visual-setup pattern; new `AppFeature/SeasonalMicrobiomeView` SwiftUI host (segmented SeasonalLoad picker + tick/undo/reset + per-load mentor cue refresh + `seasonalAwareness` achievement predicate at stableRunThreshold = 8). Per-load mentor copy carries body-affirming framing: cold = "immune library busy" NEVER "sick"; allergy = "noticing pollen" NEVER warfare; pollen = sensory NOT enemy; autumn-settle = "community pauses, gently" NEVER "winding down". Catalog filter prefers `.largeIntestine` / `.smallIntestine` / `.colon` microbes (defensive fallback to full cast when none present so unit fixtures still exercise the scene). `MicrobiomeView` gains optional `seasonalEvents:` param + new toolbar item + navigationDestination. `AppRootView` instantiates canonical `SeasonalEventService` `@State` (culturally neutral default packs). 10 new `SeasonalMicrobiomeSceneTests` pin initial state / load→feedingMode mapping / advance-tick / stable-run extension on winterCold / springAllergy reset / autumnSettle hold / summer-warm Bifido bloom / undo / reset / history-bounded. `Models/SeasonalMicrobiomeState.swift` wraps `MicrobiomeState` in `.largeIntestine` slot + new `SeasonalLoad` enum (`winterCold` / `springAllergy` / `summerWarm` / `autumnSettle`). Per-load `FeedingMode` mapping documented in doc-comment table (winter cold → balanced / spring allergy → sugar / summer warm → fiber / autumn settle → none). Pure-value `nextStableRun(prior:load:)` derivation pinned for the seasonalAwareness achievement criterion. **Trauma-informed posture** (load-bearing): cold = "immune library busy" NEVER "sick"; allergy = "body noticing pollen" NEVER "attacked"; pollen = sensory NEVER enemy. Display-label stoplist test pins these at the per-load caption tier (body-affirming framing; doesn't cross the ADR-016 disease-story-arc reviewer line). Mirrors OralMicrobiomeState (PR #124) / SkinMicrobiomeState (PR #130) / SoilMicrobiomeState (PR #131) pattern. 12 new tests. Scene + view consumers + per-load mentor copy land in a focused future round.
- [/] Integrate question kits 13-16 (extremophiles / global microbiome / microbiome research / synthesis) — **scaffold shipped PR #159** (twenty-fourth-pass round). 4 new placeholder kit JSONs (`kit_13_extremophiles.json` NGSS MS-LS2-3 / `kit_14_global_microbiome.json` NGSS MS-LS2-3 / `kit_15_microbiome_research.json` NGSS MS-LS1-1 / `kit_16_synthesis.json` NGSS MS-LS1-1) each shipping `"authoring": "draftAwaitingReview"` with deterministic "Reviewer-signoff pending" stub. Per-kit register hooks: 13 wonder-pride per ADR-016; 14 cultural-respect + Indigenous TEK credit per `.claude/rules/distributed-narrative.md` § cultural-sensitivity gates; 15 anti-credentialism per CQ CONTENT_STYLE_GUIDE.md § 4.5; 16 cumulative-arc warmth. New `QuestionKitService.phase4KitSlugs` + `loadAllPhase4Kits()` filters out non-`.reviewerSignedOff` so unreviewed content NEVER reaches kid surface. `loadAllShippedKits()` spans Phase 1+2+3+4. 5 new tests + 2 existing accumulator tests extended. Real prose reviewer-blocked per ADR-016.
- [x] Add 8 advanced achievements — **shipped PR #160** (twenty-fourth-pass round). New `MicrobeLabAchievements.phase4` set with 8 entries paired 1:1 with Phase 4 surfaces: `extremophileExplorer` (80 XP, "Edge Explorer") / `extremophileQuartet` (140 XP, "Wonder at the Edge") / `globalTourist` (120 XP, "Cross-World Traveler") / `seasonalAwareness` (60 XP, "Season Settler") / `researchSeed` (100 XP, "First Notebook Page") / `synthesisFinish` (160 XP, "Field Naturalist") / `microbeStudent` (120 XP, "Tended the Cast" — full 24/24 cast discovered, auto-rescales from PR #119 + PR #151) / `quietSteward` (80 XP, "Quiet Steward of Wonder" — 30 sessions without hitting daily cap). XP band 60-160 (slight upper-end stretch over Phase 3's 60-120; floor stays at 60 to match Phase 2/3 minimum). Trauma-informed register stoplist pinned by parameterized test (warfare + shame + victory-framing + threat-induction lexicon forbidden across all 8 titles + descriptions). Quiet-stewardship + tended-the-cast framing recognizes self-paced exploration NEVER frames the journey as a contest. `MicrobeLabAchievements.allDefinitions` now returns 10+8+4+8 = 30 achievements. 5 new tests + 1 existing aggregate test extended. Criteria-predicate wiring lands per-surface-PR (extremophile predicates inherit DiscoveryStore today; global-tour / seasonal / kit-completion / cap-discipline predicates gate on their consumer surfaces shipping in later rounds).
- [ ] Add classroom mode (ForgeKit `ForgeClassroom` integration when 0.94+ module wires)
- [x] Add parent/educator progress reports (`ForgeReporting`) standards-mapped to NGSS MS-LS1-1/2/3 + MS-LS2-3 — **shipped PR #70 + #88 (engagement skeleton + per-standard proficiency) + PR #163 (Phase 2-4 standards extension)**. `ProgressReportService` (Services/Engagement/) wraps `ForgeReporting.ForgeReportGenerator` with `ProgressReportSnapshot` carrying engagement signals + per-standard `StandardProficiency` rows derived from `QuestionAttemptStore`. Phase 1 standards (MS-LS1-1/2/3 + MS-LS2-3 + NHES 1/7) shipped PR #70; per-standard proficiency wiring shipped PR #88. **PR #163 extends to all 4 phases**: `phase2Standards` (new MS-LS2-2 ecology interactions) + `phase3Standards` (MS-LS1-3) + `phase4Standards` (MS-LS1-1 + MS-LS2-3) + `allShippedStandards` deduplicated union by code (stable order: Phase 1 → Phase 2 additions → Phase 3 → Phase 4 additions). 4 new tests in PR #163 pin per-phase coverage + dedup invariant + ordering. `ProgressReportView` (AppFeature/Settings/) renders the Form behind the parental gate. Trauma-informed posture: counts only, on-device only, anonymous `displayName` default ("your kid").
- [ ] App Store submission preparation (privacy nutrition label / KIDSAFE plan / parental gates)
- [ ] App Store screenshot + preview-video assets (await hub distribution per portfolio pipeline)

**Exit criteria**: full 24-character cast; 16 question kits; classroom mode wired; App Store metadata complete.

---

## Phase: Onboarding & Child Safety

COPPA compliance, parental consent, age gates, and first-time experience polish. Runs in parallel with Phase 1 — must land BEFORE TestFlight.

### Onboarding & Child Safety (Excellence Framework)

- [x] **First 60 Seconds experience** — Vee introduction → microscope zoom-in → first microbe meet → celebration → curiosity hook. **Closed PR #98 via the closure audit at `Docs/AUDIT_FIRST_60_SECONDS_AHA_MOMENT_CLOSURE_2026-06-13.md`** (fourteenth-pass auto-cycle round). All five beats of the canonical sequence are wired through `MicrobeLabOnboardingFlow` (welcome / pinch-to-zoom / meet Lacto / microbiome agency / quiz curiosity) — the page bodies match the technical-design-doc literal for the aha line ("one of trillions of tiny lives that help you digest food"). Timing budget hits the 60s target with comfortable headroom at relaxed reading cadence (~10s per page including caregiver scaffolding). Trauma-informed posture verified per-step: Step 1 warmth-forward, Step 3 explicit germ-myth inversion ("Most microbes aren't germs — most are quiet helpers"), Step 4 pre-emptive off-ramp for math-anxious kids ("There's no wrong choice"), Step 5 pre-emptive off-ramp for quiz surface ("Get one wrong? No big deal"). Reference impl: `Packages/Libraries/Sources/AppFeature/Onboarding/MicrobeLabOnboardingFlow.swift:34-60`. No source edits required for closure; the audit doc is the canonical artifact.
- [x] **Aha moment design** — The "I just saw a microbe" moment in session 1. **Closed PR #98 via the closure audit at `Docs/AUDIT_FIRST_60_SECONDS_AHA_MOMENT_CLOSURE_2026-06-13.md`** (fourteenth-pass auto-cycle round). Two complementary surfaces realize the aha: (a) **Surface 1** — `MicrobeLabOnboardingFlow` step 3 introduces Lacto by the canonical opener line that matches `Docs/TECHNICAL_DESIGN.md` § Onboarding & First-Time Experience verbatim; this is the session-1 one-shot aha moment, gated at `AppRootView` BEFORE the tab shell. (b) **Surface 2** — `ExploreView`'s `MentorBubble` refreshes on every tier snap post-onboarding via `refreshMentorCue(for:)` calling `VeeMentor.fallbackZoomCue(for:)`, so the aha CONTINUES across sessions every time the kid pinches to a new tier. Net-additive layers (variable-reward selector + mentor-recall store) deepen the long-tail aha via PR #38 ("X is hanging around today" cold opens) + PR #66 (warm callback layer quoting microbes the kid has previously met). Reference impls: `Packages/Libraries/Sources/AppFeature/Onboarding/MicrobeLabOnboardingFlow.swift:46-49` (canonical aha line) + `Packages/Libraries/Sources/AppFeature/ExploreView.swift:155-158` (per-tier mentor cue refresh). No source edits required for closure; the audit doc is the canonical artifact.
- [x] **Parent handoff flow** — `ParentHandoffFlow` ships a 4-step ~30s setup (welcome → content comfort → daily session cap → ready) that persists into `AppSettings` + flips `ParentHandoffStore.hasCompletedHandoff`. AppRootView gates the kid-facing 5-step onboarding behind it so the grown-up confirms preferences ONCE before handoff. Age question stays deferred to the Apple Declared Age Range API gate item; this surface is preference-capture only — no PII persisted (binary flag).
- [ ] **Age gate** — Apple Declared Age Range API on iOS 26+
- [x] **Parental consent service** — COPPA-compliant consent; annual re-consent per 2026 FTC. `ParentalConsentService` (Services/Engagement/, MainActor `@Observable` UserDefaults-backed) persists per-feature `ParentalConsentRecord` rows with a 365-day expiry (per FTC 2026 § Data retention limits). `ParentalConsentKind` enum carries the 4 distinct feature classes — `.diseaseStoryArcs` / `.weeklySummaryNotifications` / `.externalLinks` / `.classroomMode` — each surfaced as one row in `ParentalConsentManagerView` (AppFeature/Settings/) with a Toggle that calls `recordGrant(for:)` or `revoke(_:)`. Annual re-consent surfaces calmly via the "Needs reconfirm" section (one-tap refresh, never a punitive expired frame). Wired into `SettingsView`'s "For parents" section behind the existing parental gate; `AppRootView` instantiates one canonical `ParentalConsentService` and threads it through `ProfileView` → `SettingsView`. Privacy-first per `.claude/rules/age-assurance.md` § Portfolio Status — slugs + timestamps only, no PII, on-device. 11 unit tests pin fresh-empty state, grant/revoke idempotency, annual-window edges (365 days), cross-instance persistence, raw-value stability (load-bearing for the JSON schema), and trauma-informed copy stoplist for `ParentalConsentKind.description`.
- [x] **Privacy policy** — Plain-language policy accessible from Settings. Canonical doc lives at `Docs/PRIVACY_POLICY.md`; in-app surface `PrivacyPolicyView` (AppFeature/Settings/) renders a sectioned ScrollView with the same plain-language copy (one-line version → what we don't do → what lives on your device → on-device AI → parents + COPPA → crisis resources → questions → changes-to-this-policy). Inline Swift String keeps the view self-contained (no Bundle.module resource processing surprises in production); when the canonical doc changes, both copies update together in the same PR. Wired into `SettingsView` via NavigationLink from the About section's "Privacy policy" row. Settings now wraps the Form in a `NavigationStack` so the NavigationLink resolves. App Store listing surface inherits from the canonical doc when submission ships.
- [x] **Parental gates** — Required for external links and data-sharing permissions. Closed alongside the parental consent service via the same PR. `ParentalConsentService.hasValidConsent(for:)` is the canonical gate that any external-link / data-sharing surface MUST call before proceeding. `.externalLinks` covers crisis-resource `tel:` + `sms:` deep links surfaced from Settings (consumer-side gate wiring lands as those surfaces extend; the consent record is captured + ready). The existing `ParentalGateView` math gate stays the verifier-of-adult mechanism; consents stored via this service satisfy the "verifiable parental consent" record-keeping requirement per FTC 2026 § Opt-in consent for targeted ads / Data retention limits.
- [x] **Progressive disclosure** — Session 1: microscope + codex only → Sessions 2-3: + simulator (Microbiome tab) → Sessions 4+: full feature set incl. Progress + Profile. Shipped via `SessionCountStore` + `TabDisclosure` pure-mapping enum + `AppRootView.tabShell` conditional `Tab(...)` blocks

### Engagement Foundation (Excellence Framework)

- [x] **Streak system** — `StreakStore` (UserDefaults persistence of current / longest / freezes / lastRecordedAt) + `GamificationService.hydrated(from:)` so cold launches read yesterday's state; `recordSession` flushes through the store after the StreakManager actor returns. **Warm broken-streak messaging** lands via `StreakRescueOverlay` (AppFeature/Engagement/) gated on the pure `StreakRescue.from(lastRecordedAt:priorStreak:)` derivation (≥ 2 calendar-day gap with a prior streak ≥ 1 surfaces "The microbiome missed you" copy; long-streak / short-streak / fresh variants in `bodyCopy`). Streak-freeze mercy-day wiring continues to come from ForgeKit's actor; the kid's `availableFreezes` defaults to 2 + persists per session.
- [x] **DDA engine** — Invisible difficulty adjustment across microbiome puzzles + immune game wave count. `DifficultyAdjuster` (Services/Engagement/) is a pure nonisolated value type with three bands (`.introductory` / `.standard` / `.challenging`); `DifficultyAdjuster.from(sessionCount:simplifyChallenge:)` derives the band at session boundaries (1-2 → introductory; 3-4 → standard; 5+ → challenging). Outputs: `immuneWavePathogenCounts(totalWaves:)` (replaces the hardcoded `[4,6,8,10,12]` in `MacrophagePacmanScene`; `.introductory` uses `[3,4,5,6,7]`, `.challenging` `[5,7,9,11,13]`) + `microbiomeSteadyTickThreshold` (6 / 10 / 14). Wired through `AppRootView.tabShell` into `MicrobiomeView` + `ImmuneGameView`. Parent-gated `AppSettings.simplifyChallenge` toggle (Settings → Content gates) pins the band to `.introductory` regardless of session count for kids who want the chill mode permanently. **Trauma-informed posture**: the curve only ever ramps UP from `.introductory`; repeated resets never escalate difficulty. AppSettings now ships a custom decoder so adding fields doesn't invalidate the entire persisted struct.
- [x] **Session targeting** — `SessionTargetService` + `SessionTargetMachine` track the 10-15 min target window per ForgeKit's portfolio default; `phase` returns `.focused` / `.inTarget` / `.overTarget`. UI consumer `SessionNudgeOverlay` (AppFeature/Engagement/) surfaces a trauma-safe gentle stretch suggestion at the in-target boundary (once per session) and a softer pause suggestion past the upper bound; refresh cadence via `TimelineView(.periodic(by: 30))` so the service stays pure
- [x] **Variable rewards** — `VariableRewardSelector` (Services) deterministic-per-session selector with portfolio salt + ~1 in 5 hit cadence (splitmix64 mixer of `(salt, sessionCount)`); returns `.rareMicrobeSighting(slug:)` or `.specialMentorMoment`. `ExploreView` consumes via `init(sessionCount:)` + the static `copy(for:catalog:)` helper that quotes the canonical microbe display name. Trauma-informed copy framed as "hanging around today" / "quiet day under the lens" — never as a chase / loss-aversion mechanic. Hidden-codex-entry reward variant deferred until the codex-discovery surface ships its hidden-entry plumbing.
- [x] **Return loop** — Welcome-back flow for 3+ day lapsed users: `LastActiveStore` UserDefaults-persists last-session timestamp; `AppRootView` surfaces `WelcomeBackOverlay` (warm greeting, trauma-safe copy) when `daysSinceLastActive ≥ 3`. **Recap surface CLOSED PR (fifteenth-pass auto-cycle round)**: `WelcomeBackRecap` (Services/Engagement/, pure nonisolated `Sendable` value type) derives 1-2 previously-met microbe display names from `DiscoveryStore.discoveredSlugs` + the bundled catalog at the moment `loadCatalog` succeeds, threaded through `WelcomeBackOverlay` via the new optional `recap:` parameter. Trauma-informed posture: structurally opt-out (`from(...)` returns `nil` when discovery set is empty so the warm greeting still surfaces without recap card); deterministic per-`daysAway` (refreshing within a session never shuffles the recap microbes — predictability is calming, mirrors `VariableRewardSelector` / `EasterEggDetector` determinism pattern); NEVER mentions undiscovered microbes (no "you haven't met..." framing); copy stoplist (`missed` / `abandon` / `forgot` / `failed` / `should have` / `behind` / `neglect` / `shame` / `fell short` / `compared` / `better than` / `lost` / `didn't`) pinned by parameterized test across days × counts matrix. Selection clamped to ≤ 2 microbes so the recap card stays small + cold-launch surface stays calm. Frozen at catalog-load — microbes discovered during this session don't retroactively change the recap. 10 new `WelcomeBackRecapTests` pin empty-discovery / stale-slugs / partially-known / clamp-to-2 / determinism per `daysAway` / mixer-sensitivity-across-range / pick-uniqueness / copy-stoplist / lead-in-varies-by-count / singular-microbe-uses-lens-register.
- [x] **Retention metrics baseline** — D1 / D7 / D30 cohort signal via `RetentionMetricsStore` (Services/Engagement/). Persists install date + a bounded ring of distinct calendar-day session stamps (cap 32) to UserDefaults; derives `returnedWithinD1` / `D7` / `D30` + `totalDistinctSessionDays`. Privacy-first: counts only, never an event log; stays on-device per `.claude/rules/age-assurance.md` § Portfolio Status. Wired into `AppRootView.task` alongside `sessionCount.incrementForSessionStart()` after onboarding completes. 10 unit tests cover empty / single-day / D1 / D7 / D30 / beyond-window / persistence / capacity / clear paths.

**Exit criteria**: aha moment within 60s; DDA holds flow; engagement loop creates intrinsic return motivation.

---

## Phase: Delight & Parent Integration

Audio/visual/haptic polish, parent-facing dashboards, and emotional design. Runs after Phase 2 minimum.

### Delight & Polish

- [x] **Juice layer** — Visual + audio + haptic trifecta on every interaction (with iPad haptic fallback). **CLOSED per `Docs/AUDIT_JUICE_LAYER_PARTIAL_CLOSURE_2026-06-13.md`** (fifteenth-pass auto-cycle round): visual axis CLOSED via `CelebrationCoordinator` (PR #53); haptic axis CLOSED via `SensoryPaletteCoordinator` (PR #71); audio axis correctly-and-permanently-deferred per `.claude/rules/forgekit.md` § Asset generation ownership (labsmith owns SFX generation portfolio-wide; one-line `AppRootView` wire-up lands inline with the labsmith distribution PR — no MicrobeLab-side implementation work pending). **Visual + haptic axes shipped PR #71**: `SensoryPaletteCoordinator` (Services/Engagement/, MainActor `@Observable`) wraps `ForgeSensory.SensoryPalette`. `AppRootView` instantiates one coordinator + threads it through `MicrobeCodexView` → `QuizView` and `MicrobiomeView` → `ImmuneGameView`. Per-event wiring: QuizView fires `.correctAnswer` / `.incorrectAnswer` on reveal + `.achievement` on per-unlock + `.challengeComplete` on kit-finish; MicrobiomeView fires `.achievement` on per-unlock; ImmuneGameView fires `.streakMilestone(wave)` per wave-clear, `.challengeComplete` on full-run clear, and `.achievement` per immune-defense unlock. The audio dispatch path stays defaulted-nil until the SFX pack lands per `.claude/rules/forgekit.md` § Asset generation ownership — palette plays haptics + `lastEvent` observation immediately. Visual axis remains on `CelebrationCoordinator` (PR #53). 6 new `SensoryPaletteCoordinatorTests` pin init / mirror / monotonic counter / associated-value handling / mascotReaction payload / independent-instances. iPad haptic fallback inherits from `ForgeHapticEngine.shared.playSync(...)` per ForgeKit's canonical engine.
- [x] **Celebration system** — Proportional ForgeCelebration `CelebrationCoordinator` wired at `AppRootView` (single coordinator instance applied via `.celebrationOverlay(coordinator)` on the tab shell), passed down to `MicrobeCodexView` → `QuizView`, `MicrobiomeView` → `ImmuneGameView`. Tier rules: per-wave immune clear → `.medium` (subtle sparkle); full immune run → `.epic` "Defense run complete" (full-screen Lottie via `game-complete` slug with emoji + headline fallback); quiz perfect → `.epic` via `perfectRound(count:)`; quiz near-perfect (n-1 right) → `.major`; quiz partial → `.small` acknowledgement; per-achievement unlock in microbiome → `.major` via `badgeEarned(title:)`. Mentor bubble carries the educational meta-voice; celebration overlay carries the visual/haptic juice — both coexist because `CelebrationOverlayModifier` renders in the view's own overlay envelope while `MentorBubble` renders inline. `CelebrationCoordinator`'s built-in cooldown + tier-precedence rules keep events from stacking. Cinematic "first microbe of new microbiome" still pending the codex-discovery wiring + portrait pack.
- [x] **Micro-delight coverage** — All 8 types CLOSED via prior PRs (formal closure PR #95):

    | Axis | Wiring | Where |
    |---|---|---|
    | **Celebration** | `CelebrationCoordinator` proportional ForgeCelebration ripples + Lottie + chord — quiz perfect → `.epic`; quiz near-perfect → `.major`; quiz partial → `.small` acknowledgement; per-wave immune clear → `.medium`; full immune run → `.epic` "Defense run complete"; per-achievement unlock in microbiome → `.major`. Shipped PR #53; threaded everywhere via `AppRootView` | `CelebrationCoordinator` ([PR #53](https://github.com/nathant99/microbelab-app/pull/53)) |
    | **Surprise** | `EasterEggDetector` — pure nonisolated value type tracks per-session zoom-tier visit history; surfaces a one-shot mentor cue ("You walked the whole range today. X usually only shows up for the careful ones") when the kid hits all 4 tiers in one session; deterministic per-session microbe pick via splitmix64 mixer with salt distinct from the variable-reward selector so the two surfaces decorrelate; trauma-informed warm recognition (never loss-aversion "you missed it") | `EasterEggDetector` (Services/Engagement/) |
    | **Personality** | `MentorRecallStore` (UserDefaults-persisted ring buffer cap 5; FIFO recency dedup) + `VeeMentor.recallCue(for:daysSinceLastSeen:)` gives Cilia a warm callback layer that quotes microbes the kid has previously met; day-bucket pivot (0 / 1 / 2-6 / 7+) drives trauma-informed framing — same-day reads as "still hanging around from earlier today", multi-day reads as "still here when you're ready" (never "missed" / "abandon" / "forgot"); `ExploreView.init()` prefers a recall callback over the variable-reward / default cold-open copy when the store has entries | `MentorRecallStore` + `VeeMentor.recallCue` ([PR #66](https://github.com/nathant99/microbelab-app/pull/66)) |
    | **Mastery** | `MasteryMomentDetector` wired across all 3 of 3 consumer surfaces — ecology via MicrobiomeView (15 stable ticks under fiber feeding); defense via ImmuneGameView (all waves cleared + zero pathogens missed); codex via MicrobeCodexView (12/12 microbes). Canonical trifecta per moment: `.epic` ripple via `daily-complete` Lottie + trauma-informed mentor bubble subline + `.streakMilestone(N)` distinct haptic | `MasteryMomentDetector` ([PR #76](https://github.com/nathant99/microbelab-app/pull/76) ecology / [PR #79](https://github.com/nathant99/microbelab-app/pull/79) defense / [PR #86](https://github.com/nathant99/microbelab-app/pull/86) codex) |
    | **Social** | Share-worthy moments — `CodexCertificate` (codex progress; warm tiered headline scales Microbe Explorer Pass → Microbe Field Notebook → Microbe Naturalist → Microbe Scientist → Codex Complete!) + `ImmuneDefenseTrophy` (macrophage run; Defense Cadet → Defense Apprentice → Defense Specialist → Defense Champion + perfect-run Defense Master tier). Both axes ship `ImageRenderer` → `ShareLink` PNG export pipeline. Both quoteable to parents / friends without exposing PII — only the kid's chosen display name is on the card. Trauma-informed: never compares score / progress to anyone else's; never frames a partial as failure | `CodexCertificate` ([PR #75](https://github.com/nathant99/microbelab-app/pull/75)) + `ImmuneDefenseTrophy` ([PR #82](https://github.com/nathant99/microbelab-app/pull/82)) |
    | **Sensory** | `SensoryPaletteCoordinator` routes haptic feedback across QuizView (`.correctAnswer` / `.incorrectAnswer` / `.achievement` / `.challengeComplete`) + MicrobiomeView (`.achievement`) + ImmuneGameView (`.streakMilestone(wave)` / `.challengeComplete` / `.achievement`); audio dispatch defaults nil until the SFX pack lands per `.claude/rules/forgekit.md` § Asset generation ownership; iPad haptic fallback inherits from `ForgeHapticEngine.shared` | `SensoryPaletteCoordinator` ([PR #71](https://github.com/nathant99/microbelab-app/pull/71)) |
    | **Agency** | `QuestionHintStrategy` (ForgePedagogy `HintTier` scaffolding) + `QuizMachine.requestNextHint()` let the kid escalate vague → medium → specific at their own pace; "Hint" button on QuizView surfaces an inline yellow-tinted hint card per tier; trauma-informed completion-panel framing ("Asked for help on N — that's how learning works") | `QuestionHintStrategy` + `QuizMachine.requestNextHint` ([PR #59](https://github.com/nathant99/microbelab-app/pull/59)) |
    | **Discovery** | `DiscoveryStore` MainActor `@Observable` UserDefaults-backed persistence of microbe slugs the kid has met (pure-additive); MicrobeCodexView taps + ExploreView's rare-microbe sighting + curious-explorer easter-egg cues all write through to the same store so microscope-side meets sync to the codex. Discovered cards with a bundled DN-S chapter surface a `book.closed.fill` glyph + the chapter reader sheet on tap (chapter content axis shipped PR #94) | `DiscoveryStore` ([PR #86](https://github.com/nathant99/microbelab-app/pull/86)) + chapter reader ([PR #94](https://github.com/nathant99/microbelab-app/pull/94)) |
- [/] **Character personality** — `MentorRecallStore` (Services/Engagement/, MainActor `@Observable` + UserDefaults-persisted ring buffer cap 5; FIFO recency dedup) + `VeeMentor.recallCue(for:daysSinceLastSeen:)` give Cilia a warm callback layer that quotes microbes the kid has previously "met". Day-bucket pivot (0 / 1 / 2-6 / 7+) drives trauma-informed framing: same-day reads as "still hanging around from earlier today", multi-day reads as "still here when you're ready" — pinned by `longGapCueAvoidsAbandonmentFraming` test (no "missed" / "abandon" / "forgot"). `ExploreView.init()` prefers a recall callback over the variable-reward / default cold-open copy when the store has entries; `.onAppear` records rare-microbe-sighting slugs into the store so subsequent cold opens can quote them. Cast-member quirk lines per DN voice register continue to surface via `VeeMentor.voiceLine(for:rotation:)`. (PR #66, 2026-06-12)
- [x] **Mastery moments** — Distinct screen ripple + chord when child internalizes microbiome ecology. **Ecology axis shipped PR #76** (8th-pass); **defense axis shipped PR #79** (9th-pass); **codex axis shipped PR #86** (11th-pass auto-cycle sweep). All three axes wired through `MasteryMomentDetector` (Services/Engagement/, pure nonisolated `Sendable` value type) into the three consumer surfaces (`MicrobiomeView` / `ImmuneGameView` / `MicrobeCodexView`). Codex axis adds `DiscoveryStore` (Services/Engagement/, MainActor `@Observable` UserDefaults-backed persistence of microbe slugs the kid has met; pure-additive). Tap-to-discover on `MicrobeCodexView` cards + sync-write from `ExploreView`'s rare-microbe sighting + curious-explorer easter-egg cues mark microbes discovered; reaching 12/12 fires the canonical trifecta (`.epic` `personalBest` ripple via `daily-complete` Lottie + `.streakMilestone(12)` haptic + trauma-informed mentor subline "You met every microbe in the catalog. The whole cast knows you now."). `MasteryMomentDetector` (Services/Engagement/, pure nonisolated `Sendable` value type) tracks per-session mastery thresholds for `.ecologyMaster` / `.defenseMaster` / `.codexMaster` with `acknowledged: Set<MasteryKind>` so each kind fires once per session. Threshold `MasteryMomentDetector.ecologyMasteryStableTickThreshold` = 15 consecutive stable ticks under fiber feeding (≥ 3 stability-milestone events; demonstrates feeding-mode → growth-rate causality internalization). `MicrobiomeView` carries the detector as `@State`, fires `evaluateEcologyMastery()` after every tick; on a returned `Moment` it: (a) fires `CelebrationCoordinator.personalBest(metric:value:)` (`.epic` tier ripple + chord via the `daily-complete` Lottie), (b) carries the moment's subline into the mentor bubble for calm narration, (c) fires `SensoryPaletteCoordinator.streakMilestone(threshold)` for distinct haptic. `ImmuneGameView` mirrors the ecology pattern via `evaluateDefenseMastery()` (PR #79): called from `surfaceWaveClearCue(finished:)` in the `finished == true` branch (the step-button advance gate already enforces zero pathogens before `clearWave()`, so any `finished == true` path is a perfect run); on a non-nil `Moment` fires the canonical trifecta (`.epic` `personalBest` ripple + mentor bubble subline + `.streakMilestone(scene.wave)` haptic) layered over the existing wave-clear celebration at a different Lottie slug + different haptic cue so the kid feels the moment without screen-flicker. Detector also surfaces `recordCodexDiscovery(totalDiscovered:totalAvailable:)` (12/12 microbes) for follow-up wiring in `MicrobeCodexView` (waits on codex-discovery wiring landing in that surface). Trauma-informed: copy stoplist (`finally` / `at last` / `you almost` / `failed` / `behind` / `should have` / `compared to` / `better than`) pinned by parameterized test across all three moment kinds. 12 `MasteryMomentDetectorTests` pin per-kind threshold + one-shot behavior + acknowledge idempotency + trauma-safe register.
- [x] **Easter eggs** — Hidden rare-microbe encounters rewarding curious zoom exploration. `EasterEggDetector` (Services/Engagement/) is a pure nonisolated value type that tracks per-session zoom-tier visit history; `record(visit:)` returns `true` on the snap that completes all four tiers. `ExploreView` owns the detector as `@State`, surfaces a one-shot mentor cue ("You walked the whole range today. \(microbeName) usually only shows up for the careful ones — thanks for looking.") when the kid hits all 4 tiers, then acknowledges so it doesn't re-trigger mid-session. Microbe pick is deterministic per session via a splitmix64 mixer with a salt distinct from `VariableRewardSelector.appSalt` so the two engagement surfaces decorrelate. Trauma-informed posture: warm recognition, never loss-aversion ("you missed it"); the easter egg can re-trigger across sessions but never within one.
- [x] **App Intents (Siri / Spotlight / Shortcuts)** — `OpenMicroscopeIntent` + `OpenCodexIntent` + `OpenMicrobiomeIntent` (AppFeature/Intents/) conform to Apple's `AppIntent` protocol with `openAppWhenRun = true` + `LocalizedStringResource` titles + `IntentDescription`s. Each intent's `perform()` method calls `Services.NavigationCoordinator.shared.requestTab(_:)` to broker the tab switch back to the running app. `MicrobeLabAppShortcuts` (AppFeature/Intents/, conforms to `AppShortcutsProvider`) exposes all three intents as Siri-callable shortcuts with kid-friendly phrases ("Open Microscope in MicrobeLab" / "Show me the microbe codex in MicrobeLab" etc.) + SF Symbol icons. `MicrobeLabTab` (Models/) is the canonical `nonisolated public enum: String, Hashable, CaseIterable, Sendable, Codable` carrying the 5 top-level tab slugs (explore / codex / microbiome / progress / profile). `NavigationCoordinator` (Services/, MainActor `@Observable` singleton) brokers App Intent → tab-switch requests via `requestTab(_:)` / `clearRequest()` / `requestedTab: MicrobeLabTab?`. `AppRootView.tabShell(catalog:)` binds `TabView(selection: $selectedTab)` with `value: MicrobeLabTab.xxx` per Tab + observes `navigationCoordinator.requestedTab` via `.onChange(of:)` — applies the request iff the target tab is currently visible in the progressive-disclosure shell (e.g., a Shortcut targeting Microbiome silently no-ops on session 1 when the tab isn't yet surfaced), then clears the request so the same intent can re-fire later. Trauma-informed posture: the coordinator never auto-records analytics or fires haptics on intent-driven switches — the existing per-tab observers fire because the tab change reaches the same code paths a manual tap would. 9 new `NavigationCoordinatorTests` pin init defaults / requestTab / clearRequest idempotency / overwrite / all-cases requestable / MicrobeLabTab raw-value stability / Codable roundtrip / shared singleton identity. **No Info.plist edits required** — `AppShortcutsProvider` discovery uses runtime metadata. **No entitlement edits required** — the AppIntents framework is system-bundled. Closes the maximize-ForgeKit-integration loop: Apple's `AppIntents` framework natively wired without an Xcode-managed file edit (per the ten-pass invariant); future ForgeKit `ForgeIntents` helper APIs can layer on top.
- [x] **Share-worthy moments** — Both axes shipped: codex completion certificate via **PR #75** (eighth-pass auto-cycle sweep) + immune-defense run trophy via **PR #82** (tenth-pass auto-cycle sweep). Both follow the same trauma-informed posture, ImageRenderer → ShareLink pipeline, and on-device privacy posture. **Codex axis (PR #75)**: `CodexCertificate` (Services/Engagement/) snapshots the kid's discovery progress at the moment they tap "Share my codex" in the Progress tab. Headline scales warmly (`Microbe Explorer Pass` → `Microbe Field Notebook` → `Microbe Naturalist` → `Microbe Scientist` → `Codex Complete!`); subline never benchmarks the kid against anyone else; trauma-informed stoplist (`missed` / `failure` / `behind` / `should` / `must`) pinned by `CodexCertificateTests`. `CodexCertificateView` + `CodexCertificateSheet` (AppFeature/Engagement/). 8 `CodexCertificateTests`. **Immune-defense axis (PR #82)**: `ImmuneDefenseTrophy` (Services/Engagement/, pure nonisolated `Sendable` value type) snapshots the kid's macrophage-minigame run at the moment they tap "Share trophy" in the score HUD after a full or partial completion. Headline scales warmly (`Defense Cadet` → `Defense Apprentice` → `Defense Specialist` → `Defense Champion`) with a distinct top tier (`Defense Master`) gated on the same perfect-run criteria as `MasteryMomentDetector.recordDefenseRunComplete` (all waves cleared AND zero pathogens missed). Subline mirrors the codex pattern ("You held the line through N waves" — never compares score to anyone else's, never frames a partial clear as failure); trauma-informed stoplist (`missed` / `failure` / `behind` / `should` / `must` / `almost` / `fell short` / `compared` / `better than` / `lost`) pinned by `ImmuneDefenseTrophyTests` parameterized across `waves × perfect` matrix with a single literal-allowance carve-out for the perfect-master `"zero pathogens missed"` positive-framing subline. `ImmuneDefenseTrophyView` + `ImmuneDefenseTrophySheet` (AppFeature/Engagement/) mirror the codex card layout + `ImageRenderer` → `ShareLink` PNG export pipeline. `ImmuneGameView` gains a "Share trophy" button in the score HUD that becomes available the moment the run reaches `isComplete`; the trophy snapshot is frozen at the moment the kid taps share so future-session activity doesn't retroactively change a shared trophy. Sheet copy frames sharing as quiet recognition, NEVER engagement pressure. 10 new `ImmuneDefenseTrophyTests` pin headline tiers + perfect-run gating + singular/plural copy + Codable roundtrip + trauma-informed copy invariants.

### Parent Integration

- [x] **Progress dashboard** — Parent-facing standards-mapped view (NGSS MS-LS1-1 / MS-LS1-3 etc.). **Engagement skeleton landed PR #70**; **per-standard proficiency closed PR #88** (eleventh-pass auto-cycle sweep). `QuestionAttemptStore` (Services/Engagement/, MainActor `@Observable` UserDefaults-backed log of per-question `QuestionAttempt` rows — questionID + kitSlug + curriculumStandard + wasCorrect + answeredAt; PII-safe per `.claude/rules/age-assurance.md` § Portfolio Status, FIFO eviction at 500 records). `proficiencies(matching:)` derivation builds `[ForgeReporting.StandardProficiency]` from the log + scopes to standards the kid has actually attempted. `ProgressReportSnapshot.standardProficiencies` carries the proficiencies through the existing 3-layer plumbing without per-layer threading. `ProgressReportService.reportData(for:)` now derives `averageScore` from the proficiency mean (was hard-coded 0). `QuizView` writes attempt rows at the `.reveal(against:)` boundary; threading happens through `AppRootView` → `MicrobeCodexView` → `QuizView`. Strengths / Growth Areas sections render once the kid has answered tagged questions. (Previous PR #70 sub-text:): `ProgressReportService` (Services/Engagement/, pure nonisolated value type) wraps `ForgeReporting.ForgeReportGenerator`, building a `StudentReportData` snapshot from live engagement signals (session count / total time / streak / longest streak / XP / achievement count / distinct session days) and rendering the canonical `ForgeReportGenerator.parentConferenceReport(_:)` text. `phase1Standards` exposes the 4 NGSS MS-LS standards + 2 NHES standards the Phase 1 kits ship with so the parent surface lists "Standards covered" without runtime catalog scans. `ProgressReportView` (AppFeature/Settings/) is a sectioned Form that surfaces engagement counts + standards-covered list + the generated parent-conference text (selectable + monospaced for screen-reader friendliness). `SettingsView` gains a "For parents" section gated behind the existing `ParentalGateView` math-gate; the section only appears when `AppRootView` plumbs an optional `ProgressReportSnapshot` through `ProfileView` → `SettingsView`. Trauma-informed posture: counts only, on-device only, anonymous `displayName` default ("your kid"). Per-standard proficiency surfaces (strengths / growth areas) follow when per-question attempt logs land — `ForgeReportGenerator` gracefully skips those sections when empty, so the today-shipped surface is the engagement skeleton. 7 new `ProgressReportServiceTests` pin the phase1Standards coverage + reportData mapping + averageSessionMinutes division-by-zero safe / parentReportText anonymity + low-sessions recommendation flow + empty-standards skip + NGSS/.custom framework assignment.
- [x] **Parental controls** — Daily session time limits (default 30 min) + content-comfort filters (e.g., disease-story arc opt-in). ForgeKit `SessionTimerService` (actor) is the source of truth for cumulative on-device daily time; `DailyTimeCoordinator` (Services/Engagement/, MainActor `@Observable` wrapper) adapts the actor for SwiftUI consumption. AppSettings.dailySessionCap drives the cap (15 / 30 / 45 / 60 / unlimited); the wrapper rebuilds the underlying `SessionTimerConfig` on cap change. `unlimited` collapses to a 24h cap so the timer never trips. AppRootView wires start/end/pause/resume against `scenePhase` (active → start, background → endSession flush, inactive → pause). When the kid crosses the cap, `DailyCapOverlay` (AppFeature/Engagement/) surfaces a centered trauma-safe wrap-up: warm "Great session" copy with a single "See you next time" acknowledgement; never force-quits the app. Centered-overlay priority places the daily-cap above streak-rescue / welcome-back so the kid sees a calm wrap-up on the launch they crossed the cap. Disease-story content-comfort filter is wired via the parent-handoff flow's `diseaseStoryGateEnabled` preference (defaults ON; pre-Phase-3 has no surfaces to gate). 5 unit tests pin the coordinator's MainActor wrapper behaviors (init defaults / explicit cap / unlimited acceptance / idempotent updateCap / cap swap).
- [x] **Weekly summary** — Opt-in progress notification (strengths, growth areas, recommendations). `WeeklySummaryService` (Services/Engagement/, MainActor `@Observable`) wraps a `UserNotificationScheduler` adapter. Production-side `SystemNotificationScheduler` calls `UNUserNotificationCenter.current()`; tests inject a recording fake. Trigger defaults to Saturday morning 9am via `WeeklySummaryTrigger(weekday: 7, hour: 9, minute: 0, repeats: true)`. Body composition lives in `WeeklySummaryNotificationContent.compose(from:)` — pure `nonisolated` function that reads `ProgressReportSnapshot` + builds a trauma-safe title + body. Quiet-week edge case ("A quiet week — that's allowed. The microbes are still here when you want to look.") is the canonical zero-engagement framing; active-week composition includes badges (singular/plural correct), multi-day return acknowledgement, never loss-aversion or compare-to-last-week language. Wiring: `AppSettings.weeklySummaryNotificationEnabled` (defaults OFF per FTC 2026 opt-in rule). SettingsView "For parents" Toggle is gated on three checks orchestrated by the binding: (a) parental gate adult-confirm, (b) `ParentalConsentService.recordGrant(for: .weeklySummaryNotifications)`, (c) `weeklySummary.requestAuthorization()` system prompt. System denial rolls back the toggle so the row reflects actual state. `AppRootView.task` re-schedules with the freshest snapshot every cold launch when the toggle is on so Saturday's notification mirrors current engagement. Local-only (`UNCalendarNotificationTrigger`); no push tokens; no cloud; no third-party SDK. 12 unit tests pin composer quiet-week + active-week + singular-badge + cross-snapshot stoplist + category-identifier stability + service authorization propagation + schedule recording + cancel reset + trigger defaults + custom-trigger threading + request-identifier stability.
- [x] **Session closer** — End-of-session summary surface. `SessionSummary` (Services/Engagement/) is a pure nonisolated value type that captures a frozen snapshot of session-relevant stats (currentLevel / totalXP / currentStreak / microbesDiscovered / achievementsEarned) at the moment the kid taps "Wrap up today" in the Progress tab; future-session activity doesn't retroactively change a summary the kid has seen. **ForgeKit 0.99.0 ReflectionPromptModifier integration landed PR #100** (fourteenth-pass auto-cycle round): `SessionSummarySheet` now exposes an optional "Add a reflection" affordance below the canonical "See you next time" CTA; the kid taps in → the summary sheet dismisses → `ReflectionPromptSheet` (`ForgeUI.Reflection`) opens in sequence with the canonical `MicrobeLabReflectionPrompts.sessionClose` prompt ("What's one thing the microbes showed you today?") with `.text` + `.emoji` + `.skip` modalities. Persistence via the new `ReflectionEntryStore` (Services/Engagement/, MainActor `@Observable` UserDefaults-backed, ring-buffered cap 50, FIFO eviction, JSON-encoded `ForgeModels.ReflectionEntry` rows, corrupt-payload-resilient with fresh-start fallback, `parentVisible: false` for v1 per the kid-private journal posture). Voice + drawing modalities deliberately deferred — they require `NSMicrophoneUsageDescription` + PencilKit which would route through `Docs/HANDOFF_TO_USER_<TOPIC>.md` for entitlement provisioning. Trauma-informed posture: prompt copy frames reflection as warm noticing rather than performance review; the `.skip` off-ramp is precondition-enforced by `ReflectionPromptConfig.init`. Wired into both call sites (the auto-surfaced session-summary on `AppRootView` background-resume + the kid-initiated session-summary on `ProgressTabView.Wrap up today`); the affordance is opt-in via the new `onAddReflection: (() -> Void)?` parameter so call sites that pass nil continue working unchanged. 10 new `ReflectionEntryStoreTests` pin fresh-empty / append-order / capacity-eviction / prompt-id-filter / purge-older-than / clear / cross-instance-roundtrip / corrupt-payload-recovery / skip-entry-no-text. `SessionSummarySheet` (AppFeature/Engagement/) renders the snapshot as a `presentationDetents([.medium])` sheet with warm headline ("Quiet today — that's allowed" / "Solid session" / "You explored a lot today" branches), stats grid, and gentle next-session preview ("pick up where we left off" / "try a different feeding mode"). Trauma-informed: the sheet NEVER frames the streak as a threat or absence as failure. Wired into `ProgressTabView` via a toolbar "Wrap up today" button. 6 unit tests pin the trauma-safe copy invariants (zero-streak preview never mentions the broken streak; quiet-session headline always includes "allowed"). **Auto-surfacing on app background landed PR #61** — `AppRootView` stamps `sessionStartedAt` + `sessionStartXP` on cold launch + every `.background → .active` resume; on `.background` the productivity gate fires (`captureSessionSummaryIfProductive`) requiring elapsed ≥ 60s AND XP earned > 0; the next `.active` surfaces `pendingSessionSummary` via `.sheet(isPresented:)` UNLESS the daily-cap / welcome-back / streak-rescue overlays have precedence in the centered-overlay priority chain. Trauma-informed: short backgrounds (notification check) NEVER fire the summary; quiet sessions (0 XP) NEVER fire it; the summary only celebrates earned engagement.

---

## Phase: Accessibility & Trauma-Informed Polish

- [ ] VoiceOver labels for every microscope LOD sprite + cast portrait — asset-blocked on the 12-microbe portrait pack distribution; SpriteView top-level VoiceOver labels (the audit's GAP 1) closed PR #69
- [x] Dynamic Type support across all SwiftUI views — **PASS portfolio-wide** per `Docs/AUDIT_ACCESSIBILITY_PASS_2026-06-12.md` § Per-surface state (every shipped surface uses semantic font tokens — `Font.headline` / `.title2` / `.caption` / `.callout` / `.monospacedDigit` etc.); no surface hardcodes pixel font sizes; Form / ScrollView / VStack hosts adapt cleanly to a11y XL/XXL/XXXL sizes. Formal closure PR #96 — no source edits required; the audit is the canonical artifact. New surfaces shipped post-audit (PR #94 `ChapterReaderSheet`) continue the same semantic-font pattern (`.title2.weight(.semibold)` for title + `.body` for body + `.caption` for chips), so the property is preserved going forward.
- [x] Color-contrast audit (WCAG AA in default + dark + high-contrast themes) — **PASS portfolio-wide** per `Docs/AUDIT_ACCESSIBILITY_PASS_2026-06-12.md` § Per-surface state (lowest contrast in the shipped surface area is `Color.primary.opacity(0.08)` solid fallback ≥ 4.5:1 against both light + dark mode; every shipped tinted glass surface honors the `accessibilityReduceTransparency` env var via `A11yPreferences` and swaps to a solid `Color.primary.opacity(...)` fill that adapts to light/dark mode; role chips on `MicrobeCodexCard` rest on `.opacity(0.2)` tinted capsules over solid backgrounds — `green` / `gray` / `orange` / `red` all clear AA against the role-text color). Formal closure PR #96 — no source edits required; the audit is the canonical artifact.
- [x] Reduce-Motion variants for celebration + tier-transition animations — `A11yPreferences` (Services/) is a pure nonisolated value type that OR-combines the system `accessibilityReduceMotion` env with the parent-gated `AppSettings.forceReduceMotion` toggle. Wired into `AppRootView` (overlay morph + animation), `SessionNudgeOverlay` (slide-from-top transition + easeInOut), and the two centered overlays (`WelcomeBackOverlay` / `StreakRescueOverlay` no longer scale-morph when Reduce-Motion is active). Scene-side tier-transition + celebration polish lands with the LOD sprite atlas item (still asset-blocked).
- [x] Reduce-Transparency variants for any glass UI (per portfolio Liquid Glass policy) — same `A11yPreferences` surface. `SharedUI.MentorBubble` + `SharedUI.MicroscopeHUD` read `@Environment(\.accessibilityReduceTransparency)` directly (system-only — they're outside the AppSettings dep graph); AppFeature-level overlays (`AppRootView`, `SessionNudgeOverlay`, the two centered overlays) combine system + force via the resolver. Glass surfaces swap to a solid `Color.primary.opacity(...)` fill that adapts to light/dark mode. Liquid Glass on Chrome (TabBar / NavBar / etc.) stays unmodified per portfolio policy.
- [x] Trauma-informed gate review for disease-story arcs (SAMHSA TIP 57 register; off-ramps + crisis-resource surface; cultural-context note for global-microbiome cards) — **structural-tier audit shipped twenty-sixth-pass auto-cycle round (PR #3 of 5)** at `Docs/AUDIT_TRAUMA_INFORMED_GATE_REVIEW_DISEASE_STORY_2026-06-16.md`. Per-scaffold verdict matrix covers 6 scaffolds (`DiseaseStoryArc` + `VaccineExplainerStep` + `HistoricalContextCard` + `DespairSignalDetector` + `PhaseBoundaryNote` + `GlobalMicrobiomeTourStop`); cross-cutting SAMHSA TIP 57 six-principles + six off-ramp-requirements + crisis-resource surfacing audited; all PASS at structural tier. Prose authoring remains `.placeholder`-gated per ADR-016 reviewer signoff; **skip-with-summary** + **audio-only** off-ramps deferred to the prose tier + Phase 2 audio drama pipeline per the audit's "Constraints NOT covered" § — those are downstream gates, not blockers for this checkbox.
- [x] Crisis-resource list (988 / Childhelp / Crisis Text Line) surfaced from Settings — `CrisisResources` (Services) is a pure nonisolated value type with the portfolio-canonical safety hotlines + a `CrisisResource` row struct (id / title / subtitle / actionLabel / actionURL). `CrisisResourceCard` (AppFeature/Settings/) renders a Form section with one tappable row per resource; taps deep-link into the system phone / messages app via `tel:` / `sms:` URLs (no in-app modal that could trap a kid mid-crisis). Wired into `SettingsView` above the About section. Section header "If you need to talk to someone" + footer copy "You don't have to be in a crisis to call — 'not sure' is reason enough" applies SAMHSA TIP 57 validate-then-inform register. Disease-story arc integration deferred to Phase 3 when the arcs ship.

**Exit criteria**: A11y audit PASS; trauma-gate sensitivity reviewer signoff per ADR-012 / ADR-016 protocol.

---

## Cross-references

- `@Docs/TECHNICAL_DESIGN.md` — architecture + state machines + domain model
- `@Docs/IMPLEMENTATION_HANDOFF.md` — hub-shipped implementation context
- `@Docs/HANDOFF_FROM_LABSMITH_DISTRIBUTED_NARRATIVE_RETROFIT.md` — 12-character cast definition
- `@Docs/HANDOFF_FROM_LABSMITH_DN_S_STORY_PER_CHARACTER.md` — DN-S chapter-depth backstories
- `@.claude/rules/forgekit.md` § Module Catalog — ForgeKit 0.99 surface
- `@.claude/rules/spritekit.md` § Lazy Visual Setup — microscope + simulator scene pattern
- `@.claude/rules/state-machines.md` — ZoomMachine + SimulationMachine pattern
- `@.claude/rules/trauma-informed-content.md` — COVID-sensitive disease-story register
- `@.claude/rules/foundationmodels.md` — `@Generable` + fallback discipline for Vee
