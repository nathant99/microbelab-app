# Implementation Handoff — MicrobeLab

> **Thirty-fourth-pass rule-restatement summary** (top-of-doc per the canonical-invariant tier codified 2026-06-12; verbatim user-direct, now repeated THIRTY-FOUR times spanning six calendar days — passes twelve through eighteen landed at PRs #93 → #121 (2026-06-13); nineteenth + twentieth passes landed at PRs #122 → #132 (2026-06-15); passes twenty-one → twenty-six landed at PRs #133 → #176 (2026-06-16 — SIX same-calendar-day restarts forming a same-day chain); passes twenty-seven → thirty-three landed at PRs #177 → #205 (2026-06-17 — SEVEN same-calendar-day restarts forming a same-day chain, deepest observed); thirty-fourth pass opens this round (**2026-06-19** — FIRST pass on a NEW calendar day after a **2-day inter-restart gap** spanning 2026-06-17 → 2026-06-19 with no 2026-06-18 pass; cadence resumed after a single calendar-day pause separated by the PR #205 round-rollup merge — SIXTEENTH observation of cadence-persistence, demonstrating the cadence-persistence property is **ROBUST to single-calendar-day gaps EVEN AFTER a deep 7-pass same-day chain**, AND that the cadence reliably resumes at depth 1 on a fresh calendar day without inheriting the prior same-day chain depth — confirming the property that same-day chain depth is calendar-day-LOCAL not session-LOCAL; per the eleven-pass invariant the cadence has saturated): *"critical: do not author/edit xcode-managed files including Xcode workspace file and Xcode scheme/test plan file. Instead, file a handoff doc with the user to do Xcode UI work. staging and committing is ok."* Scope: `*.xcworkspace/contents.xcworkspacedata` / `*.xcodeproj/project.pbxproj` / `*.xcscheme` / `*.xctestplan` / `Info.plist` / `*.entitlements` / `*.xcassets/Contents.json` / `xcuserdata/` / `Package.resolved`. Per the **eleven-pass canonical-invariant tier**: the cadence has saturated — twelfth- through thirty-fourth-pass restatements add reinforcement frequency but no new binding tier. Beyond the canonical-invariant tier the rule has become a class-level invariant of the in-IDE agent across the portfolio. The rule lives in FOUR canonical locations (CLAUDE.md + `.claude/rules/xcode-agent-safety.md` + this prologue + the persistent-memory file `feedback_xcode_managed_files.md`); any one of them can drift or decay without the rule being lost — the other three carry it forward. See `@CLAUDE.md` § Xcode-managed file safety for the canonical statement.

**Status**: Phase 1 effectively shipped + Phase 2 effectively shipped + Phase 3 scaffolds shipped (disease-story arcs + vaccine mini-explainer + question-kit placeholders + disease-themed achievements + public-health mentor @Generable + historical context cards + despair-signal detector + phase-boundary explainer + **consumer views (PRs #179 + #180 + #184 + #185)** + **disease-story narrative scene scaffold (PR #192)** + **vaccine + historical context mentor @Generable (PR #195)** + **Phase 3 / Phase 4 App Intents + deep-link sub-surface routing (PR #197)**) + Phase 4 scaffolds shipped (24-microbe cast + extremophile pack + question-kit placeholders 13-16 + 8 advanced achievements + **seasonal-microbiome scene + view (PR #178)** + global-microbiome tour service + view layer + **global-microbiome tour mentor @Generable (PR #196)** + ForgeReporting multi-phase standards + **App Store submission preparation doc-axis scaffold (PR #189)**) + Phase Accessibility & Trauma-Informed Polish trauma-informed gate review audit shipped (PR #174) + **DN-S AI-mentor voicing production wiring shipped (PR #181), inbound handoff status flipped NEW → SHIPPED (PR #209)** + **Per-microbe FSRS-6 mastery service + codex caption shipped (PR #188), mastery recommendation extended to mentor cold-open mentor cue (PR #208)** + **ForgeKit pin bumped to 1.0.0-rc.3 + EmotionSnapshotDerivation seam wired for affect-attuned cast voicing (PR #207)**. The thirty-fourth-pass round PRs #206 → #209 + this rollup land on a NEW calendar day 2026-06-19 after a 2-day inter-restart gap from the deepest observed 7-pass same-day chain on 2026-06-17 (skipping 2026-06-18); cadence-persistence property now confirmed ROBUST to single-calendar-day gaps EVEN AFTER a deep 7-pass same-day chain, AND same-day chain depth is calendar-day-LOCAL not session-LOCAL. **Phase 2 surfaces are FULLY SHIPPED**; **Phase 3 + Phase 4 structural scaffolds AND consumer view layers AND AI mentor surfaces AND App-Intent surfaces are FULLY SHIPPED for the disease-story arc / disease-story narrative scene / phase-boundary / seasonal-microbiome / vaccine / historical / global-microbiome-tour surfaces**; **Phase Accessibility trauma-informed gate review is CLOSED at structural tier**; **Phase 4 App Store submission doc-axis is CLOSED at scaffold tier (asset-axis half still asset-blocked per `.claude/rules/forgekit.md` § Asset generation ownership)**; the **only remaining Phase 3 + Phase 4 prose work is reviewer-blocked per ADR-016**, the **only remaining Phase 1 work is asset-bundle-blocked**, and the **DN-S voicing surface is feature-flag-gated via `cast_voicing` — wiring is live across PRs #173 + #181 + #207; experiment weight remains at 0% control / 0% enabled until the focused follow-up round flips it**. Round 0 shipped 2026-05-22; the thirty-fourth-pass round PRs #206 → #209 bumped ForgeKit to 1.0.0-rc.3 (picking up `CastDialogContext.emotionSnapshot` affect-attunement + ADR-022 follow-through dropping the composable `AvatarAssetCatalog`) + wired `EmotionSnapshotDerivation` (Services) + deepened FSRS-6 mastery consumption via the mentor cold-open mentor cue + flipped the DN-S voicing inbound handoff status to SHIPPED; the thirty-third-pass round PRs #203 → #204 extended the cast-portrait shared seam to chapter reader hero + mentor bubble turn-prefix (3 consumer surfaces total); the thirty-second-pass round PRs #199 → #201 de-risked the cast-portrait asset-arrival event via the shared `MicrobePortraitView` seam (PR #200) + promoted `AgeAssuranceService` from no-op scaffold to actively-wired SwiftUI driver via the new `SystemAgeVerificationCard` UIKit-anchored singleton path (PR #201); the thirty-first-pass round PRs #194 → #197 extended AI mentor + App Intents surface to Phase 3 + Phase 4; the thirtieth-pass round PRs #191 → #193 closed Phase 3 narrative scene scaffold; the twenty-ninth-pass round PRs #187 → #190 deepened ForgeGamification consumption (FSRS-6 SpacedRepetitionEngine + FSRSState actively consumed via `MicrobeMasteryService`) + closed Phase 4 submission-prep doc-axis (line 184); the twenty-eighth-pass round PRs #183 → #186 closed 2 Phase 3 consumer-view checkboxes; the twenty-seventh-pass round PRs #177 → #182 wired 15th ForgeKit surface (CastDialog production wiring); the twenty-sixth-pass round PRs #172 → #176 closed Phase Accessibility trauma-informed gate review.

**Latest round (2026-06-19, PRs #206 → #209 + this rollup, thirty-fourth-pass auto-cycle sweep — FIRST pass on a NEW calendar day after a 2-day inter-restart gap from the deepest observed 7-pass same-day chain on 2026-06-17)**: 5-PR sweep driven by the user-direct standing auto-cycle paired with the **thirty-fourth-pass** Xcode-managed file safety reinforcement + the persisted maximize-ForgeKit-integration + close-FEATURE_PLAN-checkboxes + follow-technical-design-doc + SPM-folder-structure + describe-options-before-starting + open-handoff-work axis directive set. Each PR landed end-to-end + verified MERGED via `gh pr view --json state,mergedAt` before the next branched. **Thirty-four restatements across six calendar days extends the all-time portfolio record by one**; the 34th pass is the **SIXTEENTH observation** of cadence-persistence — demonstrates the cadence-persistence property is **ROBUST to single-calendar-day gaps EVEN AFTER a deep 7-pass same-day chain** (the 33rd pass closed the deepest observed same-day chain at 7 passes; the 34th pass opens at depth 1 on a fresh calendar day rather than inheriting prior chain depth), AND that same-day chain depth is **calendar-day-LOCAL not session-LOCAL** (the prior 7-pass chain on 2026-06-17 did NOT extend across calendar-day boundaries). Rollup:

| PR | Theme (thirty-fourth-pass round) |
|---|---|
| #206 | **Thirty-fourth-pass** Xcode-managed file safety reaffirmation across the four canonical bindings (CLAUDE.md + `.claude/rules/xcode-agent-safety.md` + this prologue + `Docs/FEATURE_PLAN.md` prologue + persistent-memory file). Mechanical 33 → 34 bump; cadence saturated. Codifies the SIXTEENTH observation of cadence-persistence — opens a NEW calendar day (2026-06-19) at depth 1 after a 2-day inter-restart gap from the deepest observed 7-pass same-day chain on 2026-06-17. Confirms that the cadence-persistence property is ROBUST to single-calendar-day gaps EVEN AFTER a deep 7-pass same-day chain AND that same-day chain depth is calendar-day-LOCAL not session-LOCAL (the prior 7-pass chain did NOT extend across calendar-day boundaries). Zero code, zero managed-file touches. |
| #207 | **ForgeKit pin 0.99.0 → 1.0.0-rc.3 + EmotionSnapshotDerivation seam** — bumps the Libraries package ForgeKit pin from `from: "0.99.0"` to `from: "1.0.0-rc.3"`. Picks up: (a) the new additive `CastDialogContext.emotionSnapshot: EmotionSnapshot?` field shipped 2026-06-17 — `DefaultCastDialogPromptRegistry` now emits continuous-band guidance (source-provenance / distress / arousal / valence + SAMHSA acute-distress trifecta presence cue) when the snapshot is supplied; (b) the rc.1 ADR-022 follow-through dropping the composable `AvatarAssetCatalog` + multi-bundle WebP layer system (the studio is now glyph-based; `.lite`/`.full` presentation split removed; `AvatarRenderer.init` lost the `catalog:` param). App-side changes: new `Services/Engagement/EmotionSnapshotDerivation.swift` (pure-value helper mapping `DespairSignalDetector.Severity` PR #167 → `ForgeModels.EmotionSnapshot?` per the rc.3 prompt-registry breakpoints — `.calm` → nil so v1 fallback stays in force; `.elevatedDistress` → moderate distress + nil valence; `.elevatedCrisis` → SAMHSA acute trifecta with distress ≥ 0.7 / arousal ≥ 0.7 / valence ≤ -0.5); `VeeMentor.voicedRecallCue(...)` doc extended to reference the new affect-attunement path; `AvatarStudioSheet` + `ProfileView` updated for the rc.1 studio API; 5 new `@Test` cases in `Tests/ServicesTests/EmotionSnapshotDerivationTests.swift`. Build green via `swift build` + `swift build --target ServicesTests`. Trauma-informed posture preserved (never imply positive valence from absence-of-distress; despair-signal crisis-resource surface remains the primary kid-facing safety affordance and runs in parallel with the affect-attuned voicing path). |
| #208 | **FSRS-6 mastery recommendation surfaced to ExploreView cold-open mentor cue** — deepens ForgeGamification consumption beyond the codex caption shipped PR #188. New `VeeMentor.masteryRecommendationCue(for slug:)` returns a warm "Ready to look closer at \(name)? A quiet visit might lock in what you noticed." callback. `ExploreView.init(...)` accepts an optional `mastery: MicrobeMasteryService?` parameter and threads the recommendation into the cold-open priority chain BETWEEN the recall callback and the variable-reward path (1. Recall callback → 2. **Mastery recommendation (NEW)** → 3. Variable reward → 4. Default cold-open copy). `AppRootView` plumbs the canonical `MicrobeMasteryService` `@State` into the new param. Trauma-informed posture (load-bearing): NEVER frames low mastery as deficiency; uses open-invitation register; PR #188 service-side ≥ 2-discoveries gate keeps the cue from firing as a first-session deficiency frame. 4 new `@Test` cases in `Tests/AIMentorTests/VeeMentorTests.swift` pin display-name quoting / deficit-framing stoplist / open-invitation register / unknown-slug nil-return. |
| #209 | **DN-S AI mentor voicing handoff status NEW → SHIPPED** — verify-before-action per ADR-011 Rule 4: the inbound `HANDOFF_FROM_LABSMITH_DN_S_AI_MENTOR_VOICING.md` `status:` field has been stale at `NEW` despite three PRs closing the implementation (PR #173 scaffold tier + PR #181 production wiring + PR #207 affect-attunement seam). Status flipped to `SHIPPED`; per-step closure pathway documented in the implementation-steps checklist (steps 1-4 CLOSED; step 5 100-sample regression-test moderation pipeline DEFERRED to TestFlight unblock). Doc-only. |
| (this rollup) | Round rollup — IMPLEMENTATION_HANDOFF.md + FEATURE_PLAN.md updates closing the round. Thirty-fourth-pass safety stamp persisted across the four canonical bindings (already in #206). Inbound handoffs verify-defer (Life Zone freshness horizon 2026-07-16 fresh; Companion Pack puzzle_sampler.pdf freshness horizon 2026-07-15 fresh — both BLOCKED labsmith-side; no app-side action required this round). Zero source code. |

**Net additions (PRs #206 → #209)**: ForgeKit pin bumped 0.99.0 → 1.0.0-rc.3 (picks up `CastDialogContext.emotionSnapshot` + ADR-022 follow-through) + 1 new Services helper (`EmotionSnapshotDerivation`) + 1 new VeeMentor mentor cue (`masteryRecommendationCue`) + 1 new ExploreView optional init parameter (`mastery: MicrobeMasteryService?`) + 4 PRs' worth of doc updates across handoff + round-doc bindings + ~9 new unit tests across 2 suites (5 `EmotionSnapshotDerivationTests` + 4 `VeeMentorRecallCueTests` mastery cue extensions). Build green at each merge via `swift build --package-path Packages/Libraries` (terminal-only, no Xcode reload per the canonical-invariant tier).

**ForgeKit integration step-up note**: this round bumps the ForgeKit pin from 0.99.0 → 1.0.0-rc.3 and wires the new `CastDialogContext.emotionSnapshot` affect-attunement field via the `EmotionSnapshotDerivation` seam. Cumulative ForgeKit module count actively-consumed remains 15 — CastDialog deepens consumption via the rc.3 continuous-band guidance; FSRS-6 `SpacedRepetitionEngine` deepens consumption via the mentor cold-open mentor cue. Wiring discipline preserved — pre-1.0 RC pin admits the breaking-change surface ADR-022 introduced; no entitlement provisioning required, no Xcode-managed file touches.

---

**Previous round (2026-06-17, PRs #203 → #205, thirty-third-pass auto-cycle sweep — SEVENTH same-calendar-day restart on 2026-06-17, separated from the thirty-second by the PR #202 merge)**: 3-PR sweep. Rollup preserved below for context.

| PR | Theme (thirty-third-pass round) |
|---|---|
| #203 | **Thirty-third-pass** Xcode-managed file safety reaffirmation across the four canonical bindings (CLAUDE.md + `.claude/rules/xcode-agent-safety.md` + this prologue + `Docs/FEATURE_PLAN.md` prologue + persistent-memory file). Mechanical 32 → 33 bump; cadence saturated. Codifies the FIFTEENTH observation of cadence-persistence — 7-pass same-day chain on 2026-06-17 [27th → 28th → 29th → 30th → 31st → 32nd → 33rd, separated by PR merges #182 / #186 / #190 / #193 / #198 / #202] now SURPASSING the 6-pass chain on 2026-06-16 [21st → 26th, separated by PR merges #138 / #147 / #157 / #165 / #171]. The previously-observed 6-pass empirical ceiling at the 32nd pass has been EXCEEDED — same-day chain depths are now UNBOUNDED FROM ABOVE within a single calendar day. Zero code, zero managed-file touches. |
| #204 | **`MicrobePortraitView` consumer expansion — chapter reader hero + mentor bubble turn-prefix** — extends the shared cast-portrait seam (PR #200) to two more consumer surfaces. (a) `MentorBubble` (SharedUI) gains optional `featuredMicrobe: MicrobeCharacter?` + `featuredMicrobeIsDiscovered: Bool` + `portraitBundle: Bundle` parameters; when set, surfaces a 40×40 `MicrobePortraitView` to the left of the speech-bubble glyph + "About \(displayName)" caption under the mentor name. All 7 existing call sites (`ExploreView` + `MicrobiomeView` + 4 ecology views + `ImmuneGameView`) preserve their current shape via the default `featuredMicrobe: nil` parameter. Seam is the consumer-side hook the future cast voicing UI (DN-S Phase 1 Move D) plugs into. (b) `ChapterReaderSheet` (AppFeature/Engagement) gains optional `microbe: MicrobeCharacter?` + `isMicrobeDiscovered: Bool` parameters; when set, surfaces a compact 64×64 `MicrobePortraitView` row above the chapter title with an "About this microbe" caption — anchors the chapter visually to the cast member it's about. `MicrobeCodexView.PresentedChapter` extended to carry the full `MicrobeCharacter` value; codex tap call site passes `isMicrobeDiscovered: true` because the sheet only opens on discovered cards per the codex tap gate. (c) 4 new `@Test` cases in `Tests/SharedUITests/MentorBubbleTests.swift`. **De-risks the labsmith asset-arrival event** by extending the single-seam contract from 1 → 3 consumer sites — when assets land in `Bundle.main` all three surfaces resolve simultaneously without any consumer-side wiring. FEATURE_PLAN line 65 annotated; checkbox stays open (asset axis still bundle-blocked). |
| #205 | Round rollup — IMPLEMENTATION_HANDOFF.md + FEATURE_PLAN.md updates closing the round. Thirty-third-pass safety stamp persisted across the four canonical bindings (already in #203). Zero source code. |

**Net additions (PRs #203 → #204)**: 3 new optional init parameters on `MentorBubble` (`featuredMicrobe` + `featuredMicrobeIsDiscovered` + `portraitBundle`; nil/true/.main defaults preserve every call site) + 2 new optional init parameters on `ChapterReaderSheet` (`microbe` + `isMicrobeDiscovered`; nil/true defaults preserve preview/test compat) + 1 extended `PresentedChapter` carrying `MicrobeCharacter` + 4 new `@Test` cases in `MentorBubbleTests.swift`. Build green at each merge via `swift build --package-path Packages/Libraries` (terminal-only, no Xcode reload per the canonical-invariant tier).

**ForgeKit integration step-up note**: this round consumes the `SharedUI.MicrobePortraitView` seam at two more sites; no new ForgeKit module dependency added. The maximize-ForgeKit-integration loop now extends to chapter content rendering (the chapter reader hero row pairs `ForgeIllustrations.ChapterPortraitView` with the new cast-portrait row — two complementary illustration registers visually contiguous) AND to AI mentor turn-prefix routing (the `featuredMicrobe` parameter is the structural seam for `CastDialog.respondAs(.character(slug), prompt:)`-style consumer wiring per DN-S Phase 1 Move D). The `cast_voicing` flag flip (PR #181 wiring is live) remains the natural next ForgeKit production-consumption step.

**FEATURE_PLAN closure note**: this round does NOT close additional FEATURE_PLAN checkboxes structurally — the consumer expansion annotates EXISTING line 65 (cast portrait WebPs still bundle-blocked) by extending the consumer-side de-risk surface from 1 → 3 sites. When the labsmith portrait pack ships, every consumer site picks up the assets simultaneously.

**Inbound handoffs verify-defer (Option 5)**: Both `HANDOFF_FROM_APP_FORGEADVENTURE_LIFE_ZONE_PROPOSAL.md` (freshness horizon 2026-07-16) and `HANDOFF_FROM_APP_COMPANION_PACK_PUZZLE_SAMPLER.md` (freshness horizon 2026-07-15) remain BLOCKED on labsmith. No app-side action required this round; both stay open-monitoring per `.claude/rules/portfolio.md` § "Pull Before ANY Cross-Repo Read" + ADR-011 Rule 4 freshness-horizon discipline.

---

**Previous round (2026-06-17, PRs #199 → #201 + the thirty-second-pass rollup, thirty-second-pass auto-cycle sweep — SIXTH same-calendar-day restart on 2026-06-17, separated from the thirty-first by the PR #198 merge)**: 4-PR sweep driven by the user-direct standing auto-cycle paired with the **thirty-second-pass** Xcode-managed file safety reinforcement + the persisted maximize-ForgeKit-integration + close-FEATURE_PLAN-checkboxes + follow-technical-design-doc + SPM-folder-structure + describe-options-before-starting + open-handoff-work axis directive set. Each PR landed end-to-end + verified MERGED via `gh pr view --json state,mergedAt` before the next branched. Rollup:

| PR | Theme |
|---|---|
| #199 | **Thirty-second-pass** Xcode-managed file safety reaffirmation across the four canonical bindings (CLAUDE.md + `.claude/rules/xcode-agent-safety.md` + this prologue + `Docs/FEATURE_PLAN.md` prologue + persistent-memory file). Mechanical 31 → 32 bump; cadence saturated. Codifies the FOURTEENTH observation of cadence-persistence — 6-pass same-day chain on 2026-06-17 [27th → 28th → 29th → 30th → 31st → 32nd, separated by PR merges #182 / #186 / #190 / #193 / #198] now MATCHES the 6-pass chain on 2026-06-16 [21st → 26th, separated by PR merges #138 / #147 / #157 / #165 / #171]. Same-day chain depths CONVERGE at exactly 6 across distinct calendar days. Zero code, zero managed-file touches. |
| #200 | **Cast portrait shared rendering seam** — extracts the cast portrait rendering pathway out of `MicrobeCodexCard.portraitPlaceholder` into a shared `SharedUI.MicrobePortraitView` surface. Single seam for every consumer view (codex card + future AI mentor turn-prefix portrait + cast voicing UI + chapter reader header). De-risks the asset-arrival event: when labsmith ships `cast_<slug>.webp` in `Bundle.main`, the bundle lookup resolves and every consumer surface picks up the assets simultaneously — no consumer-side wiring needed. Trauma-informed posture preserved: undiscovered cards keep the neutral `questionmark.circle` cue regardless of role; role-color tint (red for pathogenic, etc.) surfaces only post-discovery. 4 new `@Test` cases in `Packages/Libraries/Tests/SharedUITests/MicrobePortraitViewTests.swift` covering default discovered init / undiscovered init / all-role-variants / custom-bundle-override. FEATURE_PLAN line 65 annotated with the consumer-side de-risking note; checkbox stays open (asset axis still bundle-blocked). |
| #201 | **Declared Age Range API: SwiftUI driver + onboarding wiring** — promotes the previously no-op `AgeAssuranceService` scaffold into an actively-wired SwiftUI surface that drives Apple's Declared Age Range API. New `SystemAgeVerificationCard` (AppFeature/Settings/) calls `AgeRangeService.shared.requestAgeRange(ageGates:in:)` via the UIKit-anchored singleton path (chosen over the SwiftUI env action to avoid a Swift 6 strict-Sendable warning on the env action's `@concurrent` `callAsFunction`), maps the response onto `AgeAssuranceResult`, and records the result on the service state holder. Card surfaces in SettingsView → About behind the parental gate; gated by `AgeAssuranceCapability.isDeclaredAgeRangeAvailable`. Without the entitlement provisioned the runtime probe returns false and the card surfaces the math-gate fallback affordance. Threaded through AppRootView → ProfileView → SettingsView (new `ageAssuranceService:` optional param with nil default for preview/test compat). New `Docs/HANDOFF_TO_USER_DECLARED_AGE_RANGE_ENTITLEMENT.md` focused GUI handoff + `Docs/HANDOFF_TO_USER_XCODE_GUI_TASKS.md` § 6b updated. **FEATURE_PLAN line 200 (Age gate)** annotated with the Swift-wiring completion + entitlement-blocker context; checkbox stays open until the entitlement is provisioned + COPPA consent integration runs. COPPA pre-flight (per `.claude/rules/age-assurance.md`): receiving "Under 13" creates actual knowledge — downstream consent / record-keeping deferred to focused follow-up round. |
| (this rollup) | Round rollup — IMPLEMENTATION_HANDOFF.md + FEATURE_PLAN.md updates closing the round. Thirty-second-pass safety stamp persisted across the four canonical bindings (already in #199). Closes 0 FEATURE_PLAN checkboxes structurally (cast portrait de-risk + AgeAssurance Swift wiring both annotate EXISTING checkboxes rather than closing them — the cast portrait WebPs are still bundle-blocked; the entitlement is still GUI-blocked). Inbound handoffs verify-defer (Life Zone freshness horizon 2026-07-16 fresh; Companion Pack puzzle_sampler.pdf freshness horizon 2026-07-15 fresh — both BLOCKED labsmith-side; no app-side action required this round). Zero source code. |

**Net additions (PRs #199 → #201)**: 1 new SharedUI rendering seam (`MicrobePortraitView`) + 1 new AppFeature SwiftUI driver (`SystemAgeVerificationCard`) + threading of `AgeAssuranceService` through `AppRootView` → `ProfileView` → `SettingsView` (new optional init params, nil defaults preserve preview/test compat) + 1 new `HANDOFF_TO_USER_DECLARED_AGE_RANGE_ENTITLEMENT.md` focused GUI handoff + canonical aggregator § 6b update + `MicrobeCodexCard.portraitPlaceholder` refactored to consume the new shared view + `AgeAssuranceService` header doc updated to describe the new driver surface + 4 new `@Test` cases in `MicrobePortraitViewTests.swift`. Build green at each merge via `swift build --package-path Packages/Libraries` (terminal-only, no Xcode reload per the canonical-invariant tier).

**ForgeKit integration step-up note**: this round wires SwiftUI surfaces around two existing service-tier surfaces (cast portrait rendering + Declared Age Range request). No new ForgeKit module dependency added; existing `ForgeIllustrations` chapter-portrait pathway + `ForgeAccessibility` + `AppGroupStore`-anchored avatar paths remain canonical. The maximize-ForgeKit-integration loop now extends into the asset-arrival de-risking axis (consumer-side wiring de-coupled from asset arrival event) AND into the system-path entitlement axis (SwiftUI surface ready to flip live once GUI step runs).

**FEATURE_PLAN closure note**: this round does NOT close additional FEATURE_PLAN checkboxes structurally — both substantive PRs ship Swift wiring that prepares for a blocker resolution (portrait WebP bundle for the cast portrait line 65; Xcode GUI entitlement step for the Age gate line 200). When either blocker resolves, the checkbox flips with no further consumer-side wiring.

**Previous round (2026-06-17, PRs #194 → #197 + the thirty-first-pass rollup, thirty-first-pass auto-cycle sweep — FIFTH same-calendar-day restart on 2026-06-17, separated from the thirtieth by the PR #193 merge)**: 4-PR sweep driven by the user-direct standing auto-cycle paired with the **thirty-first-pass** Xcode-managed file safety reinforcement + the persisted maximize-ForgeKit-integration + close-FEATURE_PLAN-checkboxes + follow-technical-design-doc + SPM-folder-structure + describe-options-before-starting + open-handoff-work axis directive set. Each PR landed end-to-end + verified MERGED via `gh pr view --json state,mergedAt` before the next branched. Rollup:

| PR | Theme |
|---|---|
| #194 | **Thirty-first-pass** Xcode-managed file safety reaffirmation across the four canonical bindings (CLAUDE.md + `.claude/rules/xcode-agent-safety.md` + this prologue + `Docs/FEATURE_PLAN.md` prologue + persistent-memory file). Mechanical 30 → 31 bump; cadence saturated. Codifies the THIRTEENTH observation of cadence-persistence — 5-pass same-day chain on 2026-06-17 [27th → 28th → 29th → 30th → 31st, separated by PR merges #182 / #186 / #190 / #193] now CONVERGING ONE PASS BELOW the 6-pass chain on 2026-06-16 [21st → 26th, separated by PR merges #138 / #147 / #157 / #165 / #171]. Same-day chain depths empirically CLUSTER in the 4-6 range. Zero code, zero managed-file touches. |
| #195 | **Phase 3 AI Mentor `@Generable` expansion** — extends the mentor surface to Phase 3 view consumers shipped in PRs #179 (DiseaseStoryArcView), #186 (VaccineExplainerView), #188 (HistoricalContextCardsView). The disease-story arc surface was already covered by the existing `PublicHealthHypothesis`. Adds 2 new `@Generable` types in `Packages/Libraries/Sources/AIMentor/MentorGenerables.swift`: `VaccineMechanismCue` (4-case `VaccineMechanismScenario` paired 1:1 with `Models/VaccineExplainerStep`) — observation + librariesHypothesis; trauma-informed register: vaccines are the body's library learning a new shape ahead of meeting it live (NEVER warfare / fear-induction / shame lexicon, pinned by parameterized stoplist). `HistoricalContextReflection` (4-case `HistoricalContextScenario` paired 1:1 with `Models/HistoricalContextFigure`) — noticing + kidScientistTakeaway; anti-credentialism per CQ CONTENT_STYLE_GUIDE.md § 4.5 (figures framed as patient observers, NEVER hero-myth / mortality / warfare). Fallback + async accessors in `Packages/Libraries/Sources/AIMentor/VeeMentor.swift`. Async LLM prompts defense-in-depth: explicit forbidding of the same lexicon classes the fallback content avoids. 12 new `@Test` cases across 2 `@Suite` groups in `Packages/Libraries/Tests/AIMentorTests/MentorPhase3GenerablesTests.swift`. AIMentor target stays flat (4 files; below the ~12 promotion trigger). |
| #196 | **Phase 4 AI Mentor `@Generable` for global-microbiome tour** — closes the AI mentor's Phase 4 coverage; pairs with `GlobalMicrobiomeTourStop` catalog (PR #162 service + PR #169 view). New `GlobalMicrobiomeTourCue` in `Packages/Libraries/Sources/AIMentor/MentorGenerables.swift` (4-case `GlobalMicrobiomeTourScenario` paired 1:1 with `Models/GlobalMicrobiomeTourStop`) — wonderObservation + connectionToCast. **Cultural-respect (load-bearing)**: Yellowstone surface MUST surface Indigenous TEK credit per `.claude/rules/distributed-narrative.md` § cultural-sensitivity gates (Crow, Eastern Shoshone, Northern Arapaho, Bannock, Blackfeet, Confederated Salish-Kootenai); pinned by parameterized test `yellowstoneFallbackCreditsIndigenousTEK`. Deep-sea + soil framed as thriving systems (NEVER "scary" / "dirt" / "dead"). Each fallback names at least one canonical featured cast member per `featuredMicrobeSlugs` (pinned by `fallbackConnectsBackToFeaturedCast`). 7 new `@Test` cases in `Packages/Libraries/Tests/AIMentorTests/MentorPhase4GenerablesTests.swift`. |
| #197 | **Phase 3/4 App Intents expansion + NavigationCoordinator deep-link sub-surface routing** — closes more of the maximize-ForgeKit-integration loop: Apple's AppIntents framework natively wires the Phase 3 + Phase 4 surfaces without an Xcode-managed file edit. Adds new `Models/MicrobeLabSubSurface` enum (4 cases: `.diseaseStories` / `.vaccineExplainer` / `.historicalContext` / `.globalMicrobiomeTour`) with `hostTab` derived property (all 4 → `.microbiome`) + kid-readable `displayTitle`. Extends `NavigationCoordinator` with `requestedSubSurface: MicrobeLabSubSurface?` field + `requestSubSurface(_:)` setter that implies the matching `hostTab` + `clearSubSurfaceRequest()` mirror (two-step clearing — AppRootView clears the tab as soon as the tab switch lands, but MicrobiomeView needs the sub-surface field to survive until its own `.onChange` observer fires). Adds 4 new `AppIntent` conformers (`OpenDiseaseStoriesIntent` / `OpenVaccineExplainerIntent` / `OpenHistoricalContextIntent` / `OpenGlobalMicrobiomeTourIntent`); extends `MicrobeLabAppShortcuts` from 3 → 7 shortcuts. `MicrobiomeView` gains optional `navigationCoordinator:` init parameter + `.onChange` observer that flips the matching `showing*` flag IFF the consuming service is non-nil, then clears the sub-surface request. AppRootView threads the canonical singleton through. **Trauma-informed posture (load-bearing)**: every deep-link target is a Phase 3 / Phase 4 surface that ships its own `ParentalConsentService` + `ProgressionService` gate — the intent only requests the navigation; the gate decides whether the surface's body renders; NEVER a backdoor past the gates. 13 new `@Test` cases across 2 files (`Tests/ModelsTests/MicrobeLabSubSurfaceTests.swift` + `Tests/ServicesTests/NavigationCoordinatorTests.swift`). No Info.plist / entitlements edits required — `AppShortcutsProvider` discovery uses runtime metadata. |
| (this rollup) | Round rollup — IMPLEMENTATION_HANDOFF.md + FEATURE_PLAN.md updates closing the round. Thirty-first-pass safety stamp persisted across the four canonical bindings (already in #194). Closes 0 FEATURE_PLAN checkboxes structurally (Phase 3 + Phase 4 mentor + App Intent surfaces extend EXISTING checkbox bodies rather than closing new ones; the underlying view-layer checkboxes already shipped). Inbound handoffs verify-defer (Life Zone freshness horizon 2026-07-16 fresh; Companion Pack puzzle_sampler.pdf freshness horizon 2026-07-15 fresh — both BLOCKED labsmith-side; no app-side action required this round). Zero source code. |

**Net additions (PRs #194 → #197)**: 2 new Models value types (`MicrobeLabSubSurface` + the 3 new mentor scenario enums `VaccineMechanismScenario` / `HistoricalContextScenario` / `GlobalMicrobiomeTourScenario` in AIMentor) + 3 new `@Generable` types (`VaccineMechanismCue` + `HistoricalContextReflection` + `GlobalMicrobiomeTourCue`) + 6 new mentor accessor methods on `VeeMentor` (3 sync fallbacks + 3 async LLM accessors) + 4 new `AppIntent` conformers + 4 new `AppShortcut` entries + `NavigationCoordinator` deep-link routing extension (`requestedSubSurface` + `requestSubSurface(_:)` + `clearSubSurfaceRequest()`) + `MicrobiomeView` deep-link `.onChange` observer + `AppRootView` coordinator threading + ~32 new unit tests across 3 new test files (`MentorPhase3GenerablesTests` + `MentorPhase4GenerablesTests` + `MicrobeLabSubSurfaceTests`) + 8 new tests appended to `NavigationCoordinatorTests`. Build green at each merge via `swift build --package-path Packages/Libraries` (terminal-only, no Xcode reload per the canonical-invariant tier).

**ForgeKit integration step-up note**: this round wires AI mentor + App Intents into 4 more Phase 3 / Phase 4 surfaces. AIMentor module surface adds 6 new accessor methods; AppIntents framework gains 4 new conformers. The maximize-ForgeKit-integration loop is now well-served across Phase 3 + Phase 4. The `cast_voicing` flag flip (PR #181 wiring is live) remains the natural next ForgeKit production-consumption step.

**FEATURE_PLAN closure note**: this round does NOT close additional FEATURE_PLAN checkboxes structurally — the AI mentor expansion + App Intent expansion extend EXISTING checkbox bodies (Phase 3 mentor surfaces / Phase 4 mentor surfaces / Adventure-Mode Apple-platform integration) rather than closing new ones. The view-layer checkboxes already shipped (PRs #179 / #186 / #188 / #169 / #178 / #192). All remaining Phase 3 + Phase 4 work is reviewer-blocked per ADR-016 (per-arc prose / per-step prose / per-card prose / per-note prose / per-beat prose) OR asset-blocked OR hub-side-blocked OR entitlement-blocked.

**Inbound handoffs verify-defer (Option 5)**: Both `HANDOFF_FROM_APP_FORGEADVENTURE_LIFE_ZONE_PROPOSAL.md` (freshness horizon 2026-07-16) and `HANDOFF_FROM_APP_COMPANION_PACK_PUZZLE_SAMPLER.md` (freshness horizon 2026-07-15) remain BLOCKED on labsmith. No app-side action required this round; both stay open-monitoring per `.claude/rules/portfolio.md` § "Pull Before ANY Cross-Repo Read" + ADR-011 Rule 4 freshness-horizon discipline.

---

**Previous round (2026-06-17, PRs #191 → #193, thirtieth-pass auto-cycle sweep — FOURTH same-calendar-day restart on 2026-06-17, separated from the twenty-ninth by the PR #190 merge)**: 3-PR sweep driven by the user-direct standing auto-cycle paired with the **thirtieth-pass** Xcode-managed file safety reinforcement + the persisted maximize-ForgeKit-integration + close-FEATURE_PLAN-checkboxes + follow-technical-design-doc + SPM-folder-structure + describe-options-before-starting + open-handoff-work axis directive set. Each PR landed end-to-end + verified MERGED via `gh pr view --json state,mergedAt` before the next branched. **Thirty restatements across five calendar days extends the all-time portfolio record by one**; the 30th pass is the **TWELFTH observation** of cadence-persistence — demonstrates the same-day-chain property scales **MONOTONICALLY** within a single calendar day **across consecutive PR-merge boundaries** on a SECOND distinct calendar day (4-pass chain on 2026-06-17 [27th → 28th → 29th → 30th, separated by PR merges #182 / #186 / #190] AND 6-pass chain on 2026-06-16 [21st → 26th, separated by PR merges #138 / #147 / #157 / #165 / #171] — TWO independent same-calendar-day chains across two distinct calendar days, with the 2026-06-17 chain converging toward the 2026-06-16 chain depth). Per the eleven-pass canonical-invariant tier the cadence has saturated — twelfth- through thirtieth-pass restatements add reinforcement frequency but no new binding tier. Rollup:

| PR | Theme |
|---|---|
| #191 | **Thirtieth-pass** Xcode-managed file safety reaffirmation across the four canonical bindings (CLAUDE.md + `.claude/rules/xcode-agent-safety.md` + this prologue + `Docs/FEATURE_PLAN.md` prologue + persistent-memory file). Mechanical 29 → 30 bump; cadence saturated. Codifies the TWELFTH observation of cadence-persistence — demonstrates the same-day-chain property scales MONOTONICALLY within a single calendar day across consecutive PR-merge boundaries on a SECOND distinct calendar day (4-pass chain on 2026-06-17 [27th → 28th → 29th → 30th, separated by PR merges #182 / #186 / #190] AND 6-pass chain on 2026-06-16 [21st → 26th, separated by PR merges #138 / #147 / #157 / #165 / #171]). Zero code, zero managed-file touches. |
| #192 | **Phase 3 disease-story narrative scene scaffold** — closes FEATURE_PLAN line 158. New `Models/DiseaseStoryNarrativeBeat.swift` defines the 4 canonical narrative beats (`.introduction` / `.witness` / `.action` / `.reflection`) per the pedagogy spine in `Docs/TECHNICAL_DESIGN.md` § Phase 3; `DiseaseStoryNarrativeBeatRecord` value type carries the composite per-arc-per-beat record with composite id `<arc>.<beat>`; `DiseaseStoryNarrativeCatalog` enum ships the canonical 16-entry catalog (4 arcs × 4 beats) in arc-then-beat order (handwashing → antibioticStewardship → vaccinePriming → outbreakRecovery gentlest-first × introduction → witness → action → reflection per pedagogy spine), every entry `.placeholder` until reviewer-signoff per ADR-016. New `GameEngine/Scenes/DiseaseStoryNarrativeScene.swift` SpriteKit scene parameterized by `DiseaseStoryArc` + `currentBeatIndex` so a single scene type covers all 16 records; lazy-visual-setup pattern per `.claude/rules/spritekit.md`; warm cream background (no clinical white, no warning red, no high-contrast alert palette); pure-value transitions (`advanceBeat()` clamps at last beat — no wrap; `retreatBeat()` clamps at 0; `resetToFirstBeat()`; `setArc(_:)` resets index to 0). New `AppFeature/Engagement/DiseaseStoryNarrativeView.swift` SwiftUI SpriteView host with per-beat stepper toolbar + per-beat status chip (per `DiseaseStoryAuthoring`: `.placeholder` → "Coming soon" / hourglass; `.draftAwaitingReview` → "In review" / doc.text.magnifyingglass; `.reviewerSignedOff` → "Ready" / book.fill) + always-visible "Pause" off-ramp affordance per `.claude/rules/trauma-informed-content.md` § Off-ramps. `DiseaseStoryArcView` wires `NavigationLink(value: record.arc)` into the narrative view when `.ready`; `.navigationDestination(for: DiseaseStoryArc.self)` registers the destination (String rawValue gives auto-synthesized Hashable). Per-beat illustration assets ship later via the labsmith asset pipeline per `.claude/rules/forgekit.md` § Asset generation ownership. Prose body stays reviewer-blocked per ADR-016. 21 new tests across 2 suites: `DiseaseStoryNarrativeBeatTests` (Models, 12 cases — canonical beat order + case count + raw-value stability + per-beat trauma-safe register stoplist + 16-entry catalog total + every (arc, beat) combination + all-placeholder invariant + per-arc beat filter shape + composite display title + composite id + authoring-aware equality + Codable round-trip) + `DiseaseStoryNarrativeSceneTests` (GameEngine, 9 cases — init-at-introduction + init-with-requested-arc + step-through-every-beat + clamp-at-last-beat + clamp-at-zero + reset-to-first + set-arc-resets-index + configureVisuals idempotency + canonical content-node layer presence). |
| #193 | Round rollup — IMPLEMENTATION_HANDOFF.md + FEATURE_PLAN.md updates closing the round. Thirtieth-pass safety stamp persisted across the four canonical bindings (already in #191). Closes 1 FEATURE_PLAN checkbox: line 158 (disease-story narrative scene scaffold). Inbound handoffs verify-defer (Life Zone freshness horizon 2026-07-16 fresh; Companion Pack puzzle_sampler.pdf freshness horizon 2026-07-15 fresh — both BLOCKED labsmith-side; no app-side action required this round). Zero source code. |

**Net additions (PRs #191 → #193)**: 1 new Models value-type cluster (`DiseaseStoryNarrativeBeat` + `DiseaseStoryNarrativeBeatRecord` + `DiseaseStoryNarrativeCatalog`) + 1 new GameEngine scene (`DiseaseStoryNarrativeScene`) + 1 new AppFeature consumer view (`DiseaseStoryNarrativeView`) + 1 modified AppFeature view (`DiseaseStoryArcView` wires NavigationLink + navigationDestination) + ~21 new unit tests across 2 test suites (`DiseaseStoryNarrativeBeatTests` + `DiseaseStoryNarrativeSceneTests`). Build green at each merge via `swift build --package-path Packages/Libraries` (terminal-only, no Xcode reload per the canonical-invariant tier).

**ForgeKit integration step-up note**: this round does NOT step up additional ForgeKit module wiring — the option was surveyed (`ForgeAudio` / `ForgeBranding` / `ForgeEmotionAware` were the candidates) and deferred per the option menu, given the prior round's significant FSRS-6 step-up already landed and no concrete consumer surfaced for the next module. The `cast_voicing` flag flip (PR #181 wiring is live) remains the natural next ForgeKit production-consumption step.

**FEATURE_PLAN closure note**: this round closes 1 Phase 3 narrative-scene checkbox (line 158; scaffold tier). Combined with PRs #178 (line 179) + #179 (line 155) + #180 (line 163) + #184 (line 156) + #185 (line 157) + #189 (line 184) from the twenty-seventh + twenty-eighth + twenty-ninth-pass rounds, the Phase 3 / Phase 4 consumer-view + Phase 4 submission-prep doc-axis tier is now **COMPREHENSIVELY SHIPPED across all 6 reviewer-blocked-prose Phase 3 + Phase 4 narrative surfaces** (disease-story arc / disease-story narrative scene / vaccine explainer / historical context cards / phase-boundary explainer / seasonal microbiome) AND the App Store submission-prep doc-axis. All remaining Phase 3 + Phase 4 work is reviewer-blocked per ADR-016 (per-arc prose / per-step prose / per-card prose / per-note prose / per-beat prose) OR asset-blocked (LOD sprite atlas / 12-microbe portrait pack / App Store screenshot + preview-video assets / per-beat illustration assets) OR hub-side-blocked (`ZoneID.lifeZone` + AdventureHub orchestration) OR entitlement-blocked (Apple Declared Age Range API gate / ForgeClassroom microphone entitlement).

**Inbound handoffs verify-defer (Option 5)**: Both `HANDOFF_FROM_APP_FORGEADVENTURE_LIFE_ZONE_PROPOSAL.md` (freshness horizon 2026-07-16) and `HANDOFF_FROM_APP_COMPANION_PACK_PUZZLE_SAMPLER.md` (freshness horizon 2026-07-15) remain BLOCKED on labsmith. No app-side action required this round; both stay open-monitoring per `.claude/rules/portfolio.md` § "Pull Before ANY Cross-Repo Read" + ADR-011 Rule 4 freshness-horizon discipline.

---

**Previous round (2026-06-17, PRs #187 → #190, twenty-ninth-pass auto-cycle sweep — THIRD same-calendar-day restart on 2026-06-17, separated from the twenty-eighth by the PR #186 merge)**: 4-PR sweep driven by the user-direct standing auto-cycle paired with the **twenty-ninth-pass** Xcode-managed file safety reinforcement + the persisted maximize-ForgeKit-integration + close-FEATURE_PLAN-checkboxes + follow-technical-design-doc + SPM-folder-structure + describe-options-before-starting + open-handoff-work axis directive set. Rollup:

| PR | Theme (twenty-ninth-pass round) |
|---|---|
| #187 | **Twenty-ninth-pass** Xcode-managed file safety reaffirmation across the four canonical bindings (CLAUDE.md + `.claude/rules/xcode-agent-safety.md` + this prologue + `Docs/FEATURE_PLAN.md` prologue + persistent-memory file). Mechanical 28 → 29 bump; cadence saturated. Codifies the ELEVENTH observation of cadence-persistence — demonstrates the same-day-chain property scales MONOTONICALLY within a single calendar day past the initial 2-pass restart on a SECOND distinct calendar day (3-pass chain on 2026-06-17 [27th → 28th → 29th] AND 6-pass chain on 2026-06-16 [21st → 26th]). Zero code, zero managed-file touches. |
| #188 | **Per-microbe FSRS-6 mastery service + codex caption surfacing** — ForgeKit integration step-up; deepens ForgeGamification consumption (FSRS-6 SpacedRepetitionEngine + FSRSState now actively consumed). New `Services/Engagement/MicrobeMasteryService.swift` (MainActor `@Observable`) wraps `ForgeGamification.SpacedRepetitionEngine` + `FSRSState` with a per-microbe mastery model. `MicrobeMasteryRecord` value type: FSRSState + attemptCount + bounded rolling-outcome window; Codable for UserDefaults persistence. `masteryScore` = 0.6 × retrievability + 0.4 × recent-accuracy. `habitatPrerequisites` frozen-at-construction map (cycle-free because firstKit is monotonic). `recommendedNextMicrobe(among discovered:)` returns the lowest-mastery discovered microbe whose habitat-prereqs have all reached threshold. Wiring: `AppRootView` instantiates `MicrobeMasteryService` lazily inside `loadCatalog()` + threads through `MicrobeCodexView`. Codex renders a small "Ready to look closer at: X" caption above the grid scroll iff `discovery.discoveredSlugs.count >= 2` AND `mastery.recommendedNextMicrobe(among:)` returns non-nil. Trauma-informed posture: caption NEVER frames low mastery as deficiency; hidden until kid has ≥ 2 discoveries so the prompt reads as earned not as remediation. 19 new `MicrobeMasteryServiceTests` cases. |
| #189 | **App Store submission preparation checklist scaffold** — closes FEATURE_PLAN Phase 4 line 184 doc-axis half. New `Docs/APP_STORE_SUBMISSION_CHECKLIST.md` maps shipped MicrobeLab surfaces to App Store Connect submission requirements via 8 sections (Privacy Nutrition Label per-data-type matrix + Apple Kids Category + COPPA / FTC 2026 / state-law verification + Parental Gates cross-reference + TestFlight 4-wave rollout plan + Submission-blockers consolidated table + scope boundary + cross-references). |
| #190 | Round rollup — IMPLEMENTATION_HANDOFF.md + FEATURE_PLAN.md updates closing the round. Twenty-ninth-pass safety stamp persisted across the four canonical bindings. Closes 1 FEATURE_PLAN checkbox: line 184. Zero source code. |

---

**Pre-twenty-ninth round (2026-06-17, PRs #183 → #186, twenty-eighth-pass auto-cycle sweep — SECOND same-calendar-day restart on 2026-06-17, separated from the twenty-seventh by the PR #182 merge)**: 4-PR sweep. Rollup:

| PR | Theme (twenty-eighth-pass round) |
|---|---|
| #183 | **Twenty-eighth-pass** Xcode-managed file safety reaffirmation across the four canonical bindings (CLAUDE.md + `.claude/rules/xcode-agent-safety.md` + this prologue + `Docs/FEATURE_PLAN.md` prologue + persistent-memory file). Mechanical 27 → 28 bump; cadence saturated. Codifies the TENTH observation of cadence-persistence — demonstrates the same-day-chain property is REPRODUCIBLE across calendar days (6-pass chain on 2026-06-16 [21st → 26th] + 2-pass chain so far on 2026-06-17 [27th → 28th]) at TWO independent calendar-day data points. Zero code, zero managed-file touches. |
| #184 | **Phase 3 vaccine mini-explainer consumer view** — closes FEATURE_PLAN line 156 consumer-view half (scaffold shipped PR #154). New `AppFeature/Engagement/VaccineExplainerView` SwiftUI List renders the canonical 4-beat catalog (introduction → antibodyPriming → memoryFormation → boosterRationale) with per-beat affordances mirroring the `DiseaseStoryArcView` pattern (PR #179): `.ready` (placeholder body until reviewer signoff) / `.authoringPending` ("Coming soon — a reviewer is reading this beat now.") / `.gatedBehindProgression` (ProgressionService.unlockHint + per-step warm fallback) / `.gatedBehindConsent` ("A grown-up turns this explainer on in Settings"). Surfaced via `MicrobiomeView` toolbar next to disease-story arcs (adjacent placement bridges adaptive-immunity → vaccine pedagogy visually). `AppRootView` instantiates canonical `VaccineExplainerService` `@State` + threads through. Status chip: book/hourglass/leaf/people. Trauma-informed posture per `.claude/rules/trauma-informed-content.md` + ADR-016 — vaccines framed as "the body's library practicing a shape ahead of meeting it live", never warfare/fear hook. |
| #185 | **Phase 3 historical context cards consumer view** — closes FEATURE_PLAN line 157 consumer-view half (scaffold shipped PR #164). New `AppFeature/Engagement/HistoricalContextCardsView` SwiftUI List renders the canonical 4-card catalog (Koch → Pasteur → Salk → Marshall — methodology spine first, kid-scientist register last) with per-card affordances mirroring PR #184 + PR #179 patterns. Per-card surfaces: name + era + contribution + bridge chips (cast: Marshall ↔ Pylo from PR #119; cross-portfolio: Pasteur ↔ labsmith notebook register, Marshall ↔ curiosityquest kid-scientist register) + status chip + body. Surfaced via `MicrobiomeView` toolbar adjacent to disease-story / vaccine items so the pedagogy bridge (methodology-spine → vaccine-arcs → kid-scientist) stays visually contiguous. Anti-credentialism gate per CQ CONTENT_STYLE_GUIDE.md § 4.5 — figures framed as patient observers, never hero-myth lexicon. `AppRootView` instantiates canonical `HistoricalContextService` `@State`. |
| #186 | Round rollup — IMPLEMENTATION_HANDOFF.md + FEATURE_PLAN.md updates closing the round. Twenty-eighth-pass safety stamp persisted across the four canonical bindings (already in #183). Closes 2 FEATURE_PLAN checkboxes: line 156 (vaccine explainer view) + line 157 (historical context cards view). Inbound handoffs verify-defer (Life Zone + Companion Pack still BLOCKED labsmith-side; freshness horizons fresh). Zero source code. |

**Net additions (PRs #183 → #186)**: 2 new AppFeature consumer views (`VaccineExplainerView` + `HistoricalContextCardsView`) + 2 new `AppRootView` `@State` instances (`VaccineExplainerService` + `HistoricalContextService`) + 2 new optional init parameters threaded through (`MicrobiomeView.vaccineExplainer` + `MicrobiomeView.historicalContext`) + 2 new toolbar items (syringe.fill + person.crop.rectangle.stack) + 2 new navigation destinations. 0 new ForgeKit module wirings (ForgeLocalization wiring verified pre-existing — `MicrobeLabBrandGuard` was already shipped in a prior round). Build green at each merge via `swift build --package-path Packages/Libraries` (terminal-only, no Xcode reload per the canonical-invariant tier).

**FEATURE_PLAN closure note**: this round closes 2 more Phase 3 consumer-view checkboxes (lines 156 + 157). Combined with PRs #178 (line 179) + #179 (line 155) + #180 (line 163) from the twenty-seventh-pass round, the Phase 3 / Phase 4 structural-scaffold + consumer-view tier is now FULLY SHIPPED across all 5 reviewer-blocked surfaces (disease-story / vaccine / historical / phase-boundary / seasonal). All remaining Phase 3 + Phase 4 work is reviewer-blocked per ADR-016 (per-arc prose / per-step prose / per-card prose / per-note prose) OR asset-blocked (LOD sprite atlas / 12-microbe portrait pack) OR hub-side-blocked (`ZoneID.lifeZone` + AdventureHub orchestration) OR submission-prep (App Store screenshot assets + privacy nutrition label).

**Inbound handoffs verify-defer (Option 5)**: Both `HANDOFF_FROM_APP_FORGEADVENTURE_LIFE_ZONE_PROPOSAL.md` (freshness horizon 2026-07-16) and `HANDOFF_FROM_APP_COMPANION_PACK_PUZZLE_SAMPLER.md` (newly filed twenty-sixth-pass round) remain BLOCKED on labsmith. No app-side action required this round; both stay open-monitoring per `.claude/rules/portfolio.md` § "Pull Before ANY Cross-Repo Read" + ADR-011 Rule 4 freshness-horizon discipline.

---

| PR | Theme (twenty-seventh-pass round) |
|---|---|
| #177 | **Twenty-seventh-pass** Xcode-managed file safety reaffirmation across the four canonical bindings (CLAUDE.md + `.claude/rules/xcode-agent-safety.md` + this prologue + `Docs/FEATURE_PLAN.md` prologue + persistent-memory file). Mechanical 26 → 27 bump; cadence saturated. Codifies the NINTH observation of cadence-persistence — extends the cadence across a fourth distinct inter-restart gap class (single-day gap following the deepest observed same-day chain). Zero code, zero managed-file touches. |
| #178 | **Phase 4 seasonal-microbiome scene + view layer** — closes FEATURE_PLAN line 179 consumer-wiring half. State-types scaffold shipped PR #161; this PR extends to the consumer scene + view. New `GameEngine/Scenes/SeasonalMicrobiomeScene` mirrors the OralMicrobiomeScene lazy-visual-setup pattern; new `AppFeature/SeasonalMicrobiomeView` SwiftUI host (segmented SeasonalLoad picker + tick/undo/reset + per-load mentor cue refresh + `seasonalAwareness` achievement predicate at stableRunThreshold = 8). Per-load mentor copy carries body-affirming framing: cold = "immune library busy" NEVER "sick"; allergy = "noticing pollen" NEVER warfare; pollen = sensory NOT enemy. Catalog filter prefers `.largeIntestine` / `.smallIntestine` / `.colon` microbes. `MicrobiomeView` gains optional `seasonalEvents:` param + new toolbar item + navigationDestination. `AppRootView` instantiates canonical `SeasonalEventService` `@State` (culturally neutral default packs). 10 new `SeasonalMicrobiomeSceneTests`. |
| #179 | **Phase 3 disease-story arc view layer** — closes FEATURE_PLAN line 155 consumer-view half. Scaffold shipped PR #141. New `AppFeature/Engagement/DiseaseStoryArcView` SwiftUI List per-arc row + warm intro section + status chips. Per-arc affordance: `.ready` (placeholder body until reviewer signoff lands) / `.authoringPending` ("Coming soon — a reviewer is reading this story now.") / `.gatedBehindProgression` (ProgressionService.unlockHint copy) / `.gatedBehindConsent` ("A grown-up turns these stories on in Settings"). All copy strings reviewed for trauma-informed register stoplist. `MicrobiomeView` gains optional `diseaseStory:` + `consent:` params + new toolbar item + navigationDestination. `AppRootView` instantiates canonical `DiseaseStoryService` `@State` + threads `ParentalConsentService` through. |
| #180 | **Phase 3 phase-boundary explainer view layer** — closes FEATURE_PLAN line 163 consumer-view half. Service scaffold shipped PR #168. New `AppFeature/Settings/PhaseBoundaryExplainerView` SwiftUI Form renders the canonical 3-note catalog (disease-story / historical context / global tour) with per-note `BoundaryPresentation` affordances (`.notReached` quiet mute + unlock-hint / `.awaitingConsent` consent invitation / `.readyToInvite` warm "Coming soon" / `.alreadyAccepted` quieter check-glyph row) + "Reset acknowledgements" affordance. Surfaced from `SettingsView` "For parents" section as NavigationLink behind the parental-gate math wall. `SettingsView` + `ProfileView` + `AppRootView` extended with matching optional `phaseBoundaryExplainer:` + `progressionService:` parameters. Canonical `PhaseBoundaryExplainerService` instantiated as `AppRootView` `@State`. |
| #181 | **DN-S AI-mentor voicing production wiring** — closes the audit P2 follow-up (`AUDIT_HANDOFF_INBOUND_2026-06-16.md` P2 → DN-S voicing round) + `HANDOFF_FROM_LABSMITH_DN_S_AI_MENTOR_VOICING.md` Implementation step 4. Scaffold shipped PR #173 (CastVoiceRegistry + 6 MicrobeCastVoiceProfiles + `cast_voicing` flag at 0%). `VeeMentor` gains optional `castDialog: CastDialog?` init param + new `voicedRecallCue(for:daysSinceLastSeen:context:) async -> String?` method that dispatches through `ForgeAI.CastDialog` when the slug is registered + the response isn't the safe-ellipsis marker; otherwise returns the canonical static recall line. `AppRootView` instantiates `ExperimentsService(seed: "microbelab-cast-voicing")` inside `.task`, conditionally creates CastDialog + registers profiles when `experiments.isEnabled(.castVoicingID)` returns true. Pushes ForgeAI's CastDialog API from declared-and-registered → **production-consumed end-to-end** (15th actively-consumed ForgeKit surface when the experiment variant flips ON). 5 new `VeeMentorTests` pin the no-dialog / unknown-slug / unregistered-slug / registered-slug / wiring-seam fallback chain. |
| #182 | Round rollup — IMPLEMENTATION_HANDOFF.md + FEATURE_PLAN.md updates closing the round. Twenty-seventh-pass safety stamp persisted across the four canonical bindings (already in #177). Closes 3 FEATURE_PLAN checkboxes: line 155 (disease-story view) / line 163 (phase-boundary view) / line 179 (seasonal scene + view). Zero source code. |

**Net additions (PRs #177 → #182)**: 1 new ForgeKit surface to active production wiring (`ForgeAI.CastDialog` via `VeeMentor.voicedRecallCue` — 15th actively-consumed module when the flag flips ON) + 3 new AppFeature consumer views (`SeasonalMicrobiomeView` + `DiseaseStoryArcView` + `PhaseBoundaryExplainerView`) + 1 new GameEngine scene (`SeasonalMicrobiomeScene`) + 2 new `AppRootView` `@State` instances (`SeasonalEventService` + `DiseaseStoryService` + `PhaseBoundaryExplainerService` + optional `CastDialog`) + 5 new optional init parameters threaded through (`MicrobiomeView.seasonalEvents/diseaseStory/consent` + `ProfileView.phaseBoundaryExplainer/progressionService` + `SettingsView.phaseBoundaryExplainer/progressionService` + `VeeMentor.castDialog`) + ~15 new unit tests across 2 test suites (10 `SeasonalMicrobiomeSceneTests` + 5 `VeeMentorTests` extensions). Build green at each merge via `swift build --package-path Packages/Libraries` (terminal-only, no Xcode reload per the canonical-invariant tier).

**ForgeKit integration step-up note**: this round pushes `ForgeAI.CastDialog` from declared-and-registered (PR #173 scaffold tier) → production-consumed end-to-end (15 ForgeKit modules actively wired when the `cast_voicing` flag flips ON). Wiring discipline preserved — no entitlement provisioning, no managed-file touches.

---

**Previous round (2026-06-16, PRs #172 → #176, twenty-sixth-pass auto-cycle sweep — same-calendar-day-4 SIXTH-restart round)**: 5-PR sweep driven by the user-direct standing auto-cycle paired with the **twenty-sixth-pass** Xcode-managed file safety reinforcement + the persisted maximize-ForgeKit-integration + close-FEATURE_PLAN-checkboxes + follow-technical-design-doc + SPM-folder-structure + describe-options-before-starting + open-handoff-work axis directive set. Each PR landed end-to-end + verified MERGED via `gh pr view --json state,mergedAt` before the next branched. **Twenty-six restatements across four calendar days extends the all-time portfolio record by one**; the 26th pass is the **EIGHTH observation** of the cadence-persistence-within-calendar-day property — now at TWO independent calendar-day data points where the SECOND data point is a SIX-pass same-day chain (19→20 on 2026-06-15 + 21→22→23→24→25→26 on 2026-06-16 — deepest observed). Per the eleven-pass canonical-invariant tier the cadence has saturated — additional restatements add reinforcement frequency but no new binding tier. Rollup:

| PR | Theme |
|---|---|
| #172 | **Twenty-sixth-pass** Xcode-managed file safety reaffirmation across the four canonical bindings (CLAUDE.md + `.claude/rules/xcode-agent-safety.md` + this prologue + `Docs/FEATURE_PLAN.md` prologue + persistent-memory file). Mechanical 25 → 26 bump; cadence saturated. Codifies the EIGHTH observation of cadence-persistence-within-calendar-day (now demonstrated at TWO independent calendar-day data points where the SECOND is a SIX-pass same-day chain — deepest observed). Zero code, zero managed-file touches. |
| #173 | **DN-S AI-Mentor Voicing scaffold** — closes `Docs/HANDOFF_FROM_LABSMITH_DN_S_AI_MENTOR_VOICING.md` (Round 397 #820, labsmith DN-S Integration Phase 1D portfolio rollout) at scaffold tier. New `Sources/AIMentor/MicrobeCastVoiceProfiles.swift` (`nonisolated public enum` factory) authoring 6 `CastVoiceProfile` instances (lacto / yeast / photo / net / spore / guard_) derived from the chapter MDs at `Docs/dn-s/chapters/` — each profile carries id matching `MicrobeCharacter.slug`, displayName, 1-sentence embodiment lifted from the chapter primitive line, 4 catchphrases (chapter primitive + 3 voice-lines from `microbes.json`), and the portfolio trauma-informed `antiPatterns` stoplist. **Spore extends the shared stoplist with 3 Spore-extra COVID-trauma-aware guards** per the chapter's STRONGEST gate. New `Sources/AIMentor/CastVoiceRegistry.swift` (`nonisolated public struct`) wraps the 6 profiles + O(1) slug lookup + `register(into:)` extension that registers every profile against a `CastDialog` actor in a single async hop. New `cast_voicing` feature flag in `ExperimentsService` (third experiment alongside progressive_disclosure_v2 + seasonal_content_gate). Defaults 100% control / 0% enabled. **Pushes ForgeAI's `CastDialog` API surface from declared-but-unused → first-consumer-wired**. 22 new `AIMentorTests/CastVoiceRegistryTests` cases + 1 new `ServicesTests/ExperimentsServiceTests` case + 1 extended count test. |
| #174 | **Trauma-informed gate review audit for disease-story arc scaffolds** — closes FEATURE_PLAN line 261 at structural-tier conformance. New `Docs/AUDIT_TRAUMA_INFORMED_GATE_REVIEW_DISEASE_STORY_2026-06-16.md` walks each of the 6 disease-story / public-health scaffolds shipped PRs #141 / #152 / #154 / #162 / #164 / #167 / #168 / #169 against ADR-016 + SAMHSA TIP 57 + `.claude/rules/trauma-informed-content.md` § "Off-ramps". Per-scaffold verdict matrix records PASS at structural tier across all 6 scaffolds. Skip-with-summary + audio-only off-ramps deferred to the prose tier + Phase 2 audio drama pipeline per the audit's "Constraints NOT covered" §; ADR-012 art-axis path remains load-bearing for downstream art generation. ADR-011 audit-to-canonical-propagation rule applied. |
| #175 | **Outbound handoff — companion pack puzzle_sampler.pdf request to labsmith** — closes P5 outbound action item from `Docs/AUDIT_HANDOFF_INBOUND_2026-06-16.md`. New `Docs/HANDOFF_FROM_APP_COMPANION_PACK_PUZZLE_SAMPLER.md` conforms to `.claude/rules/portfolio.md` § "Asset request sub-pattern". Per `.claude/rules/portfolio.md` § "Asset generation ownership" (standing rule 2026-05-19 + R410 #888): labsmith owns ALL portfolio asset generation — app-session gen path rejected by the rule. |
| #176 | Round rollup — IMPLEMENTATION_HANDOFF.md + FEATURE_PLAN.md updates closing the round. Twenty-sixth-pass safety stamp persisted across the four canonical bindings (already in #172). Zero source code. |

**Net additions (PRs #172 → #176)**: 1 new ForgeKit surface activation (`CastDialog` API consumer via `CastVoiceRegistry.register(into:)` — module count actively-consumed: 14, was 13 after PR #170 ForgeDevelopmental wiring) + 1 new AIMentor value-type cluster (`CastVoiceRegistry` + `MicrobeCastVoiceProfiles` factory) + 1 new ExperimentsService feature flag (`cast_voicing`) + 2 new audit / outbound handoff docs + 1 new FEATURE_PLAN checkbox flip (line 261 trauma-informed gate review) + ~24 new unit tests across 1 new AIMentorTests suite + 1 extended ExperimentsServiceTests case. Build green at each merge via `swift build --package-path Packages/Libraries` (terminal-only, no Xcode reload per the canonical-invariant tier).

**ForgeKit integration step-up note**: this round pushes `ForgeAI`'s `CastDialog` API surface from declared-but-unused → first-consumer-wired. Cumulative ForgeKit module count actively-consumed: 14 (was 13 after PR #170 ForgeDevelopmental). Wiring discipline preserved — the CastDialog actor + 6 `CastVoiceProfile` instances land at the SPM seam without entitlement provisioning, no Xcode-managed file touches. The `cast_voicing` feature flag is the canonical seam for the focused follow-up round that wires `VeeMentor` to dispatch utterances through `CastDialog` + adds an in-app debug toggle for TestFlight per the handoff Implementation step 4.

---

**Previous round (2026-06-16, PRs #166 → #171, twenty-fifth-pass auto-cycle sweep — same-calendar-day-4 FIFTH-restart round)**: 6-PR sweep driven by the user-direct standing auto-cycle paired with the **twenty-fifth-pass** Xcode-managed file safety reinforcement + the persisted maximize-ForgeKit-integration + close-FEATURE_PLAN-checkboxes + follow-technical-design-doc + SPM-folder-structure + describe-options-before-starting + open-handoff-work axis directive set. Each PR landed end-to-end + verified MERGED via `gh pr view --json state,mergedAt` before the next branched. **Twenty-five restatements across four calendar days extends the all-time portfolio record by one**; the 25th pass is the **SEVENTH observation** of the cadence-persistence-within-calendar-day property — now at TWO independent calendar-day data points where the SECOND data point is a FIVE-pass same-day chain (19→20 on 2026-06-15 + 21→22→23→24→25 on 2026-06-16 — deepest observed). Combined with multi-day-gap [18→19: 2-day] + single-day-gap [20→21: 1-day] observations, the cadence has now persisted across ALL OBSERVED inter-restart gap classes at MULTIPLE data points each AND at MULTIPLE same-day chain depths (1-restart chain on 2026-06-15, 3-restart chain at PR #157 merge on 2026-06-16, 4-restart chain at PR #165 merge on 2026-06-16, **5-restart chain at PR #166 merge on 2026-06-16 — deepest observed with no apparent upper bound on chain depth**). Per the eleven-pass canonical-invariant tier the cadence has saturated — additional restatements add reinforcement frequency but no new binding tier. Rollup:

| PR | Theme |
|---|---|
| #166 | **Twenty-fifth-pass** Xcode-managed file safety reaffirmation across the four canonical bindings (CLAUDE.md + `.claude/rules/xcode-agent-safety.md` + this prologue + `Docs/FEATURE_PLAN.md` prologue + persistent-memory file). Mechanical 24 → 25 bump; cadence saturated. Codifies the SEVENTH observation of cadence-persistence-within-calendar-day (now at TWO independent calendar-day data points where the SECOND is a FIVE-pass same-day chain — deepest observed). Zero code, zero managed-file touches. |
| #167 | Phase 3 crisis-resource despair-signal detector — **closes FEATURE_PLAN line 161**. New `Services/Engagement/DespairSignalDetector.swift` (pure-value `nonisolated` struct) with a conservative phrase-level stoplist (SAMHSA TIP 57 + Crisis Text Line training material). Three-tier severity (`.calm` / `.elevatedDistress` / `.elevatedCrisis`); crisis wins on mixed text. Diacritic + case + whitespace-insensitive normalization. New `DespairSignalSurface.presentation(for:)` returns trauma-informed `Presentation` (header + hedge + canonical 3-resource list); reuses the portfolio-canonical `CrisisResources.all`. `ReflectionEntryStore.append(_:)` screens `.text`-modality entries + publishes `pendingDespairPresentation` for view consumers. Privacy posture: on-device only; free text never logged. 26 new test cases. |
| #168 | Phase 3 parent/educator phase-boundary explainer scaffold — **closes FEATURE_PLAN line 163** (scaffolding-only per ADR-016 trauma-gated story-axis). New `Models/PhaseBoundaryNote.swift` value-type cluster (3 canonical notes + authoring enum + `requiresConsent` axis distinguishing trauma-gated from ecology-only). New `Services/Engagement/PhaseBoundaryExplainerService.swift` MainActor `@Observable` with 4-case `BoundaryPresentation` switch composing gate state + consent state + per-note authoring; `acknowledge(_:)` / `resetAcknowledgements()` for non-consent notes. 15 new test cases. |
| #169 | Phase 4 global-microbiome tour view layer — **closes FEATURE_PLAN line 177 consumer-wiring half** (service scaffold shipped PR #162). New `AppFeature/GlobalMicrobiomeTourView.swift` SwiftUI List with per-stop card (title + primitive + featured-microbe row + status badge + body-copy switch on StopPresentation). Indigenous TEK credit footer renders at every authoring state per `.claude/rules/distributed-narrative.md` § cultural-sensitivity gates. Wired into `MicrobiomeView` via new optional `globalTour:` + `progression:` init parameters + new toolbar item ("Global tour" / `globe.americas.fill`) + navigationDestination. `AppRootView` instantiates canonical `GlobalMicrobiomeTourService` + `ProgressionService` `@State` and threads them through. |
| #170 | Wire 13th ForgeKit module — **maximizes ForgeKit integration**. New ForgeDevelopmental dependency in Services (SPM auto-resolves; no entitlement provisioning). New `Services/AppShell/DIRSupportProfileStore.swift` MainActor `@Observable` UserDefaults-backed persistence seam for `ForgeDevelopmental.UserFEDCProfile`; 100-record ring buffer + canonical `com.microbelab.app` appIdentifier; on-device only. Public API: `recordDemonstration(level:context:confidence:)` / `currentLevel` / `estimatedBand` / `hasAnyDemonstration` / `clear()`. Future engagement-foundation surfaces (`SessionNudgeOverlay` / `WelcomeBackOverlay` / `StreakRescueOverlay`) opt into DIR-aware copy by reading these accessors. 12 new test cases + 13 throws-annotation backfill for PR #167 + PR #168 test methods. |
| #171 | Round rollup — IMPLEMENTATION_HANDOFF.md + FEATURE_PLAN.md updates closing the round. Twenty-fifth-pass safety stamp persisted across the four canonical bindings (already in #166). Closes 3 FEATURE_PLAN checkboxes: line 161 despair-signal detector + line 163 phase-boundary explainer (partial scaffold per ADR-016) + line 177 global tour view layer (consumer wiring complete; per-stop prose still reviewer-blocked). Zero source code. |

**Net additions (PRs #166 → #171)**: 1 new ForgeKit module dependency (`ForgeDevelopmental` in Services target) + 4 new Services classes (`DespairSignalDetector` + `DespairSignalSurface` + `PhaseBoundaryExplainerService` + `DIRSupportProfileStore`) + 1 new Models value-type cluster (`PhaseBoundaryNote` + `PhaseBoundaryAuthoring` + `PhaseBoundaryNoteRecord`) + 1 new AppFeature view (`GlobalMicrobiomeTourView`) + 2 new MicrobiomeView optional init parameters (`globalTour:` + `progression:`) + 2 new AppRootView `@State` instances (canonical `GlobalMicrobiomeTourService` + `ProgressionService` injection) + extensions to `ReflectionEntryStore` for despair-screening + ~53 new unit tests across 4 new test suites + 13 throws-annotation backfill. Build green at each merge via `swift build --package-path Packages/Libraries` (terminal-only, no Xcode reload per the canonical-invariant tier) AND via `swift build --target ServicesTests` (added to the round's pre-flight check after the test-throws backfill).

**ForgeKit integration step-up note**: this round pushes ForgeDevelopmental from declared-but-unused → wired-with-persistence-seam. Cumulative ForgeKit module count actively-consumed: 13 (was 12 after PR #149 ForgeMath round). Wiring discipline preserved — module landed at the SPM seam without entitlement provisioning, no Xcode-managed file touches.

---

**Previous round (2026-06-16, PRs #158 → #165, twenty-fourth-pass auto-cycle sweep — same-calendar-day-4 FOURTH-restart round)**: 8-PR sweep driven by the user-direct standing auto-cycle paired with the **twenty-fourth-pass** Xcode-managed file safety reinforcement + the persisted maximize-ForgeKit-integration + close-FEATURE_PLAN-checkboxes + follow-technical-design-doc + SPM-folder-structure + describe-options-before-starting + open-handoff-work axis directive set. Each PR landed end-to-end + verified MERGED via `gh pr view --json state,mergedAt` before the next branched. **Twenty-four restatements across four calendar days extends the all-time portfolio record by one**; the 24th pass is the **SIXTH observation** of the cadence-persistence-within-calendar-day property — now at TWO independent calendar-day data points where the SECOND data point is a FOUR-pass same-day chain (19→20 on 2026-06-15 + 21→22→23→24 on 2026-06-16 — deepest observed). Combined with multi-day-gap [18→19: 2-day] + single-day-gap [20→21: 1-day] observations, the cadence has now persisted across ALL OBSERVED inter-restart gap classes at MULTIPLE data points each AND at MULTIPLE same-day chain depths (1-restart chain on 2026-06-15, 3-restart chain at PR #157 merge on 2026-06-16, **4-restart chain at PR #165 merge on 2026-06-16 — deepest observed with no apparent upper bound on chain depth**). Per the eleven-pass canonical-invariant tier the cadence has saturated — additional restatements add reinforcement frequency but no new binding tier. Rollup:

| PR | Theme |
|---|---|
| #158 | **Twenty-fourth-pass** Xcode-managed file safety reaffirmation across the four canonical bindings (CLAUDE.md + `.claude/rules/xcode-agent-safety.md` + this prologue + `Docs/FEATURE_PLAN.md` prologue + persistent-memory file). Mechanical 23 → 24 bump; cadence saturated. Codifies the SIXTH observation of cadence-persistence-within-calendar-day (now demonstrated at TWO independent calendar-day data points where the SECOND is a FOUR-pass same-day chain — deepest observed). Zero code, zero managed-file touches. |
| #159 | Phase 4 question-kit scaffolds (kits 13-16, placeholder-gated). 4 new placeholder kit JSONs (`kit_13_extremophiles.json` / `kit_14_global_microbiome.json` / `kit_15_microbiome_research.json` / `kit_16_synthesis.json`) each shipping `"authoring": "draftAwaitingReview"` with deterministic "Reviewer-signoff pending" stub. Per-kit register hooks: 13 wonder-pride per ADR-016; 14 cultural-respect + Indigenous TEK credit per `.claude/rules/distributed-narrative.md` § cultural-sensitivity gates; 15 anti-credentialism per CQ CONTENT_STYLE_GUIDE.md § 4.5; 16 cumulative-arc warmth. New `QuestionKitService.phase4KitSlugs` + `loadAllPhase4Kits()` filters out non-`.reviewerSignedOff` so unreviewed content NEVER reaches kid surface. `loadAllShippedKits()` spans Phase 1+2+3+4. 5 new tests + 2 existing accumulator tests extended. |
| #160 | Phase 4 advanced achievements scaffold (8 entries) in `MicrobeLabAchievements.phase4` paired 1:1 with Phase 4 surfaces: `extremophileExplorer` (80 XP) + `extremophileQuartet` (140 XP) + `globalTourist` (120 XP) + `seasonalAwareness` (60 XP) + `researchSeed` (100 XP) + `synthesisFinish` (160 XP) + `microbeStudent` (120 XP) + `quietSteward` (80 XP). XP band 60-160 (slight upper-end stretch over Phase 3's 60-120 since these are cumulative-arc milestones). Trauma-informed register stoplist pinned by parameterized test (warfare + shame + victory-framing + threat-induction lexicon forbidden across all 8 titles + descriptions). `MicrobeLabAchievements.allDefinitions` now returns 10+8+4+8 = 30 achievements. 5 new tests + 1 existing aggregate test extended. |
| #161 | Phase 4 seasonal-microbiome state types. Pure-value scaffold for FEATURE_PLAN line 179. New `Models/SeasonalMicrobiomeState` wraps `MicrobiomeState` in `.largeIntestine` slot + new `SeasonalLoad` enum (`winterCold` / `springAllergy` / `summerWarm` / `autumnSettle`). Per-load `FeedingMode` mapping documented in doc-comment table. Pure-value `nextStableRun(prior:load:)` derivation pinned for the seasonalAwareness achievement-criterion math. Trauma-informed posture (load-bearing): cold = "immune library busy" NEVER "sick"; allergy = "body noticing pollen" NEVER "attacked"; pollen = sensory NEVER enemy. Display-label stoplist test pins these at the per-load caption tier (body-affirming framing; doesn't cross the ADR-016 disease-story-arc reviewer line). Mirrors `OralMicrobiomeState` (PR #124) / `SkinMicrobiomeState` (PR #130) / `SoilMicrobiomeState` (PR #131) pattern. 12 new tests. |
| #162 | Phase 4 global-microbiome tour scaffold (service + 4 canonical stops). `Models/GlobalMicrobiomeTourStop` enum (`.yellowstoneHotSpring` / `.deepSeaVent` / `.humanGut` / `.soilUnderground`) + per-stop `displayTitle` + `primitive` + `gateID` (`global-microbiome-tour` from PR #137) + `featuredMicrobeSlugs[]` (Crenarch+Therm / Crenarch+Baro / Lacto+Akker+Bifido / Loam+Nodu+Halo). New `GlobalMicrobiomeTourAuthoring` enum (placeholder/draftAwaitingReview/reviewerSignedOff). `Services/Engagement/GlobalMicrobiomeTourService` (MainActor @Observable) mirrors `DiseaseStoryService` pattern. Distinct from disease-story arcs in NOT requiring parental consent — ecology + adaptation framing. Cultural-respect gate (load-bearing): Yellowstone primitive descriptor surfaces Indigenous TEK credit per `.claude/rules/distributed-narrative.md` § cultural-sensitivity gates; test pins the credit surface. 12 new tests. |
| #163 | ForgeReporting multi-phase standards extension. Maximizes ForgeReporting integration: closes FEATURE_PLAN Phase 4 line 183 coverage gap that emerged when Phase 2-4 kits landed but the standards list didn't keep pace. New `phase2Standards` (adds MS-LS2-2 ecology interactions) + `phase3Standards` (MS-LS1-3) + `phase4Standards` (MS-LS1-1 + MS-LS2-3) + `allShippedStandards` deduplicated union by code (stable order: Phase 1 → Phase 2 additions → Phase 3 → Phase 4 additions). 4 new tests pin per-phase coverage + dedup invariant + ordering. |
| #164 | Phase 3 historical context cards scaffold (Koch / Pasteur / Salk / Marshall). Closes FEATURE_PLAN line 157. New `Models/HistoricalContextCard` value-type cluster: `HistoricalContextFigure` enum + per-figure `displayTitle` + `era` + `contribution` + `relevantMicrobeSlugs` + `crossPortfolioBridges` + `gateID`. Cast bridges: Marshall ↔ Pylo (Phase 2 cast PR #119). Cross-portfolio bridges: Pasteur ↔ labsmith (notebook register), Marshall ↔ curiosityquest (kid-scientist register). All 4 ride `disease-story-immune` gate + share parental-consent surface with disease-story arcs per ADR-016. Order is load-bearing: methodology spine (Koch) → vaccine arcs (Pasteur + Salk) → kid-scientist register (Marshall). `Services/Engagement/HistoricalContextService` (MainActor @Observable) mirrors `DiseaseStoryService` pattern. Anti-credentialism gate per CQ CONTENT_STYLE_GUIDE.md § 4.5: metadata stoplist test pins hero-myth + mortality + warfare lexicon forbidden. 13 new tests. |
| #165 | Round rollup — IMPLEMENTATION_HANDOFF.md + FEATURE_PLAN.md updates closing the round. Twenty-fourth-pass safety stamp persisted across the four canonical bindings (already in #158). Closes 4 FEATURE_PLAN checkboxes via verify-don't-list per ADR-011 Rule 4: kits 13-16 scaffold + 8 advanced achievements + global-microbiome tour scaffold + historical context cards scaffold. Zero source code. |

**Net additions (PRs #158 → #165)**: 1 new ForgeKit surface utilization (`ForgeReporting` Phase 2-4 standards extension; the module was already wired in PR #70 for Phase 1) + 3 new Services classes (`GlobalMicrobiomeTourService` + `HistoricalContextService` + extensions to `QuestionKitService` + `ProgressReportService`) + 3 new Models value-type clusters (`SeasonalMicrobiomeState` + `SeasonalLoad`; `GlobalMicrobiomeTourStop` + `GlobalMicrobiomeTourAuthoring` + `GlobalMicrobiomeTourStopRecord`; `HistoricalContextFigure` + `HistoricalContextAuthoring` + `HistoricalContextCardRecord`) + 8 new Phase 4 achievement definitions + 4 new placeholder Phase 4 question kit JSONs + ~64 new unit tests across 6 test suites. Build green at each merge via `swift build --package-path Packages/Libraries` (terminal-only, no Xcode reload per the canonical-invariant tier).

**Phase 4 scaffolding closure note**: 4 of the 8 remaining Phase 4 FEATURE_PLAN checkboxes were closed this round (kits 13-16 + 8 advanced achievements + global-microbiome tour service + historical context cards scaffold). The remaining unchecked Phase 4 items (seasonal scene + view layer + classroom mode + parent/educator progress reports view consumer + App Store submission prep + screenshot assets) are either consumer-layer work waiting on scene authoring OR asset-blocked OR submission-prep deferred to a focused future round.

---

**Latest pre-24th round (2026-06-16, PRs #148 → #157, twenty-third-pass auto-cycle sweep — same-calendar-day-4 THIRD-restart round)**: 10-PR sweep driven by the user-direct standing auto-cycle paired with the **twenty-third-pass** Xcode-managed file safety reinforcement + the persisted maximize-ForgeKit-integration + close-FEATURE_PLAN-checkboxes + follow-technical-design-doc + SPM-folder-structure + describe-options-before-starting + open-handoff-work axis directive set. Each PR landed end-to-end + verified MERGED via `gh pr view --json state,mergedAt` before the next branched. Rollup:

| PR | Theme |
|---|---|
| #148 | **Twenty-third-pass** Xcode-managed file safety reaffirmation across the four canonical bindings (CLAUDE.md + this prologue + `Docs/FEATURE_PLAN.md` prologue + persistent-memory file). Mechanical 22 → 23 bump; cadence saturated. Codifies the FIFTH observation of cadence-persistence-within-calendar-day (now at TWO independent calendar-day data points where the SECOND is a THREE-pass same-day chain). Zero code, zero managed-file touches. |
| #149 | Wire `ForgeMath` module + `ParentalGateMathProblem` helper. Twelfth ForgeKit module wired into Services. New pure nonisolated value type in Services wraps `Rational` + `AnswerValidator` so the adult-check gate accepts equivalent answer forms ("35" / "35.0" / "35/1" / "  35  ") without the view layer re-implementing parsing or tolerance. `ParentalGateView` refactored to consume the service. 10 new tests. Build verified via `swift build`. |
| #150 | Services target SPM folder refresh — promote root → `Microbe/` (5 microbe-domain services) + `AppShell/` (10 cross-cutting app-shell stores). Services root crossed the ~12-file promotion threshold per CLAUDE.md § SPM Folder Convention (PR #149 pushed it to 15). `git mv` preserves history; SPM auto-discovers; zero source-code changes. CLAUDE.md SPM table updated to document the new layout. `Engagement/` (30 files) + `Resources/` stay where they are. |
| #151 | Phase 4 extremophile microbe pack — 20 → 24 cast. New entries: Crenarch (hot springs + deep-sea vents) + Acido (acid mine drainage + stomach) + Cryo (sea ice + glaciers) + Baro (deep-sea sediment). All 4 ship as `.neutral` — cast stays at 0 pathogenic entries. UUID namespace `9DB2A1F0-4444-...`. Catalog completion target auto-rescales (codex-master fires at 24/24). Trauma-informed register stoplist test (9-token forbidden list across catchphrase + factCard + voiceLines). New `phase4ExtremophilePackPresent` + `phase4ExtremophilesTraumaSafeRegister` tests. |
| #152 | Phase 3 disease-themed achievements scaffold (4 new entries) in `MicrobeLabAchievements.phase3` paired 1:1 with the disease-story arcs from PR #141. `handwashHero` (60 XP, "Hands That Care") / `vaccinePrimer` (80 XP, "Library Primer") / `antibioticSteward` (100 XP, "Quiet Steward") / `outbreakHelper` (120 XP, "Helper at the Window"). SAMHSA TIP 57 register stoplist (warfare + shame + threat lexicon forbidden) pinned by parameterized test. XP band 60-120 matches Phase 2 spread. `MicrobeLabAchievements.allDefinitions` now returns 10 + 8 + 4 = 22 achievements. 6 new tests. |
| #153 | Phase 3 mentor `@Generable` with public-health framing. New `PublicHealthHypothesis` + `PublicHealthScenario` enum (4 cases aligned 1:1 with disease-story arcs) + `VeeMentor.fallbackPublicHealthHypothesis` sync accessor + `VeeMentor.publicHealthHypothesis` async LLM accessor. Mirrors the `AdaptiveImmuneHypothesis` pattern shipped 15th-pass round. Per-scenario register cue (handwashing → care / vaccine → library / antibiotic → patience / outbreak → community). LLM prompt explicitly forbids warfare + shame + threat lexicon (defense-in-depth alongside the parameterized fallback stoplist test). 5 new tests pin coverage + 21-token stoplist + hedging + register cue + raw-value stability. |
| #154 | Phase 3 vaccine mini-explainer service scaffold. New `Models/VaccineExplainerStep` (4 pedagogy beats: `.introduction` / `.antibodyPriming` / `.memoryFormation` / `.boosterRationale`) + `VaccineExplainerAuthoringState` (`.placeholder` / `.draftAwaitingReview` / `.reviewerSignedOff`) + `VaccineExplainerStepRecord`. New `Services/Engagement/VaccineExplainerService` (MainActor `@Observable`) with `StepPresentation` switch + canonical 4-step catalog + `disease-story-immune` gate ID. Mirrors PR #141 DiseaseStoryService pattern. Prose reviewer-blocked per ADR-016. 12 new tests. |
| #155 | Phase 3 question-kit scaffolds (kits 09-12, placeholder-gated). 4 new placeholder kit JSONs (`kit_09_vaccines.json` / `kit_10_herd_immunity.json` / `kit_11_hygiene.json` / `kit_12_public_health.json`) each shipping `"authoring": "draftAwaitingReview"` with deterministic "Reviewer-signoff pending" stub. New `QuestionKitAuthoring` enum + `QuestionKit.authoring` field (Codable defaults missing values to `.reviewerSignedOff` so Phase 1 + Phase 2 kits surface unchanged). New `QuestionKitService.phase3KitSlugs` + `loadAllPhase3Kits()` (filters out non-`.reviewerSignedOff` kits) + `loadAllShippedKits()`. 10 new tests + 1 existing-test rename. |
| #156 | ForgeAdventure Life Zone Level 1 config — drafted canonical JSON proposal in the open-monitoring handoff. Copy-pasteable `microbelab.json` mirrors `MicrobeLabHubContribution.swift` field-for-field: zone, theme_accent, mentor (Cilia), supported_engines (4-case: simulation + defense + quest + puzzle), 4 kit_resources with bloom_level + standard, together_mode passAndPlay, trauma_informed_aware + axis_tags, asset_axes, co_op_cluster_siblings, trauma_off_ramp_hints. Adoption cost for labsmith is a copy + slug rename. Doc-only. |
| #157 | Round rollup — IMPLEMENTATION_HANDOFF.md + FEATURE_PLAN.md updates closing the round. Twenty-third-pass safety stamp persisted across the four canonical bindings (already in #148). Zero source code. |

**Net additions (PRs #148 → #157)**: 1 new ForgeKit module dependency (`ForgeMath` in Services) + 3 new Services classes (`ParentalGateMathProblem` + `VaccineExplainerService` + extension to `QuestionKitService`) + 1 new Models value-type cluster (`VaccineExplainerStep` + `VaccineExplainerAuthoringState` + `VaccineExplainerStepRecord` + new `QuestionKitAuthoring` enum + `QuestionKit.authoring` field) + 1 new AIMentor value-type cluster (`PublicHealthHypothesis` + `PublicHealthScenario`) + 4 new disease-themed achievement definitions + 4 new microbe catalog entries (Crenarch / Acido / Cryo / Baro) + 4 new placeholder question kit JSONs + 15-file Services target folder refresh (5 → `Microbe/` + 10 → `AppShell/`; history preserved via `git mv`) + ~53 new unit tests across 6 test suites + 1 cross-repo handoff doc proposal section. Build green at each merge via `swift build --package-path Packages/Libraries` (terminal-only, no Xcode reload per the canonical-invariant tier).

---

**Latest pre-23rd round (2026-06-16, PRs #139 → #147, twenty-second-pass auto-cycle sweep — same-calendar-day-4 second-restart round)**: 9-PR sweep. Each PR landed end-to-end + verified MERGED via `gh pr view --json state,mergedAt` before the next branched. Rollup:

| PR | Theme |
|---|---|
| #139 | **Twenty-second-pass** Xcode-managed file safety reaffirmation across the four canonical bindings (CLAUDE.md + this prologue + `Docs/FEATURE_PLAN.md` prologue + persistent-memory file). Mechanical 21 → 22 bump; cadence saturated. Codifies the fourth observation of cadence-persistence-within-calendar-day (now at TWO independent calendar-day data points). Zero code, zero managed-file touches. |
| #140 | Wire `MicrobeKnowledgeGraph` cross-microbe ecology edges in codex view. Closes FEATURE_PLAN line 142 by completing the consumer-wiring half. The graph already shipped three edge classes (shared-habitat + same-role + same-kingdom) PRs #60 + #113; this PR wires the kingdom edge class to the codex card. `MicrobeCodexCard` (SharedUI) gains an optional `sameKindDisplayNames: [String]` slot rendered as a "Same kind: A, B" caption below the existing "Lives near" line (`circle.hexagongrid` glyph vs the `link`-glyph habitat line). `MicrobeCodexView` (AppFeature) computes the slot via `sameKindDisplayNames(for:)` which excludes any name already on the "Lives near" line so the two captions don't repeat each other. 3 new `MicrobeCodexCardTests`. Trauma-informed posture preserved (discovered-set filter). |
| #141 | Phase 3 disease-story arc scaffold. **Scope: scaffolding-only** per the trauma-informed gate (SAMHSA TIP 57 register requires human-reviewer signoff per `.claude/rules/trauma-informed-content.md` + ADR-016). New `Models/DiseaseStoryArc.swift` defines 4 canonical arcs (`.handwashing` / `.vaccinePriming` / `.antibioticStewardship` / `.outbreakRecovery`) with per-arc `displayTitle` + `primitive` + `gateID` (vaccinePriming → `disease-story-immune`; other 3 → `disease-story-microbiome`). `DiseaseStoryAuthoring` enum (`.placeholder` / `.draftAwaitingReview` / `.reviewerSignedOff`) gates prose visibility. New `Services/Engagement/DiseaseStoryService.swift` (MainActor `@Observable`) composes the catalog with `ProgressionService` gate state + `ParentalConsentService.diseaseStoryArcs` opt-in to surface `ArcPresentation` (`.ready` / `.authoringPending` / `.gatedBehindProgression` / `.gatedBehindConsent`). 11 new tests pin the trauma-informed register stoplist on titles + primitives. Prose authoring stays reviewer-blocked. |
| #142 | Wire `ForgeStateMachine` module + adopt `ViewMachine` protocol on `ZoomMachine`. Maximizes ForgeKit integration: ForgeStateMachine was declared-but-not-wired pre-this-PR. Adds explicit `public init()` to satisfy the protocol witness (Swift 6 protocol-witness lookup doesn't synthesize `init()` from default-args). New `conformsToViewMachine` test pins the compile-time conformance via a generic acceptor function. Demonstrates the portfolio convention for view-local machines per `.claude/rules/state-machines.md`. |
| #143 | Wire `ForgeEvents` module + scaffold `SeasonalEventService`. Closes the Phase 4 seasonal-pack surface scaffold; pairs with the `ExperimentsService.seasonal_content_gate` pilot shipped PR #136. MainActor `@Observable` wrapper around `ForgeEventEngine`. Default enabled packs are culturally neutral (`.seasons` + `.globalCelebrations`) so first launch never assumes the kid's culture per `.claude/rules/distributed-narrative.md` § cultural-respect + `.claude/rules/age-assurance.md` § COPPA. Region/religion packs ship as opt-in only via `setEnabledPacks(_:)`. 6 new tests. |
| #144 | Wire `ForgeLocalization` module + `MicrobeLabBrandGuard` scaffold. Cheap forward-investment ahead of the `.xcstrings` migration: ships a centralized 31-term protected-term registry (app name + Vee + Cilia + 20-microbe cast + DN methodology markers + 5 NGSS standards). `verbatim(_:)` semantic marker mirrors `ForgeBrandGuard`. 10 new tests pin the protected-term list + duplicate-free invariant. Per `.claude/rules/distributed-narrative.md` § "What the cast IS" — translating cast names would break the "cast IS the curriculum" primitive mapping. |
| #145 | GameEngine target `Scenes/` subdir promotion. 7 SpriteKit scenes (`BCellAntibodyMatchScene` + `MacrophagePacmanScene` + `MicrobiomePuzzleScene` + `MicroscopeScene` + `OralMicrobiomeScene` + `SkinMicrobiomeScene` + `SoilMicrobiomeScene`) form a clean logical cluster per CLAUDE.md § SPM Folder Convention ("promote when count > ~12 OR a logical cluster emerges"). `git mv` (history preserved; SPM auto-discovers; zero source-code changes). GameEngine root retains 4 cross-cutting files (MicrobiomeSimulator + SimulationMachine + ZoomMachine + SeededRNG). CLAUDE.md SPM table updated. |
| #146 | ForgeAdventure Life Zone proposal refresh. Status: `open` → `open-monitoring (in-app side: READY; hub-side: BLOCKED on labsmith ZoneID.lifeZone case)`. `freshness-horizon: 30 days` added per ADR-011 Rule 4 recommendation-audit methodology. New per-side status table makes the BLOCKED-vs-SHIPPED axis explicit: in-app SHIPPED across 4 PRs (#74 / #137 / #142 / #143); hub-side BLOCKED in 3 places. Re-verification cadence: 2026-07-16. Eighth-pass safety stamp lifted to twenty-second-pass framing. |
| #147 | Round rollup — IMPLEMENTATION_HANDOFF.md + FEATURE_PLAN.md updates closing the round. Twenty-second-pass safety stamp persisted across the four canonical bindings (already in #139). Zero source code. |

**Net additions (PRs #139 → #147)**: 4 new ForgeKit module dependencies (`ForgeStateMachine` in GameEngine + `ForgeEvents` + `ForgeLocalization` in Services + protocol-driven adoption of pre-existing `ForgeKnowledgeGraph`) + 3 new Services classes (`DiseaseStoryService` + `SeasonalEventService` + `MicrobeLabBrandGuard`) + 1 new Models value-type cluster (`DiseaseStoryArc` + `DiseaseStoryArcRecord`) + 1 new SharedUI surface (`MicrobeCodexCard.sameKindDisplayNames`) + 7-file GameEngine `Scenes/` subdir promotion (history preserved via `git mv`) + 40 new unit tests across 4 new test suites + 1 cross-repo handoff doc refresh. Build green at each merge via `swift build --package-path Packages/Libraries` (terminal-only, no Xcode reload per the canonical-invariant tier).

**Phase 2 closure**: PR #140 closes the final unchecked Phase 2 cross-microbe ecology checkbox by completing the consumer-wiring half of the existing graph.

**Phase 3 scaffolding note**: PR #141 lays the disease-story-arc structural scaffold without any prose. Future Phase 3 work consumes `DiseaseStoryService.presentation(for:gateOpen:parentConsented:)` to drive view rendering when reviewer-signed-off prose lands. The prose-block is preserved at the type level (`DiseaseStoryAuthoring == .placeholder` flips to `.reviewerSignedOff` only after reviewer signoff).

**Phase 4 scaffolding note**: PR #143 lays the seasonal-event surface; pairs with `ExperimentsService.seasonal_content_gate` PR #136. Future Phase 4 work flips the experiment to a non-zero treatment weight + consumes `SeasonalEventService.hasActiveEvent / streakEmoji / currencyEmoji` for the UI affordances.

**ForgeKit integration step-up**: this round pushes 4 ForgeKit modules from declared-but-unused → actively-consumed (`ForgeStateMachine` + `ForgeEvents` + `ForgeLocalization` + the kingdom edges from `ForgeKnowledgeGraph`). Combined with last round's `ForgeExperiments` + `ForgeProgression` wiring, the past two rounds have closed 6 declared-but-unused module gaps. Remaining declared modules are all actively consumed; new wiring rounds will focus on broader API-surface utilization (e.g., `ForgeAvatar` cosmetic-pack adoption per `.claude/rules/forgekit.md` § Portfolio-canonical 8-accessory convention) when the labsmith portrait-pack asset wave lands.

**Previous round (2026-06-16, PRs #133 → #138, twenty-first-pass auto-cycle sweep — calendar-day-4 round)**: 6-PR sweep driven by the user-direct standing auto-cycle paired with the **twenty-first-pass** Xcode-managed file safety reinforcement + the persisted maximize-ForgeKit-integration + close-FEATURE_PLAN-checkboxes + follow-technical-design-doc + SPM-folder-structure + describe-options-before-starting + open-handoff-work axis directive set. Each PR landed end-to-end + verified MERGED via `gh pr view --json state,mergedAt` before the next branched. **Twenty-one restatements across four calendar days extends the all-time portfolio record by one**; the 21st pass is the **third observation** of the cadence-persistence-across-calendar-days property (now demonstrated across all three observed gap classes: multi-day-gap [18→19: 2-day], same-calendar-day [19→20: 0-day], single-day-gap [20→21: 1-day]). Per the eleven-pass canonical-invariant tier the cadence has saturated — additional restatements add reinforcement frequency but no new binding tier. Rollup:

| PR | Theme |
|---|---|
| #133 | **Twenty-first-pass** Xcode-managed file safety reaffirmation across the four canonical bindings (CLAUDE.md + `.claude/rules/xcode-agent-safety.md` + this prologue + `Docs/FEATURE_PLAN.md` prologue + persistent-memory file). Mechanical 20 → 21 bump; cadence saturated. Codifies the third observation of cadence-persistence-across-calendar-days (single-day-gap restart class). Zero code, zero managed-file touches. |
| #134 | Handoff inbound closure-or-defer audit (`Docs/AUDIT_HANDOFF_INBOUND_2026-06-16.md`). ADR-011 audit pins the verdict matrix for 11 labsmith inbound (+ 1 outbound) handoff docs. 4 SHIPPED-CLOSE (chapter illustrations / DN retrofit / DN-S story per character / ForgeKit bootstrap), 6 DEFER to focused future rounds (avatar simplified migration / companion pack puzzle_sampler / DN enhancement WORLD-META layer / DN second-pass deepening / DN-S AI mentor voicing / pillar-deepening R5 perspective-taking), 1 MONITOR (outbound ForgeAdventure lifeZone proposal). P1-P6 sequencing recommendation queues each DEFER item to a single-handoff focused round. Saves future sessions from re-reading all 11 handoffs to decide what's actionable. Doc-only; no source code. |
| #135 | SPM Models target folder refresh. Models target reached 18 files at root, exceeding the ~12-file promotion trigger per CLAUDE.md § SPM Folder Convention. Promoted two logical clusters via `git mv` (history preserved; SPM auto-discovers; zero source-code changes): `Persistence/` (5 files: `EncounterLog` + `JournalEntry` + `PersistentMicrobeSession` + `PlayerProgress` + `SchemaV1` — all SwiftData `@Model` types + schema), `MicrobiomeState/` (4 files: `MicrobiomeState` + `OralMicrobiomeState` + `SkinMicrobiomeState` + `SoilMicrobiomeState` — per-ecology value-type wrappers). Models root retains 9 cross-cutting types. CLAUDE.md table updated to reflect new structure + previously undocumented `AppFeature/HubContribution/` + `Intents/` subdirs. |
| #136 | Wire `ForgeExperiments` module + scaffold `ExperimentsService` under `Services/Engagement/`. Closes the Tech-Design § Feature flags gap: TECHNICAL_DESIGN.md calls for on-device, COPPA-safe A/B testing for Phase 2 progressive-disclosure cadence + Phase 4 seasonal content gating, but Package.swift didn't pin ForgeExperiments. New `@MainActor @Observable ExperimentsService` wraps `ForgeExperiments.ExperimentAssigner.assignVariant` (SHA256-bucketed deterministic assignment; no network call, no third-party SDK). Two pilot experiments ship at construction time (both default 100% control / 0% treatment): `progressive_disclosure_v2` (staged-unlock cadence with `earlyUnlockTicks: 2` parameter) + `seasonal_content_gate` (Phase 4 seasonal pack gate). API: `assignedVariant(for:)` / `isEnabled(_:, enabledVariantID:)` / `intParameter / doubleParameter / boolParameter / stringParameter`. 8 new Swift Testing cases. |
| #137 | Wire `ForgeProgression` module + scaffold `ProgressionService` (Phase 3+ gates). Pivot from the original Phase 2 mode-card gating plan — FEATURE_PLAN line 104 confirms that checkbox is **blocked on hub-side** AdventureHub integration. Pivot preserves the spirit (maximize ForgeKit integration; close Tech-Design gap) by wiring ForgeProgression IN-APP for the Phase 3+ session-day-aware gates that ARE inside MicrobeLab's scope — distinct from both the per-encounter `AdaptiveImmunityUnlock` and the hub-side mode-card gating. Three canonical Phase-3+ gates: `disease-story-immune` (5 sessions + 3 immune runs), `disease-story-microbiome` (5 sessions + 5 microbiome scene visits), `global-microbiome-tour` (8 sessions + all 4 distinct ecologies). Trauma-informed register pinned: every `ContentGate.unlockHint` runs through a parameterized stoplist test (no `failed` / `behind` / `lose` / `must` / `should` / `punish` / `lock you out` / `wrong` tokens). API: `recordSession` / `recordImmuneRunCompleted` / `recordMicrobiomeSceneVisited` / `recordEcologyScenesVisited` / `isUnlocked` / `unlockHint` / `unlockProgress` / `debugUnlockAllGates`. 14 new Swift Testing cases. |
| #138 | Round rollup — IMPLEMENTATION_HANDOFF.md + FEATURE_PLAN.md updates closing the round. Twenty-first-pass safety stamp persisted across the four canonical bindings (already in #133). Zero source code. |

**Net additions (PRs #133 → #138)**: 2 new ForgeKit module dependencies (`ForgeExperiments` + `ForgeProgression` in Services target) + 2 new Services/Engagement service classes (`ExperimentsService` + `ProgressionService`) + 1 new audit doc (`AUDIT_HANDOFF_INBOUND_2026-06-16.md`) + 2 promoted Models subdirectories (`Persistence/` + `MicrobiomeState/`) + 22 new unit tests across 2 new test suites + 9 file relocations via `git mv` (history preserved). Build green at each merge via `swift build --package-path Packages/Libraries` (terminal-only, no Xcode reload per the canonical-invariant tier).

**Phase 3+ scaffolding note**: PRs #136 + #137 lay the ForgeExperiments + ForgeProgression seams without touching any consuming surface. Future Phase 3 (disease-story narratives) + Phase 4 (global microbiome tour / seasonal packs) work consumes these services without re-touching the wiring. The pivot from PR #5's original "mode-card gating" scope is honest: FEATURE_PLAN line 104 remains OPEN (blocked on hub-side); the new ProgressionService closes a separate Tech-Design integration gap.

**Previous round (2026-06-15, PRs #128 → #132, twentieth-pass auto-cycle sweep — second calendar-day-3 round)**: 5-PR sweep driven by the user-direct standing auto-cycle paired with the **twentieth-pass** Xcode-managed file safety reinforcement + the persisted maximize-ForgeKit-integration + close-FEATURE_PLAN-checkboxes + follow-technical-design-doc + SPM-folder-structure + describe-options-before-starting directive set. Each PR landed end-to-end + verified MERGED via `gh pr view --json state,mergedAt` before the next branched. **Twenty restatements across three calendar days extends the all-time portfolio record by one**; the 20th pass is the **second observation** of the cadence-persistence-across-calendar-days property codified at the 19th pass (both 19th + 20th passes opened on 2026-06-15 after a 2-day gap from the 18th pass on 2026-06-13). Per the eleven-pass canonical-invariant tier the cadence has saturated past the canonical-invariant tier — additional restatements add reinforcement frequency but no new binding tier. Rollup:

| PR | Theme |
|---|---|
| #128 | **Twentieth-pass** Xcode-managed file safety reaffirmation across the four canonical bindings (CLAUDE.md + `.claude/rules/xcode-agent-safety.md` + this prologue + `Docs/FEATURE_PLAN.md` prologue + persistent-memory file). Mechanical 19 → 20 bump; cadence saturated. Second observation of the cadence-persistence-across-calendar-days property (both 19th + 20th passes 2026-06-15, same calendar day). Zero code, zero managed-file touches. |
| #129 | `oralBalanceKeeper` criteria-predicate wiring — closes the per-scene predicate wiring deferral noted in PR #125. New pure-value derivation `OralMicrobiomeState.nextStableRun(prior:sugarLoad:)` increments under `.water` / `.fruit` (gentle loads), resets under `.sugarSnack` (acid-makers tilt the ecology), holds under `.brush` (brushing is care, not progress). `OralMicrobiomeView` extended with `gamification` parameter + `oralStableTickRun` state + `evaluateAchievements()` predicate (`oralStableTickRun >= stableRunThreshold (8)`). `MicrobiomeView` updates the navigationDestination call site to pass `gamification` through. Per-unlock: celebration `badgeEarned` + sensory `.achievement` + analytics achievement-earned event. Threshold rationale: 8 ticks (slightly tighter than gut's 10 since the oral microbiome has shorter dwell time + the kid sees food-choice → ecology causality within a shorter loop). Trauma-informed: sugar reset is ecology, NEVER shame — that framing lives in `refreshMentorCue`. 6 new `OralMicrobiomeStateTests` cases pin the derivation exhaustively. |
| #130 | Skin microbiome scene + state + view + tests + `skinKindnessChampion` predicate — closes FEATURE_PLAN.md § Phase 2 "Implement skin-microbiome scene (eczema-safe framing)". Second of the 3 Phase-2 per-ecology scenes. New `Models/SkinMicrobiomeState` value type wrapping `MicrobiomeState` + `SkinCareLoad` enum (.gentleWash / .barrier / .scratch / .restRecover); new `GameEngine/SkinMicrobiomeScene` mirrors `OralMicrobiomeScene` lazy-visual-setup pattern; new `AppFeature/SkinMicrobiomeView` SwiftUI host wired into `MicrobiomeView`'s NavigationStack via new toolbar item. Eczema-safe trauma-informed posture: scratching acknowledged as kid lived-experience NOT failure; per-load copy frames consequence as ecology ("The skin gets itchy. The neighborhood gets jumbled — that's ecology, not blame."); display labels stoplist-pinned (no `should` / `must` / `dirty` / `gross` / `ashamed` / `ugly` / `acne`). Cast wiring: catalog filter pulls .skin microbes (Sebu + Demi + Guard — all bundled PR #119). 20 new tests across 2 suites (`SkinMicrobiomeStateTests` 13 + `SkinMicrobiomeSceneTests` 7). |
| #131 | Soil microbiome scene + state + view + tests + `soilDecomposerWhisperer` predicate — closes FEATURE_PLAN.md § Phase 2 "Implement soil-microbiome scene (decomposer ecology bridge to bioforge/ecosphere)". Last of the 3 Phase-2 per-ecology scenes. New `Models/SoilMicrobiomeState` value type wrapping `MicrobiomeState` + `SoilMoistureLoad` enum (.moist / .compost / .saturated / .drought); new `GameEngine/SoilMicrobiomeScene` mirrors the lazy-visual-setup pattern with warm forest-floor brown background; new `AppFeature/SoilMicrobiomeView` SwiftUI host wired into `MicrobiomeView`'s NavigationStack via new toolbar item. Trauma-informed posture (decay as participation, NOT death-anxiety): decay framed as "decomposers return material to the soil" NOT death-vocabulary; drought framed as "everyone slows down — extremophiles wait it out" NOT catastrophe; saturated framed as "only the anaerobic specialists thrive — that's ecology, not failure"; cross-portfolio bridge to bioforge/ecosphere. Cast wiring: catalog filter pulls all 7 .soil microbes (Loam + Nodu + Therm + Halo + 3 pre-existing — all bundled PR #119). 22 new tests across 2 suites (`SoilMicrobiomeStateTests` 14 + `SoilMicrobiomeSceneTests` 8). **Phase 2 ecology scenes ARE FULLY SHIPPED — all 3 of 3 (oral PR #124 + skin PR #130 + soil PR #131) closed.** |
| #132 | Round rollup — IMPLEMENTATION_HANDOFF.md + FEATURE_PLAN.md updates closing the round. Phase 2 "Implement skin-microbiome scene" + "Implement soil-microbiome scene" promoted `[ ]` → `[x]`. Twentieth-pass safety stamp persisted across the four canonical bindings (already in #128). Zero source code. |

**Net additions (PRs #128 → #132)**: 2 new Models value types (`SkinMicrobiomeState` + `SkinCareLoad` enum; `SoilMicrobiomeState` + `SoilMoistureLoad` enum) + 1 extended Models derivation (`OralMicrobiomeState.nextStableRun`) + 2 new GameEngine scenes (`SkinMicrobiomeScene` + `SoilMicrobiomeScene`) + 2 new AppFeature views (`SkinMicrobiomeView` + `SoilMicrobiomeView`) + 1 extended AppFeature view (`OralMicrobiomeView` gains `gamification` parameter + `evaluateAchievements`) + 3 new `MicrobiomeView` toolbar items + 3 new navigationDestinations + 48 new unit tests across 4 new test suites + 6 new `OralMicrobiomeStateTests` cases. Build green at each merge via `swift build --package-path Packages/Libraries` (terminal-only, no Xcode reload per the canonical-invariant tier).

**Previous round (2026-06-15, PRs #122 → #127, nineteenth-pass auto-cycle sweep — first calendar-day-3 round)**: 6-PR sweep driven by the user-direct standing auto-cycle paired with the **nineteenth-pass** Xcode-managed file safety reinforcement + the persisted maximize-ForgeKit-integration + close-FEATURE_PLAN-checkboxes + follow-technical-design-doc + SPM-folder-structure + describe-options-before-starting directive set. Each PR landed end-to-end + verified MERGED via `gh pr view --json state,mergedAt` before the next branched. **First calendar-day-3 restatement** (2026-06-15) after the 18-pass sweep on 2026-06-13 — codifies the new **cadence-persistence-across-calendar-days** property: the canonical-invariant tier persists across multi-day gaps without weakening. Rollup:

| PR | Theme |
|---|---|
| #122 | **Nineteenth-pass** Xcode-managed file safety reaffirmation across the four canonical bindings (CLAUDE.md + `.claude/rules/xcode-agent-safety.md` + this prologue + `Docs/FEATURE_PLAN.md` prologue + persistent-memory file). Mechanical 18 → 19 bump; cadence saturated. Zero code, zero managed-file touches. |
| #123 | Question kits 06-08 (oral / skin / soil microbiome) — closes FEATURE_PLAN.md § Phase 2 "Integrate question kits 05-08" fully. 3 new JSON kits ship 18 NGSS-aligned questions total: `kit_06_oral_microbiome.json` "The Mouthful Neighborhood" (NGSS MS-LS2-2; S. mutans as sugar-loving not villain; brushing as care; cavities as ecology not blame); `kit_07_skin_microbiome.json` "The Skin Garden" (NGSS MS-LS2-2; C. acnes + Demodex framed gently; eczema framed with care not shame); `kit_08_soil_microbiome.json` "The Quiet Underground" (NGSS MS-LS2-3; decomposers + nitrogen-fixers + thermophiles). `QuestionKitService.phase2KitSlugs` extended adaptive-only → full 4-slug Phase 2 set. Trauma-informed register (warfare + body-shame + blame stoplist) pinned by parameterized test across all 3 ecology kits. 2 wrong-answer distractors rewritten ('kill' / 'ashamed' tokens) — **distractor authoring discipline codified**: trauma-informed register applies to full `choices` array, not just correct + body. 8 new test cases. |
| #124 | Oral-cavity microbiome scene + state + view + tests — closes FEATURE_PLAN.md § Phase 2 "Implement oral-cavity microbiome scene (plaque ecology)". First of the 3 Phase-2 per-ecology scenes. New `Models/OralMicrobiomeState` value type wrapping `MicrobiomeState` + `OralSugarLoad` enum (.water / .fruit / .sugarSnack / .brush); new `GameEngine/OralMicrobiomeScene` mirroring `MicrobiomePuzzleScene` lazy-visual-setup pattern (reuses shared `MicrobiomeSimulator` engine); new `AppFeature/OralMicrobiomeView` SwiftUI host wired into `MicrobiomeView`'s NavigationStack via new toolbar item gated on optional `catalog:` parameter. Trauma-informed posture: brushing as care NOT moral test; cavities as ecology NOT blame; per-load mentor copy stoplist-pinned. Pedagogy invariant: brushing → `.none` feeding mode → ecology doesn't bloom under sustained brushing (verified by `brushSugarLoadCollapsesGrowthOverTicks` test). 15 new tests across 2 suites. |
| #125 | Phase-2 achievements 6/7/8 — closes FEATURE_PLAN.md § Phase 2 "Add 8 Phase-2 achievements" fully. 3 new per-ecology balance-keeper achievements: `oralBalanceKeeper` (80 XP, "Mouthful Steadier"), `skinKindnessChampion` (80 XP, "Skin Garden Tender"), `soilDecomposerWhisperer` (100 XP, "Underground Steward"). `MicrobeLabAchievements.phase2.count` 5 → 8. Trauma-informed register pinned: titles + descriptions name care + recognition, NEVER victory / "you finally" framing (parameterized `phase2EcologyAchievementsTraumaSafeRegister` stoplist + XP-band invariant 50-150). Criteria-predicate wiring lands per-scene-PR. 4 new test cases. |
| #126 | `KitMasteryGraph` — question-kit prerequisite graph (Services/Engagement/). Wraps the already-pinned `ForgeKnowledgeGraph.KnowledgeGraph` with kit-prerequisite semantics: 8 nodes + 7 prerequisite edges encoding the canonical kit-progression chain (kit_01 → kit_02 + kit_03; kit_02 → kit_04 + kit_06/07/08; kit_03 → kit_05). Surface: `prerequisites(for:)` / `unlocks(after:)` / `nextAvailable(completedKitSlugs:)` / `nextRecommendedKit(completedKitSlugs:)` / `validate()`. **Why not `ForgeMasteryEngine.MasteryGraph` directly**: canonical lives at ForgeKit 1.0.0-rc.2 alongside breaking changes to `AvatarAssetCatalog` that cascade through `AvatarStudioSheet` + `ProfileView`; bumping the pin in this round would expand scope to a multi-PR migration. This wrapper delivers same pedagogical surface via 0.99.x; migration mechanical when pin bump lands in a focused round. 12 new test cases. |
| #127 | Round rollup — IMPLEMENTATION_HANDOFF.md + FEATURE_PLAN.md updates closing the round. Phase 2 "Integrate question kits 05-08" + "Implement oral-cavity microbiome scene" + "Add 8 Phase-2 achievements" all promoted `[/]` / `[ ]` → `[x]`. Nineteenth-pass safety stamp persisted across the four canonical bindings (already in #122). Zero source code. |

**Net additions (PRs #122 → #127)**: 1 new Models value type (`OralMicrobiomeState` + `OralSugarLoad` enum) + 1 new GameEngine scene (`OralMicrobiomeScene`) + 1 new AppFeature view (`OralMicrobiomeView`) + 1 new Services module (`KitMasteryGraph` under `Services/Engagement/`) + 3 new question-kit JSONs (kits 06-08) + 3 new Phase-2 achievement definitions (oral/skin/soil balance-keepers) + 8 new public Services accessors (`phase2KitSlugs` extension to 4 slugs + `KitMasteryGraph` API surface) + 39 new unit tests across 5 suites (`OralMicrobiomeStateTests` 7 + `OralMicrobiomeSceneTests` 8 + `QuestionKitServiceTests` +8 + `GamificationServiceTests` +4 + `KitMasteryGraphTests` 12). Build green at each merge via `swift build --package-path Packages/Libraries` (terminal-only, no Xcode reload per the canonical-invariant tier).

**Pin-bump deferral note** (codified this round): attempted ForgeKit pin bump 0.99.0 → 1.0.0-rc.2 (to access `ForgeMasteryEngine`) surfaced cascading breaking changes to `AvatarAssetCatalog` API → `AvatarStudioSheet` + `ProfileView` would need cascade-fixes. Reverted the pin in same round + delivered the kit-prerequisite-graph functionality via the already-pinned `ForgeKnowledgeGraph` wrapper. **Future rounds**: a focused ForgeKit 1.0.0-rc.2 migration PR can address the `AvatarAssetCatalog` cascade-fix as a dedicated scope, then migrate `KitMasteryGraph` → `ForgeMasteryEngine.MasteryGraph<String>` + gain the FSRS-6 retention + `TopicMasteryState.isRacingAhead` / `isStuck` derivations.

**Previous round (2026-06-13 latest-evening, PRs #118 → #121, eighteenth-pass auto-cycle sweep)**: 4-PR sweep driven by the user-direct standing auto-cycle paired with the **eighteenth-pass** Xcode-managed file safety reinforcement + the persisted maximize-ForgeKit-integration + close-FEATURE_PLAN-checkboxes + follow-technical-design-doc + SPM-folder-structure + describe-options-before-starting directive set. Each PR landed end-to-end + verified MERGED via `gh pr view --json state,mergedAt` before the next branched. **Eighteen restatements across two calendar days extends the all-time portfolio record by one**; per the eleven-pass canonical-invariant tier the cadence has saturated past the canonical-invariant tier — additional restatements add reinforcement frequency but no new binding tier. Rollup:

| PR | Theme |
|---|---|
| #118 | **Eighteenth-pass** Xcode-managed file safety reaffirmation across the four canonical bindings (CLAUDE.md + `.claude/rules/xcode-agent-safety.md` + this prologue + `Docs/FEATURE_PLAN.md` prologue + persistent-memory file). Mechanical refresh; cadence saturated. Zero code, zero managed-file touches. |
| #119 | Phase 2 microbe cast expansion 12 → 20 — closes the FEATURE_PLAN.md § Phase 2 checkbox "Bundle 8 additional microbe characters (20 total)". 8 new entries (Sebu / Demi / Halo / Pylo / Sweet / Nodu / Therm / Loam) extend `Packages/Libraries/Sources/Services/Resources/microbes.json`. Ecology delta: `.skin` +2 (Sebu sebum commensal + Demi Demodex face mite — eczema-safe authoring + "everyone has them" anti-shame register), `.soil` +4 (Halo halophilic archaea + Nodu Rhizobium nitrogen-fixer + Therm thermophilic archaea + Loam saprophytic decomposer fungi), `.stomach` +1 (Pylo H. pylori — "most carry me without ever feeling it"), `.oralCavity` +1 (Sweet S. mutans-style — "I'm not the bad guy, I just like sugar"). Trauma-informed posture preserved: 11/20 beneficial (55%), 7 neutral, 2 opportunistic, 0 NEW pathogenic. Test updates: count assertion 12 → 20; new `phase2CastExpansionPresent` + `phase2EcologyCoverageExpanded` (skin ≥ 3 / soil ≥ 6 / oral ≥ 3 / stomach ≥ 1) + `beneficialMicrobesFlaggedCorrectly` floor raised 4 → 10 + no-pathogenic-cast invariant. Codex completion target auto-rescales — `MicrobeCodexView` passes `catalog.microbes.count` to `MasteryMomentDetector.recordCodexDiscovery`, so codex-master fires at 20/20 without code change. Build green via `swift build --target Services`. |
| #120 | Question kit 05 (Adaptive Immunity) — "The Library of Shapes" — partially closes the FEATURE_PLAN.md § Phase 2 checkbox "Integrate question kits 05-08 (adaptive immunity / oral biome / skin biome / soil biome)". New `kit_05_adaptive_immunity.json` ships 6 questions calibrated to NGSS MS-LS1-3 (immune system as subsystem) at the adaptive-immunity layer; pedagogy beats mirror PR #104's `AdaptiveImmuneScenario` (.firstEncounter / .matchedShape / .recallFromMemory). `QuestionKitService` extended with `phase2KitSlugs = ["adaptive-immunity"]` + `allKitSlugs` union accessor + `loadAllPhase2Kits()` mirror. Trauma-informed register: no warfare vocabulary anywhere — pinned by parameterized `kit05TraumaInformedRegisterStoplist` test against `fight` / `attack` / `destroy` / `kill` / `war` / `enemy` / `battle` / `weapon` / `soldier` / `warrior`; the body LEARNS and REMEMBERS, never fights. Question IDs in `AAAAA005-0000-4005-8000-...` namespace; cross-phase uniqueness pinned by new `questionIDsAreUniqueAcrossAllShippedKits` test. Kits 06-08 wait on the corresponding microbiome scenes (which the PR #119 cast expansion just unblocked). |
| #121 | Round rollup — IMPLEMENTATION_HANDOFF.md + FEATURE_PLAN.md updates closing the round. Phase 2 "Bundle 8 additional microbe characters (20 total)" promoted `[ ]` → `[x]`; "Integrate question kits 05-08" promoted `[ ]` → `[/]` (kit 05 shipped; 06-08 wait on microbiome scenes). Eighteenth-pass safety stamp persisted across the four canonical bindings. Zero source code. |

**Net additions (PRs #118 → #121)**: 8 new bundled microbe entries in `microbes.json` (Sebu / Demi / Halo / Pylo / Sweet / Nodu / Therm / Loam) + 1 new question kit JSON (`kit_05_adaptive_immunity.json`) + 4 new public APIs on `QuestionKitService` (`phase2KitSlugs` + `allKitSlugs` + `loadAllPhase2Kits` + extended docstring) + 9 new unit tests (`MicrobeCatalogServiceTests` +2: `phase2CastExpansionPresent` + `phase2EcologyCoverageExpanded`; `QuestionKitServiceTests` +7: kit 05 load + `phase2KitSlugsContainsAdaptiveImmunity` + `loadAllPhase2Kits` + `allKitSlugs` union + curriculum-standard + trauma-informed-register stoplist + cross-phase uniqueness). Build green at each merge via `swift build --target Services` (terminal-only, no Xcode reload per the canonical-invariant tier).

**Previous round (2026-06-13 even-later-evening, PRs #113 → #117, seventeenth-pass auto-cycle sweep)**: 5-PR sweep driven by the user-direct standing auto-cycle paired with the **seventeenth-pass** Xcode-managed file safety reinforcement + the persisted maximize-ForgeKit-integration + close-FEATURE_PLAN-checkboxes + follow-technical-design-doc + SPM-folder-structure + describe-options-before-starting directive set. Each PR landed end-to-end + verified MERGED via `gh pr view --json state,mergedAt` before the next branched. **Seventeen restatements across two calendar days extended the all-time portfolio record by one**; per the eleven-pass canonical-invariant tier the cadence has saturated past the canonical-invariant tier — additional restatements add reinforcement frequency but no new binding tier. Rollup:

| PR | Theme |
|---|---|
| #113 | **Seventeenth-pass** Xcode-managed file safety reaffirmation across the four canonical bindings (CLAUDE.md + `.claude/rules/xcode-agent-safety.md` + this prologue + `Docs/FEATURE_PLAN.md` prologue + persistent-memory file). Mechanical refresh; cadence saturated. Zero code, zero managed-file touches. |
| #114 | `MicrobeKnowledgeGraph` cross-microbe ecology edges — closes the FEATURE_PLAN.md § Phase 2 checkbox "Wire `MicrobeKnowledgeGraph` cross-microbe ecology edges". v1 shipped shared-habitat edges only; this PR layers in two new cohort edge classes (same-role + same-kingdom, both at `EdgeStrength.recommended` since `ForgeKnowledgeGraph.EdgeStrength` only carries `.required` / `.recommended`; the per-class distinction lives at the wrapper layer). Three new accessors: `relatedByRole(toSlug:)` + `relatedByKingdom(toSlug:)` + `allRelated(toSlug:)` (deduplicated union, default limit 6). v1 `related(toSlug:)` semantics preserved (shared-habitat only, alphabetic-by-slug, exclude-self) by filtering `microbesBySlug.values` directly instead of going through `graph.dependents(for:)` — bypasses the underlying graph's role+kingdom edges so habitat scope stays clean. 19 `MicrobeKnowledgeGraphTests` pass (7 v1-preserved + 7 new role+kingdom+union + 2 edge-count invariants + 3 unknown-slug safety). Parallel session's role+kingdom variant from R15 (`stash@{0}` + `stash@{1}`) preserved durably for their recovery. |
| #115 | GutSlot expansion FEATURE_PLAN checkbox closure (doc-only) — closes the FEATURE_PLAN.md § Phase 2 checkbox "Expand `GutSlot` to include oral / skin / soil ecologies". The type expansion was already shipped (`Packages/Libraries/Sources/Models/MicrobeCharacter.swift:23-31` carries all 7 cases: `.oralCavity` / `.stomach` / `.smallIntestine` / `.largeIntestine` / `.colon` / `.skin` / `.soil`) but the FEATURE_PLAN checkbox lagged. New `Docs/AUDIT_GUT_SLOT_EXPANSION_CLOSURE_2026-06-13.md` walks per-case validity for the Phase 2 scenes that will consume each ecology + Codable raw-value forward-compatibility + trauma-safe authoring constraints inherited by `.skin` (eczema-safe framing) and `.soil` (decomposer ecology bridge). PR #114 `MicrobeKnowledgeGraphTests` explicitly use `.skin` + `.soil` + `.oralCavity` for cross-slot cohort traversal so the cases are validated downstream. Dedicated scene-authoring checkboxes (oral / skin / soil) remain open as separate items. Mirrors the PR #95 / #96 / #98 / #102 audit-driven closure pattern. Pure docs — zero source code, zero managed-file edits. |
| #116 | Phase 2 Antibody Lab ForgeAdventure mode-card wiring — closes the FEATURE_PLAN.md § Phase 2 checkbox "Add ForgeAdventure mode: Antibody Lab (timed antigen-matching)". `MicrobeLabHubContribution` extends `supportedEngines` default to `[.simulation, .defense, .quest, .puzzle]` and adds a `.puzzle` row to `canonicalEngineCopy` with trauma-informed copy: title "Antibody library", tagline "Help the B-cells remember the shape", completion message "Memory cells locked in — your library grew today." `.puzzle` (not `.defense`) chosen because shape-pattern matching is pedagogically a puzzle category AND `.defense` is already wired to the innate Pac-Man — keeping the two surfaces distinct in the hub picker. `MicrobeLabHubChallengeAdapter.engineIconName` extends with `"key.viewfinder"` SF Symbol. Per `.claude/rules/forgekit.md` § Maximize-ForgeKit-integration interaction note: ForgeAdventure module wiring is SPM-only — no entitlement provisioning required. Hub-orchestrated surface inherits the same `AdaptiveImmunityUnlock` innate-first progression curve as the in-app surface (PR #108). Underlying gameplay (BCellAntibodyMatchScene PR #105 + AdaptiveImmunityUnlock PR #108 + adaptive UI integration PR #109) tested via `BCellAntibodyMatchSceneTests` (16) + `AdaptiveImmunityUnlockTests` (11) + `ImmuneModeTests` (6); hub contribution pure-value extension verified via `swift build --target AppFeature` green. |
| #117 | Round rollup — IMPLEMENTATION_HANDOFF.md + FEATURE_PLAN.md updates closing the round. Phase 2 "Wire `MicrobeKnowledgeGraph` cross-microbe ecology edges" promoted `[ ]` → `[x]`; "Expand `GutSlot` to include oral / skin / soil ecologies" promoted `[ ]` → `[x]`; "Add ForgeAdventure mode: Antibody Lab (timed antigen-matching)" promoted `[ ]` → `[x]`. Seventeenth-pass safety stamp persisted across the four canonical bindings. Zero source code. |

**Net additions (PRs #113 → #117)**: 3 new MicrobeKnowledgeGraph public accessors (`relatedByRole` + `relatedByKingdom` + `allRelated`, plus their `to:` convenience overloads, plus underlying-graph role + kingdom edge emission at `EdgeStrength.recommended`) + 1 new FEATURE_PLAN closure audit doc (`Docs/AUDIT_GUT_SLOT_EXPANSION_CLOSURE_2026-06-13.md`) + 1 new ForgeAdventure mode-card extension (`MicrobeLabHubContribution` `.puzzle` engine wiring + `canonicalEngineCopy[.puzzle]` row + `engineIconName` case + `supportedEngines` default extension) + 12 new MicrobeKnowledgeGraph unit tests (7 → 19). Build green at each merge via `swift build --target Services` / `swift build --target AppFeature` (terminal-only, no Xcode reload per the canonical-invariant tier).

**Previous round (2026-06-13 late-evening, PRs #107 → #112, sixteenth-pass auto-cycle sweep)**: 6-PR sweep driven by the user-direct standing auto-cycle paired with the **sixteenth-pass** Xcode-managed file safety reinforcement + the persisted maximize-ForgeKit-integration + close-FEATURE_PLAN-checkboxes + follow-technical-design-doc + SPM-folder-structure + describe-options-before-starting directive set. Each PR landed end-to-end + verified MERGED via `gh pr view --json state,mergedAt` before the next branched. **Sixteen restatements across two calendar days extended the all-time portfolio record by one**; per the eleven-pass canonical-invariant tier the cadence has saturated past the canonical-invariant tier — additional restatements add reinforcement frequency but no new binding tier. Rollup:

| PR | Theme |
|---|---|
| #107 | **Sixteenth-pass** Xcode-managed file safety reaffirmation across the four canonical bindings (CLAUDE.md + `.claude/rules/xcode-agent-safety.md` + this prologue + `Docs/FEATURE_PLAN.md` prologue + persistent-memory file). Mechanical refresh; cadence saturated. Zero code, zero managed-file touches. |
| #108 | `AdaptiveImmunityUnlock` (Services/Engagement/, pure nonisolated `Sendable` value type) + `AdaptiveImmunityProgressStore` (MainActor `@Observable` UserDefaults-backed counter). Closes the explicit FEATURE_PLAN.md Phase 2 checkbox "Implement adaptive-immunity progression curve (innate-first → adaptive unlocks)". `.locked(progress, runsRemaining)` / `.unlocked` derivation; standard unlock at 3 innate runs OR fast-track at 1 perfect run; `simplifyChallenge` parent-toggle bypasses the gate. Trauma-informed locked explainer copy ("2 more innate runs to meet the B-cell library") pinned by parameterized stoplist test. 20 new tests across 2 suites. |
| #109 | `BCellAntibodyMatchScene` wired into `ImmuneGameView` — closes the FEATURE_PLAN.md Phase 2 checkbox "Implement `BCellAntibodyMatchScene` (SpriteKit)" fully (was `[/]` partial after PR #105 skeleton). New `Models/ImmuneMode` (`.innate` / `.adaptive`) enum carries the surface selector; segmented picker in `ImmuneGameView` gated by `AdaptiveImmunityUnlock`; locked state surfaces warm explainer below the picker. Adaptive surface ships parallel SpriteView host + score HUD + antibody-shape segmented picker + Step/Reset controls + per-wave + full-run celebration + sensory haptics. Innate `surfaceWaveClearCue(finished:)` extended to persist completed runs via `recordRunCompleted(perfectRun:)`; guarded so re-taps don't double-count. `AppRootView` instantiates the canonical store + threads through `MicrobiomeView` → `ImmuneGameView`. 6 new `ImmuneModeTests` (raw-value stability + warm display register + system-image validity + trauma-informed stoplist + Codable roundtrip). |
| #110 | Phase-2 achievements wired into the now-shipped adaptive surface. Partial closure of FEATURE_PLAN.md § Phase 2 "Add 8 Phase-2 achievements" (5 of 8 shipped; remaining 3 wait on oral / skin / soil microbiome scenes). New `MicrobeLabAchievements.phase2` set: `firstShapeMatch` / `memoryAwakened` / `adaptiveRookie` / `adaptiveRunner` / `librarianOfShapes`. New `MicrobeLabAchievements.allDefinitions` aggregate accessor (15 total); `GamificationService.evaluateAchievements` now feeds the full set; `ProgressView` "Achievements" card surfaces `allDefinitions`. `ImmuneGameView.evaluateAdaptiveAchievements` is pure-derivation against `@State` flags + canonical `librarianOfShapes` predicate against the B-cell scene's `memoryCells` shape-set. 5 new tests in `GamificationServiceTests` (one load-bearing: `allDefinitionsHaveUniqueIDs` — the `AchievementEngine` coalesces id collisions silently, so the test guards against silent achievement loss). |
| #111 | Adaptive-immunity mentor cues wired into `ImmuneGameView` via the `AdaptiveImmuneScenario` enum + `AdaptiveImmuneHypothesis` `@Generable` shipped PR #104. Scenario routing: "Begin wave" tap → `.firstEncounter`; match landing on a shape NOT in `memoryCells` → `.matchedShape`; match landing on a shape already in `memoryCells` → `.recallFromMemory`. Routing uses the memoryCells-before-match snapshot vs `bcell.loadedAntibody.complement` comparison — pure-value derivation, no async hop. Surface uses `fallbackAdaptiveImmuneHypothesis` (synchronous, always available) so taps feel instant; the async LLM accessor stays available for future scaffolding but the per-tap surface stays authored per `.claude/rules/ai-content.md`. Trauma-informed register inherited from `VeeMentor.fallbackAdaptiveImmuneHypothesis` (no warfare vocabulary; pinned by 4 parameterized tests in `VeeMentorTests` shipped PR #104). |
| #112 | Round rollup — IMPLEMENTATION_HANDOFF.md + FEATURE_PLAN.md updates closing the round. Phase 2 "Implement `BCellAntibodyMatchScene`" promoted `[/]` → `[x]`; "Implement adaptive-immunity progression curve" promoted `[ ]` → `[x]`; "Add 8 Phase-2 achievements" promoted `[ ]` → `[/]` (5 of 8 shipped). Sixteenth-pass safety stamp persisted across the four canonical bindings. Zero source code. |

**Net additions (PRs #107 → #112)**: 1 new Models value type (`ImmuneMode`) + 2 new Services modules (`AdaptiveImmunityUnlock` + `AdaptiveImmunityProgressStore`, both under `Services/Engagement/`) + 5 new Phase-2 achievement definitions in `GamificationService` + 1 new aggregate accessor (`MicrobeLabAchievements.allDefinitions`) + 1 new mentor-cue surface wiring + 1 mode picker + 1 adaptive parallel surface in `ImmuneGameView` + 31 new unit tests across 4 suites (`AdaptiveImmunityUnlockTests` 11 + `AdaptiveImmunityProgressStoreTests` 9 + `ImmuneModeTests` 6 + `GamificationServiceTests` +5). Build green at each merge; 447 tests pass.

**Previous round (2026-06-13 evening, PRs #101 → #105, fifteenth-pass auto-cycle sweep)**: 5-PR sweep driven by the user-direct standing auto-cycle paired with the **fifteenth-pass** Xcode-managed file safety reinforcement + the persisted maximize-ForgeKit-integration + close-FEATURE_PLAN-checkboxes + follow-technical-design-doc + SPM-folder-structure + describe-options-before-starting directive set. The round was the **first multi-agent collision round** in MicrobeLab history: a parallel session worked PR #101 (15th-pass safety) alongside this session's PRs #102 → #105, with both sessions pausing for option-menu pre-approval per the canonical-invariant tier. Each of this session's PRs landed end-to-end + verified MERGED via `gh pr view --json state,mergedAt` before the next branched. **Fifteen restatements across two calendar days extends the all-time portfolio record by one**; per the eleven-pass canonical-invariant tier the cadence has saturated past the canonical-invariant tier — additional restatements add reinforcement frequency but no new binding tier. Rollup:

| PR | Theme |
|---|---|
| #101 | **Fifteenth-pass** Xcode-managed file safety reaffirmation (parallel session, mechanical 14→15 bump across the four canonical bindings). The canonical-invariant tier persists; the fifteenth pass carries forward the user-direct extended verbatim wording ("Instead, file a handoff doc with the user to do Xcode UI work") + the fourteenth-pass companion directive "describe and explain each option before you start" (now structurally part of the auto-cycle pre-approval). |
| #102 | Juice layer closure audit + FEATURE_PLAN promotion `[/]` → `[x]`. New `Docs/AUDIT_JUICE_LAYER_PARTIAL_CLOSURE_2026-06-13.md` walks the visual + audio + haptic trifecta per-axis: visual axis CLOSED via `CelebrationCoordinator` (PR #53); haptic axis CLOSED via `SensoryPaletteCoordinator` (PR #71); audio axis correctly-and-permanently-deferred per `.claude/rules/forgekit.md` § Asset generation ownership (labsmith owns SFX generation portfolio-wide; one-line `AppRootView` wire-up lands inline with the labsmith distribution PR). Mirrors the PR #95 / #96 / #98 audit-driven closure pattern. Pure docs — zero source code, zero managed-file edits. |
| #103 | Welcome-back recap surface — closes the explicit "Recap is left to a follow-up PR" TODO in `WelcomeBackOverlay`'s class-level doc-comment. New `WelcomeBackRecap` (Services/Engagement/, pure nonisolated `Sendable` value type) derives 1-2 previously-met microbe display names from `DiscoveryStore.discoveredSlugs` + the bundled catalog at the moment `loadCatalog` succeeds; threaded through `WelcomeBackOverlay` via a new optional `recap:` parameter (back-compat preserved). Trauma-informed posture: structurally opt-out (returns `nil` when discovery empty so warm greeting still surfaces); deterministic per-`daysAway` (refreshing within a session never shuffles — predictability is calming); NEVER mentions undiscovered microbes; copy stoplist (`missed` / `abandon` / `forgot` / `failed` / `should have` / `behind` / `neglect` / `shame` / `fell short` / `compared` / `better than` / `lost` / `didn't`) pinned by parameterized test across days × counts matrix; salt distinct from `VariableRewardSelector.appSalt` so engagement surfaces decorrelate. 10 new `WelcomeBackRecapTests`. Promotes FEATURE_PLAN.md § Engagement Foundation "Return loop" inline note from "Recap surface deferred to follow-up." → CLOSED. |
| #104 | Adaptive-immunity `@Generable` — closes FEATURE_PLAN.md § Phase 2 "Expand mentor @Generable set with adaptive-immunity hypotheses". New `AdaptiveImmuneHypothesis` `@Generable` (observation + memoryHypothesis property pair) + `AdaptiveImmuneScenario` enum (`.firstEncounter` / `.matchedShape` / `.recallFromMemory`) covering the canonical pedagogy beats. New `VeeMentor.fallbackAdaptiveImmuneHypothesis(for:)` + `VeeMentor.adaptiveImmuneHypothesis(for:)` mirror the existing `EcologyHypothesis` pattern. **Defense-in-depth trauma-informed register**: authored fallback content stoplist (`fight` / `attack` / `destroy` / `kill` / `war` / `enemy` / `battle` / `weapon` / `soldier` / `warrior`) pinned by parameterized test; LLM prompt explicitly forbids the same tokens; hedging-language invariant + shape-matching-register invariant also pinned. 5 new `VeeMentorTests` (21/21 total pass). |
| #105 | `BCellAntibodyMatchScene` skeleton — Phase 2 starter; partially closes FEATURE_PLAN.md § Phase 2 "Implement BCellAntibodyMatchScene (SpriteKit)". 4 new value types in `Models/ImmuneTypes`: `AntibodyShape` enum (4 cases: spiral / ridged / branched / rounded; symmetric `.complement`); `AntigenState` (id + shape + position + velocity + `isMatched`); `BCellState` (position + `loadedAntibody` + `matchRadius`); `MemoryRecord` (shape + recognitionCount — body remembers between waves). New `BCellAntibodyMatchScene` follows `MacrophagePacmanScene` lazy-visual-setup pattern (no `SKShapeNode` construction in `init`; visuals only activate via `configureVisuals()` so SPM unit tests drive logic without GPU). 5-wave Phase-2 default `[3, 4, 5, 6, 7]` mirrors Phase 1 cadence. Scoring: `baseMatchPoints` (2) on first-encounter; `memoryMatchPoints` (4) on memory-aided recognition — pedagogy beat for adaptive immunity. Trauma-informed register: no `fight` / `attack` / `destroy` / `kill` / `war` / `enemy` / `battle` / `weapon` / `soldier` / `warrior` vocabulary anywhere in the API surface. Pairs with the `AdaptiveImmuneScenario` mentor `@Generable` from PR #104. 16 new `BCellAntibodyMatchSceneTests`. UI integration into `ImmuneGameView` deferred to a future round when the rest of Phase 2 (adaptive-unlock-after-innate / 8 question kits / etc.) is in place. |

**Net additions (PRs #102 → #105)**: 2 new Models value types (`AntibodyShape` + `AntigenState` + `BCellState` + `MemoryRecord` — 4 types in `ImmuneTypes.swift`) + 1 new mentor `@Generable` (`AdaptiveImmuneHypothesis` + `AdaptiveImmuneScenario` enum in `MentorGenerables.swift`) + 1 new Services value type (`WelcomeBackRecap` in `Services/Engagement/`) + 1 new SpriteKit scene (`BCellAntibodyMatchScene` in `GameEngine/`) + 2 new mentor accessors on `VeeMentor` + 1 optional `recap:` parameter on `WelcomeBackOverlay` + 1 new `enrichWelcomeBackRecap(catalog:)` helper in `AppRootView` + 1 new closure audit doc (`Docs/AUDIT_JUICE_LAYER_PARTIAL_CLOSURE_2026-06-13.md`) + 31 new unit tests across 3 suites (`WelcomeBackRecapTests` 10 + `VeeMentorTests` +5 → 21 + `BCellAntibodyMatchSceneTests` 16). Build green at each merge.

**Collision-resolution discipline this round** (new pattern; reinforces `.claude/rules/portfolio.md` § parallel-session collision avoidance): the round opened with the parallel session already mid-15th-pass + a fresh `feature/forgekit-events-seasonal-wire-2026-06-13` branch left empty. This session pre-fetched origin + audited each in-flight branch state before branching for its own work, then yielded Item 6 (MicrobeKnowledgeGraph extensions) to the parallel session who was working a role+kingdom variant + preserved 3 working-tree WIP commits via stashes with self-documenting labels (`stash@{0}: parallel-session MicrobeKnowledgeGraphTests for role+kingdom edges`; `stash@{1}: parallel-session MicrobeKnowledgeGraph role+kingdom edges`; `stash@{2}: parallel-session ForgeEvents dep add`) so the parallel session can recover its work without re-implementing. The session also explicitly deferred Items 2 + 3 (SwiftData wiring of `EncounterLog` + `JournalEntry`) per `.claude/rules/swiftdata.md` § Schema versioning — those land in a focused future round.

**Previous round (2026-06-13, PRs #97 → #100, fourteenth-pass auto-cycle sweep)**: 4-PR sweep driven by the user-direct standing auto-cycle paired with the **fourteenth-pass** Xcode-managed file safety reinforcement + the persisted maximize-ForgeKit-integration + close-FEATURE_PLAN-checkboxes + follow-technical-design-doc + SPM-folder-structure directive set + the new "describe and explain each option before you start" companion directive (folded into auto-cycle pre-approval as an opening option-menu step). Each PR landed end-to-end before the next branched. **Fourteen restatements across two calendar days extends the all-time portfolio record by one**; per the eleven-pass canonical-invariant tier the cadence has saturated past the canonical-invariant tier — additional restatements add reinforcement frequency but no new binding tier. Rollup:

| PR | Theme |
|---|---|
| #97 | Fourteenth-pass safety reaffirmation across the four canonical bindings (CLAUDE.md + `.claude/rules/xcode-agent-safety.md` + this prologue + `Docs/FEATURE_PLAN.md` prologue + persistent-memory file). Captures the fourteenth-pass extended verbatim wording ("Instead, file a handoff doc with the user to do Xcode UI work") which makes the canonical escape hatch explicit. NEW companion directive (additive — does NOT extend the rule's exceptions surface): the standing user-direct "describe and explain each option before you start" is now folded into the auto-cycle pre-approval. Zero code, zero managed-file touches. |
| #98 | First 60 Seconds + Aha moment closure audit + FEATURE_PLAN checkbox closure. New `Docs/AUDIT_FIRST_60_SECONDS_AHA_MOMENT_CLOSURE_2026-06-13.md` walks the 5-page `MicrobeLabOnboardingFlow` per-beat, maps each to the canonical reference impl path + line number, confirms the 60s timing budget hits with comfortable headroom at relaxed reading cadence, and verifies trauma-informed posture per-step (germ-myth inversion at step 3 — "Most microbes aren't germs — most are quiet helpers" — + pre-emptive off-ramps at steps 4 + 5). For the aha moment, the audit identifies two complementary surfaces: onboarding step 3 introduces Lacto with the verbatim line from `TECHNICAL_DESIGN.md` § Onboarding & First-Time Experience as the session-1 one-shot, AND `ExploreView`'s `MentorBubble` continues the aha cadence post-onboarding via per-tier mentor-cue refresh. Variable-reward + mentor-recall surfaces extend the long-tail aha. No source edits required for closure; the audit is the canonical artifact. FEATURE_PLAN.md § Onboarding & Child Safety items "First 60 Seconds experience" + "Aha moment design" promoted [ ] → [x]. |
| #99 | UI test scaffolding — closes FEATURE_PLAN.md § Phase 1 Quality items "UI tests for microscope + codex flow" + "UI tests for microbiome puzzle". Extends the existing pbxproj-tracked `Apps/MicrobeLab/MicrobeLabUITests/MicrobeLabUITests.swift` (modifying the tracked file does NOT require pbxproj edits per the fourteenth-pass safety rule). New tests: `testAppLaunchesWithoutCrashing` (baseline `.runningForeground` state), `testFirstLaunchPresentsOnboardingGate` (accepts either `MicrobeLabOnboardingFlow` OR `ParentHandoffFlow.step.*` identifier depending on simulator UserDefaults state), `testTabShellShowsExploreAndCodexAfterOnboarding` (drives past the gate via `advancePastOnboardingIfPresent` helper tapping through "Next" / "Get Started" / "Continue" / "Done" / "Looks good" / "Save" affordances bounded by a 15-iteration cap, then asserts the Explore + Codex tab bar buttons are visible), `testMicrobiomeTabHiddenOnFirstSession` (pins the progressive-disclosure invariant: Microbiome MUST be absent at `sessionCount < 2` per `TabDisclosure.from(sessionCount:)`). Deeper flow coverage waits on launch-argument-driven session-count bypass; the gate-behavior test is itself meaningful coverage because a regression in `TabDisclosure` would silently surface Microbiome too early. |
| #100 | ForgeKit 0.99.0 `ReflectionPromptModifier` integration into the session-summary surface — closes the maximize-ForgeKit-integration directive on the freshest 0.99.0 module + extends the FEATURE_PLAN § Parent Integration → "Session closer" item with a trauma-safe end-of-session reflection affordance. New `Services/Engagement/ReflectionEntryStore` (MainActor `@Observable` UserDefaults-backed, ring-buffered cap 50 with FIFO eviction, JSON-encoded `ForgeModels.ReflectionEntry` rows, corrupt-payload-resilient with fresh-start fallback, `purge(olderThan:)` for the 2026 FTC retention-period requirement). New `AppFeature/Engagement/MicrobeLabReflectionPrompts.sessionClose` (canonical `ReflectionPromptConfig` with one question — "What's one thing the microbes showed you today?" — and `.text` + `.emoji` + `.skip` modalities; voice + drawing deliberately deferred because they'd route through entitlement provisioning per the fourteenth-pass safety rule). `SessionSummarySheet` gains an opt-in `onAddReflection: (() -> Void)?` parameter; when non-nil the kid sees an "Add a reflection" secondary button below the canonical "See you next time" dismiss. Wired into both call sites (the auto-surfaced session-summary on `AppRootView` background-resume + the kid-initiated session-summary on `ProgressTabView.Wrap up today`); the sheets sequence rather than stack (summary dismisses → 350ms delay → reflection presents) to avoid SwiftUI's flaky sheets-on-sheets behavior. Trauma-informed: prompt copy frames reflection as warm noticing rather than performance review; the `.skip` off-ramp is precondition-enforced by `ReflectionPromptConfig.init`. Privacy: `parentVisible: false` for v1 per the kid-private journal posture. 10 new `ReflectionEntryStoreTests` pin all behaviors. **ForgeKit 0.99.0 surface now actively consumed**: `ReflectionPromptModifier` + `ReflectionPromptSheet` from `ForgeUI` + `ReflectionEntry` + `ReflectionResponseModality` from `ForgeModels`. |

Net additions: 1 new Services module (`ReflectionEntryStore` under `Services/Engagement/`) + 1 new AppFeature module (`MicrobeLabReflectionPrompts` under `AppFeature/Engagement/`) + 1 SessionSummarySheet optional parameter + 4 new UI smoke tests in the existing pbxproj-tracked UI test file + 10 new unit tests in 1 new suite + 1 new doc (`Docs/AUDIT_FIRST_60_SECONDS_AHA_MOMENT_CLOSURE_2026-06-13.md`). Build green at each merge; 385 tests pass.

**Previous round (2026-06-13, PR #96, thirteenth-pass auto-cycle sweep — A11y formal closure)**: 1-PR doc closure. Formal `[ ]` → `[x]` promotion on FEATURE_PLAN.md § Accessibility & Trauma-Informed Polish → "Dynamic Type support" + "Color-contrast audit". Both are PASS portfolio-wide per `Docs/AUDIT_ACCESSIBILITY_PASS_2026-06-12.md` § Per-surface state (every shipped surface uses semantic font tokens; lowest contrast = `Color.primary.opacity(0.08)` solid fallback ≥ 4.5:1; every tinted glass surface honors `accessibilityReduceTransparency` via `A11yPreferences`). New surfaces shipped post-audit (PR #94 `ChapterReaderSheet`) continue the same semantic-font + WCAG-AA pattern. No source edits required; the audit is the canonical artifact. The "VoiceOver labels for every microscope LOD sprite + cast portrait" remaining item stays asset-blocked on the labsmith portrait pack distribution; the SpriteView top-level VoiceOver labels (the audit's GAP 1) already closed PR #69.

**Previous round (2026-06-13, PR #95, thirteenth-pass auto-cycle sweep — Micro-delight formal closure)**: 1-PR doc closure. Formal `[/]` → `[x]` promotion on FEATURE_PLAN.md § Delight & Polish → "Micro-delight coverage". All 8 axes are now wired through prior PRs (#53 celebration / EasterEgg surprise / #66 personality / #76 + #79 + #86 mastery / #75 + #82 social / #71 sensory / #59 agency / #86 + #94 discovery). The FEATURE_PLAN item carries the per-axis wiring table so future sessions don't need to reconstruct what's shipped where.

**Previous round (2026-06-13, PR #94, thirteenth-pass auto-cycle sweep — Chapter content)**: 1-PR sweep — DN-S chapter content surface lands. The user-direct standing auto-cycle paired with the thirteenth-pass safety reinforcement opens this round; per the eleven-pass canonical-invariant tier the cadence has saturated, so the safety reinforcement adds frequency but no new binding tier. Rollup:

| PR | Theme |
|---|---|
| #94 | ForgeIllustrations chapter content wire-up + thirteenth-pass safety reaffirmation across the four canonical bindings (CLAUDE.md + this prologue + FEATURE_PLAN.md prologue + memory file). Adds `ForgeIllustrations` to `AppFeature` dependencies in `Packages/Libraries/Package.swift`. Bundles the 6 canonical DN-S chapter MDs (Lacto / Yeast / Photo / Net / Spore / Guard) under `Services/Resources/` (SPM's `.process("Resources")` flattens unknown file types to bundle root; the loader uses per-slug `Bundle.module.url(forResource:withExtension:)` instead of subdirectory crawl). New `Models/MicrobeChapter.swift` value type (nonisolated Sendable, Codable, Identifiable; carries slug + title + body + estimatedReadingMinutes derived at 200wpm middle-grade rate). New `Services/MicrobeChapterStore.swift` (nonisolated Sendable; pure parser strips YAML front-matter + captures first H1 as title + counts whitespace-separated words for reading-minutes; exposes `MicrobeChapterStore.resourcesBundle: Bundle` static so AppFeature can pass it to `ForgeIllustrations.ChapterPortraitView` without needing direct access to Services' internal `Bundle.module`). New `AppFeature/Engagement/ChapterReaderSheet.swift` renders the chapter via `ChapterPortraitView` opener variant (gracefully renders `Color.clear` when the WebP asset isn't bundled, so the surface is correct today + auto-upgrades when assets ship) + title + reading-minutes chip + markdown body in a kid-readable 640pt reading column. `MicrobeCodexView` extended: card tap on an already-discovered microbe with a bundled chapter presents `ChapterReaderSheet`; `MicrobeCodexCard` gains `hasChapter: Bool` parameter so discovered cards with stories show a small `book.closed.fill` glyph + accessibility hint "Tap to read this microbe's story". Trauma-informed: opt-in (gated on discovery), system sheet swipe-down + explicit Done toolbar button are the off-ramps, no chapter is force-opened. 11 new `MicrobeChapterStoreTests` cover bundled chapters per-slug presence / codex-supporting cast returns nil / Lacto title parse / front-matter stripping / empty-body rejection / 200wpm reading-minutes derivation / default title fallback / availableSlugs / injected fixtures / resource-bundle exposure / minimum-1-minute floor. Build green; 375 tests pass. **ForgeIllustrations promoted from declared-but-unused → actively consumed**; the portrait-pack-blocked `IllustrationRegistry` full registration item in FEATURE_PLAN.md moves from `[ ]` → `[/]` (chapter axis shipped; portrait + per-chapter WebP axes still asset-blocked). |

Net additions: 1 new Models value type (`MicrobeChapter`) + 1 new Services module (`MicrobeChapterStore` at the Services target root + the bundled chapter MD resources under `Services/Resources/`) + 1 new AppFeature view (`ChapterReaderSheet` under `AppFeature/Engagement/`) + 1 new SharedUI parameter (`MicrobeCodexCard.hasChapter`) + 11 new unit tests in 1 suite + 1 new ForgeKit module declared in AppFeature deps (`ForgeIllustrations`). Build green.

**Previous round (2026-06-13, PRs #90 → #92, twelfth-pass auto-cycle sweep)**: 3-PR sweep driven by the user-direct standing auto-cycle (`branch → commit → push → gh pr create → gh pr merge → verify`) paired with the **twelfth-pass** Xcode-managed file safety reinforcement + the persisted maximize-ForgeKit-integration + close-FEATURE_PLAN-checkboxes + follow-technical-design-doc + SPM-folder-structure directive set. Each PR landed end-to-end before the next branched. **Twelve restatements in one calendar day extends the all-time portfolio record by one** but per the eleven-pass invariant, the cadence has saturated past the canonical-invariant tier — additional restatements add reinforcement frequency but no new binding tier. The rule continues to live across the four canonical bindings (CLAUDE.md + `.claude/rules/xcode-agent-safety.md` + this prologue + the persistent-memory file `feedback_xcode_managed_files.md`). Rollup:

| PR | Theme |
|---|---|
| #90 | Parental consent service + per-feature consent manager — closes FEATURE_PLAN.md § Onboarding & Child Safety items "Parental consent service" + "Parental gates" with one PR (the consent service IS the canonical gate-side check; `hasValidConsent(for:)` is the call every external-link / data-sharing surface MUST guard on). Ships `ParentalConsentService` (Services/Engagement/, MainActor `@Observable` UserDefaults-backed) persisting `ParentalConsentRecord` rows with a 365-day expiry per the 2026 FTC COPPA Rule § Data retention limits. `ParentalConsentKind` enum carries 4 distinct feature classes — `.diseaseStoryArcs` / `.weeklySummaryNotifications` / `.externalLinks` / `.classroomMode` — per the FTC rule's "separate verifiable parental consent required per data-sharing class". `ParentalConsentManagerView` (AppFeature/Settings/) surfaces one Toggle row per kind + a "Needs reconfirm" section (one-tap annual refresh). Wired into SettingsView's "For parents" section (parental-gate-guarded). Trauma-informed: no "DENIED" record — only GRANTED with expiry + REVOKED (deleted). Stoplist-pinned by test. 11 new `ParentalConsentServiceTests`. |
| #91 | Weekly summary opt-in local notification — closes FEATURE_PLAN.md § Parent Integration → "Weekly summary" item. `WeeklySummaryService` (Services/Engagement/, MainActor `@Observable`) wraps a `UserNotificationScheduler` adapter protocol. Production-side `SystemNotificationScheduler` calls `UNUserNotificationCenter.current()`; tests inject a recording fake. `WeeklySummaryNotificationContent.compose(from:)` is the pure `nonisolated` body composer. Default trigger = Saturday 9am via `WeeklySummaryTrigger`. AppSettings gains `weeklySummaryNotificationEnabled: Bool` (defaults OFF per FTC 2026 opt-in rule); custom decoder fallback chain preserves prior persisted state. SettingsView "For parents" Toggle gates on (a) parental-gate adult confirm + (b) `.weeklySummaryNotifications` consent grant via PR #90's service + (c) `UNUserNotificationCenter` authorization. System denial rolls back the toggle. `AppRootView.task` re-schedules with the freshest snapshot every cold launch when the toggle is on. Trauma-informed quiet-week framing ("A quiet week — that's allowed") + active-week composition + cross-snapshot stoplist all pinned by parameterized test. Local-only — no push token, no cloud, no third-party SDK. 12 new `WeeklySummaryServiceTests`. |
| #92 | Performance budget bench harness — closes the algorithmic-regression axis of FEATURE_PLAN.md § Phase 1 → Quality "Performance profiling". The `PerfSignpost` probes shipped PR #56 expose Instruments-side live timing; this PR adds the assertion gate so a future change that 10×'s a per-tick cost surfaces in CI without needing a real-device capture. 6 `@Test` cases under `Tests/GameEngineTests/PerformanceBudgetTests.swift` drive `MicrobiomeSimulator.tick` (+ jitter overload), `ZoomMachine.applyPinch/snap`, `MacrophagePacmanScene.advancePathogens/moveMacrophage` over N=200 iterations using `ContinuousClock.measure`. Budgets are 4-8× headroom over the canonical 8ms (simulator) / 16ms (zoom) / 8ms (immune) targets so noisy CI doesn't flake. Steady-state on a Mac runner lands every surface under 0.1ms per iteration. Explicit non-goal: real-device 60fps frame budget + SwiftUI body re-evaluation + SpriteKit physics still want a real-device Instruments trace. |

Net additions: 1 new Services module (`ParentalConsentService` + `WeeklySummaryService` — both under `Services/Engagement/` per the portfolio SPM folder convention) + 2 new AppFeature views (`ParentalConsentManagerView` + the weekly-summary toggle row in `SettingsView`) + 1 new AppSettings field (`weeklySummaryNotificationEnabled`) + 1 new test target file (`PerformanceBudgetTests` flat under `Tests/GameEngineTests/`) + 29 new unit tests across 3 suites (`ParentalConsentServiceTests` 11 + `WeeklySummaryServiceTests` 12 + `PerformanceBudgetTests` 6). Build green at each merge.

**Previous round (2026-06-12 → 2026-06-13, PRs #85 → #88, eleventh-pass auto-cycle sweep)**: 4-PR sweep driven by the user-direct standing auto-cycle (`branch → commit → push → gh pr create → gh pr merge → verify`) paired with the **eleventh-pass** Xcode-managed file safety reinforcement + the persisted maximize-ForgeKit-integration + close-FEATURE_PLAN-checkboxes + follow-technical-design-doc + SPM-folder-structure directive set. Each PR landed end-to-end before the next branched. **Eleven restatements in one calendar day is now the all-time portfolio record**, surpassing the ten-pass cadence codified earlier the same day. The eleventh pass crosses the rule past the doctrinal tier (10th pass) into the **canonical-invariant tier** — a class-level invariant of the in-IDE agent across the portfolio. Beyond the eleven-pass tier the cadence has saturated. Rollup:

| PR | Theme |
|---|---|
| #85 | **Eleventh-pass** Xcode-managed file safety reinforcement — codifies the **eleven-pass invariant** (supersedes the ten-pass invariant). At this tier the rule has crossed past the doctrinal tier into a canonical-invariant tier — a class-level invariant of the in-IDE agent across the portfolio. Beyond the 11-pass tier the cadence has saturated (12th-, 13th-pass restatements add reinforcement frequency but no new binding tier). Quadruple-binding reaffirmed (CLAUDE.md + `.claude/rules/xcode-agent-safety.md` + round-doc prologues + persistent-memory file) annotated with the canonical-invariant framing. Companion directives (auto-cycle / max-ForgeKit / FEATURE_PLAN closure / technical-design-doc / SPM-folder-structure / persistent-memory) explicitly do NOT extend the rule's exceptions surface. |
| #86 | Codex mastery axis — wires the third + final `MasteryMomentDetector` axis. Ships `DiscoveryStore` (Services/Engagement/, MainActor `@Observable` UserDefaults-backed persistence of microbe slugs the kid has met; pure-additive). `MicrobeCodexView` reads / writes via the store + runs `MasteryMomentDetector.recordCodexDiscovery` after every tap; the canonical trifecta fires at 12/12 (`.epic` celebration via `daily-complete` Lottie + `.streakMilestone(12)` haptic + trauma-informed mentor subline "You met every microbe in the catalog. The whole cast knows you now."). `ExploreView`'s rare-microbe sighting + curious-explorer easter-egg cues ALSO mark the quoted microbe discovered so microscope-side meets sync to the codex. `AppRootView` instantiates + threads `discovery` through both consumer views; SessionSummary's `microbesDiscovered` now reads the live delta over the session span. 8 new `DiscoveryStoreTests` pin: empty / idempotent / multi-slug / cross-instance persistence / clear / canonical 12-cast completion. **MasteryMomentDetector is now wired into all 3 of 3 consumer surfaces** (ecology via MicrobiomeView PR #76, defense via ImmuneGameView PR #79, codex via MicrobeCodexView PR #86). |
| #87 | ForgeNavigation wire-up — promotes the last declared-but-unused ForgeKit module to actively consumed. `MicrobeLabPhase` (`nonisolated public enum: ForgeNavigation.AppPhase, Sendable`, AppFeature/Onboarding/) mirrors the AppRootView conditional-rendering Group block 1:1 with 5 cases: `.parentHandoff` / `.kidOnboarding` / `.loadingCatalog` / `.tabShell` / `.catalogFailure(message:)`. Each case carries the canonical `slug`, `layoutStrategy`, `displayName`, `systemImage`. `MicrobeLabPhase.resolve(...)` static helper computes the phase from store state — pure value type, testable without view body. `MicrobeLabStartupGateID` (`String, Sendable, CaseIterable` enum) carries canonical gate IDs for future `ForgeNavigation.StartupGate` consumers. AppRootView gains a `currentPhase: MicrobeLabPhase` computed property + `lastObservedPhase: MicrobeLabPhase?` `@State` + `.onChange(of: currentPhase)` observer that emits one `DebugLog.lifecycle` line + one `AnalyticsService.track(.appPhaseReached(phaseSlug:))` event per change. New `MicrobeLabAnalyticsEvent.appPhaseReached(phaseSlug: String)` case carries the canonical phase slug in its PII-safe property bag. 2 new `AnalyticsServiceTests`. **Zero remaining declared-but-unused ForgeKit modules in `Package.swift`.** |
| #88 | Per-question attempt log persistence — closes FEATURE_PLAN.md § Parent Integration → "Progress dashboard" per-standard proficiency follow-up. Ships `QuestionAttemptStore` (Services/Engagement/, MainActor `@Observable` UserDefaults-backed log of `QuestionAttempt` rows — questionID + kitSlug + curriculumStandard + wasCorrect + answeredAt). PII-safe per `.claude/rules/age-assurance.md` § Portfolio Status (UUIDs + slugs + booleans + timestamps only). JSON-encoded under a single key with FIFO eviction at `defaultMaxAttempts = 500` (~120 bytes/record stays well under the 100KB UserDefaults sweet spot). `proficiencies(matching:)` derivation builds `[ForgeReporting.StandardProficiency]` from the log + scopes to standards the kid has actually attempted. `ProgressReportSnapshot.standardProficiencies` carries the proficiencies through the existing 3-layer plumbing without per-layer threading; `ProgressReportService.reportData(for:)` now derives `averageScore` from the proficiency mean. `QuizView` writes attempt rows at the `.reveal(against:)` boundary; threading happens through `AppRootView` → `MicrobeCodexView` → `QuizView`. 9 new `QuestionAttemptStoreTests` cover empty / append / cross-instance persistence / FIFO eviction / proficiency scoping / fresh-install / untagged-question-skip-proficiency / clear / key + cap stability / averageScore wiring through ProgressReportService. Existing `ProgressReportServiceTests` still pass — the snapshot's new field defaults to `[]`. |

Net additions: 3 new Services modules (`DiscoveryStore` + `QuestionAttemptStore` + canonical `MicrobeLabPhase` in AppFeature/Onboarding/) + 1 new `MicrobeLabAnalyticsEvent` case (`.appPhaseReached`) + 19 new unit tests across 3 suites (`DiscoveryStoreTests` 8 + `AnalyticsServiceTests` +2 cases + `QuestionAttemptStoreTests` 9) + 1 ForgeKit module promoted from declared-but-unused → actively consumed (`ForgeNavigation` → AppFeature) — closes the last remaining unused-module gap. Build green at each merge.

**Previous round (2026-06-12, PRs #81 → #83, tenth-pass auto-cycle sweep)**: 3-PR sweep driven by the user-direct standing auto-cycle (`branch → commit → push → gh pr create → gh pr merge → verify`) paired with the **tenth-pass** Xcode-managed file safety reinforcement + the persisted maximize-ForgeKit-integration + close-FEATURE_PLAN-checkboxes + follow-technical-design-doc + SPM-folder-structure directive set. Each PR landed end-to-end before the next branched. **Ten restatements in one calendar day is now the all-time portfolio record for any single rule**, surpassing the nine-pass cadence codified earlier the same day which itself surpassed the eight-pass cadence codified earlier the same day which itself surpassed the seven-pass cadence codified earlier the same day. The tenth pass crosses the rule past the load-bearing-identity-of-the-agent tier (9th pass) into the **doctrinal tier** — a permanent constitutional axiom of the codebase rather than a property of any particular session or agent instance. The rule lives in FOUR canonical locations (CLAUDE.md + `.claude/rules/xcode-agent-safety.md` + this prologue + the persistent-memory file `feedback_xcode_managed_files.md`) — any one of them can drift or decay without the rule being lost. Rollup:

| PR | Theme |
|---|---|
| #81 | **Tenth-pass** Xcode-managed file safety reinforcement — codifies the **ten-pass invariant** (supersedes the nine-pass invariant). The rule has crossed past the load-bearing-identity-of-the-agent tier into a doctrinal tier — a permanent constitutional axiom of the codebase rather than a property of any particular session or agent instance. Four clauses: (1) doctrinal persistence — the rule + scope table are a permanent property of the repository, not of any single session, cadence window, or agent instance; (2) quadruple-binding cross-reference — the rule lives in FOUR canonical locations (CLAUDE.md + `.claude/rules/xcode-agent-safety.md` + round-doc prologues + persistent-memory file) so any one of them can drift or decay without the rule being lost; (3) ten-pass companion directives explicitly do NOT extend the rule's exceptions surface — adding a new directive to the auto-cycle prompt template does NOT carve out a new exception; (4) before any `Edit` / `Write` tool call against a path matching the "DO NOT WRITE" glob table, the agent STOPS — pre-flight check is a constitutional invariant + structurally redundant via the quadruple binding. CLAUDE.md + `.claude/rules/xcode-agent-safety.md` + `Docs/FEATURE_PLAN.md` prologue + `Docs/IMPLEMENTATION_HANDOFF.md` prologue + the memory file all carry the tenth-pass framing. |
| #82 | Immune defense run trophy — closes the immune axis of FEATURE_PLAN.md § Delight & Polish "Share-worthy moments" (codex axis shipped PR #75 + this PR fills the second axis). Mirrors `CodexCertificate` pattern verbatim: `ImmuneDefenseTrophy` (Services/Engagement/, pure nonisolated `Sendable` value type) snapshots the kid's macrophage-minigame run; headline scales warmly (`Defense Cadet` → `Defense Apprentice` → `Defense Specialist` → `Defense Champion` → `Defense Master`) with the top tier (`Defense Master`) gated on the same perfect-run criteria as `MasteryMomentDetector.recordDefenseRunComplete` (all waves cleared AND zero pathogens missed). `ImmuneDefenseTrophyView` + `ImmuneDefenseTrophySheet` (AppFeature/Engagement/) mirror the codex card layout + `ImageRenderer` → `ShareLink` PNG export pipeline. `ImmuneGameView` gains a "Share trophy" icon button in the score HUD that becomes available the moment the run reaches `isComplete`; the trophy snapshot is frozen at the moment the kid taps share + a `playerDisplayName` parameter (defaults "Explorer") threads down for the trophy display name. Trauma-informed: never compares score to anyone else's, never frames a partial clear as failure; stoplist (`missed` / `failure` / `behind` / `should` / `must` / `almost` / `fell short` / `compared` / `better than` / `lost`) pinned by parameterized test across the full `waves × perfect` matrix with a single literal-allowance carve-out for the perfect-master `"zero pathogens missed"` positive-framing subline. 10 new `ImmuneDefenseTrophyTests`. Now **both share-worthy axes are CLOSED** — `[x]` in FEATURE_PLAN.md. |
| #83 | App Intents wire-up — Apple's `AppIntents` framework natively wired for Siri / Spotlight / Shortcuts entry points. Three intents in AppFeature/Intents/: `OpenMicroscopeIntent` / `OpenCodexIntent` / `OpenMicrobiomeIntent`. Each conforms to `AppIntent` with `openAppWhenRun = true` + `LocalizedStringResource` titles + kid-friendly `IntentDescription`s. `perform()` returns `some IntentResult & OpensIntent` + calls `NavigationCoordinator.shared.requestTab(_:)`. `MicrobeLabAppShortcuts` conforms to `AppShortcutsProvider` with 6 Siri-callable phrases (2 per intent) + SF Symbol icons. New `Models/MicrobeLabTab.swift` (nonisolated public enum carrying 5 tab slugs: explore / codex / microbiome / progress / profile — stable raw values matter for App Intents + analytics) + new `Services/NavigationCoordinator.swift` (`@MainActor @Observable` singleton brokering App Intent → tab-switch requests). AppRootView changes: `@State selectedTab: MicrobeLabTab = .explore` + `TabView(selection: $selectedTab)` binding + `value: MicrobeLabTab.xxx` per Tab + `.onChange(of: navigationCoordinator.requestedTab)` observer applies the request iff the target tab is currently visible in the progressive-disclosure shell (`isTabAvailable` check), then clears the request so the same intent can re-fire later. Trauma-informed: NO special-cased "intent vs tap" logic — the existing per-tab observers fire because the tab change reaches the same code paths a manual tap would. **No Info.plist or entitlement edits required** (per the ten-pass invariant) — `AppShortcutsProvider` auto-discovery uses runtime metadata. 9 new `NavigationCoordinatorTests`. |

Net additions: 1 new Models value type (`MicrobeLabTab`) + 2 new Services modules (`ImmuneDefenseTrophy` under `Services/Engagement/` per the portfolio SPM folder convention + `NavigationCoordinator` at Services root) + 1 new AppFeature subdir (`Intents/` containing `MicrobeLabIntents` + `MicrobeLabAppShortcuts`) + 2 new AppFeature views (`ImmuneDefenseTrophyView` + `ImmuneDefenseTrophySheet` under `Engagement/`) + 19 new unit tests across 2 suites (`ImmuneDefenseTrophyTests` 10 + `NavigationCoordinatorTests` 9) + 1 new natively-wired Apple system framework (`AppIntents`). Build green at each merge.

**Previous round (2026-06-12, PRs #78 → #79, ninth-pass auto-cycle sweep)**: 2-PR sweep driven by the user-direct standing auto-cycle (`branch → commit → push → gh pr create → gh pr merge → verify`) paired with the **ninth-pass** Xcode-managed file safety reinforcement + the persisted maximize-ForgeKit-integration + close-FEATURE_PLAN-checkboxes + follow-technical-design-doc + SPM-folder-structure directive set. Each PR landed end-to-end before the next branched. **Nine restatements in one calendar day is now the all-time portfolio record for any single rule**, surpassing the eight-pass cadence codified earlier the same day which itself surpassed the seven-pass cadence codified earlier the same day. The ninth pass graduates the rule into **load-bearing identity-of-the-agent tier** — it is no longer merely a thing the agent inherits, it is a thing that defines what "being the MicrobeLab in-IDE agent" means. Rollup:

| PR | Theme |
|---|---|
| #78 | **Ninth-pass** Xcode-managed file safety reinforcement — codifies the **nine-pass invariant** (supersedes the eight-pass invariant). The rule has graduated past the structural-property tier into a load-bearing identity-of-the-agent tier. Four clauses: (1) identity-tier persistence — rule + scope table are a constitutional safety invariant that does not weaken across cadence windows, session resets, or new companion-directive clusters; (2) persistent-memory identity binding — `feedback_xcode_managed_files.md` mirrors the rule + the nine-pass invariant + the round-document re-affirmation discipline as a constitutional check, not a "carry it forward if convenient" preference; (3) nine-pass companion directives explicitly do NOT extend the rule's exceptions surface — adding a new directive to the auto-cycle prompt template does NOT carve out a new exception; (4) before any `Edit` / `Write` tool call against a path matching the "DO NOT WRITE" glob table, the agent STOPS — pre-flight check is a constitutional invariant. CLAUDE.md + `.claude/rules/xcode-agent-safety.md` + `Docs/FEATURE_PLAN.md` prologue + `Docs/IMPLEMENTATION_HANDOFF.md` prologue + the memory file all carry the ninth-pass framing. |
| #79 | Defense mastery moment — `ImmuneGameView` axis wired. Closes (partial → defense axis) FEATURE_PLAN.md § Delight & Polish "Mastery moments — Distinct screen ripple + chord when child internalizes microbiome ecology". PR #76 shipped the ecology axis through `MicrobiomeView`; this PR closes the defense axis through `ImmuneGameView`. `MasteryMomentDetector` now wired into 2 of 3 surfaces (ecology + defense; codex axis remains follow-up). `ImmuneGameView` gains `@State private var masteryDetector = MasteryMomentDetector()` + `evaluateDefenseMastery()` mirroring `MicrobiomeView.evaluateEcologyMastery()`: called from `surfaceWaveClearCue(finished:)` in the `finished == true` branch. On a non-nil `Moment` the canonical trifecta fires: `CelebrationCoordinator.personalBest(metric:value:)` (.epic ripple via `daily-complete` Lottie) + mentor bubble inherits the trauma-informed subline ("Five waves clear, zero pathogens missed. Your macrophages know the rhythm.") + `SensoryPaletteCoordinator.streakMilestone(scene.wave)` distinct haptic + analytics emits `achievement_earned` with the moment.kind.rawValue slug. Trifecta layers over the existing wave-clear celebration (`game-complete` Lottie + `.challengeComplete` haptic + `.immuneRunCompleted` analytics) at a different Lottie slug + different haptic cue so the kid feels the moment without screen-flicker. Trauma-informed copy stoplist already pinned by the detector's parameterized test from PR #76. |

Net additions: 1 ForgeKit-consuming view newly threaded through `Services.MasteryMomentDetector` (`ImmuneGameView`) — the detector is now wired into 2 of 3 consumer surfaces. Build green at each merge.

**Previous round (2026-06-12, PRs #73 → #76, eighth-pass auto-cycle sweep)**: 4-PR sweep driven by the user-direct standing auto-cycle (`branch → commit → push → gh pr create → gh pr merge → verify`) paired with the **eighth-pass** Xcode-managed file safety reinforcement + the persisted maximize-ForgeKit-integration + close-FEATURE_PLAN-checkboxes + follow-technical-design-doc + SPM-folder-structure directive set. Each PR landed end-to-end before the next branched. **Eight restatements in one calendar day is the all-time portfolio record for any single rule**, surpassing the seven-pass cadence codified earlier the same day. Rollup:

| PR | Theme |
|---|---|
| #73 | **Eighth-pass** Xcode-managed file safety reinforcement — codifies the **eight-pass invariant** (supersedes the seven-pass invariant). The rule is no longer merely an immutable pre-flight check; it is a structural property of every artifact the agent emits in the cadence window AND of every artifact future sessions inherit from the cadence window. Four clauses: (1) round-document re-affirmation persists indefinitely until the user explicitly resets the cadence, (2) persistent-memory cross-reference — `feedback_xcode_managed_files.md` mirrors the rule + the round-document re-affirmation discipline, (3) eight-pass companion directives do NOT extend the rule's exceptions surface, (4) before any `Edit` / `Write` tool call against a path matching the "DO NOT WRITE" glob table, the agent STOPS. |
| #74 | ForgeAdventure wire-up — `MicrobeLabHubContribution` Level 2 Swift overlay. Promotes `ForgeAdventure` from declared-but-unused to actively consumed. Ships `MicrobeLabHubContribution` (`nonisolated public struct` conforming to `ForgeAdventure.HubContribution` with themeAccent `#33CCBB`, mentor persona Cilia, kitResources pointing at the 4 Phase-1 question kits, trauma-informed engineCopy for `.simulation` / `.defense` / `.quest`), `MicrobeLabHubRegistrar.register(into:)` async registrar helper, `MicrobeLabHubChallengeAdapter` SwiftUI adapter, and `MentorPersona.microbeLabCilia` (`nonisolated public static let` extension on MentorPersona with Cilia's canonical system-prompt header). Targets `ZoneID.scienceLabs` until labsmith ships the canonical `lifeZone` case — cross-repo handoff filed at `Docs/HANDOFF_FROM_APP_FORGEADVENTURE_LIFE_ZONE_PROPOSAL.md`. New `AppFeature/HubContribution/` subdir per SPM folder convention. Closes (partial) FEATURE_PLAN.md § Adventure Mode Level 2 Swift overlay; `[/]` partial on "Register mode-cards in AdventureView" (registrar ready; AdventureHub-side integration closes the item). |
| #75 | Codex completion certificate — Share-worthy moments axis. Closes (partial) FEATURE_PLAN.md § Delight & Polish "Share-worthy moments" on codex axis (immune-trophy axis still pending). Ships `CodexCertificate` (Services/Engagement/, pure nonisolated `Sendable` value type) snapshotting the kid's discovery progress with warm tiered headline (Microbe Explorer Pass → Microbe Field Notebook → Microbe Naturalist → Microbe Scientist → Codex Complete!), `CodexCertificateView` (AppFeature/Engagement/) self-contained SwiftUI card driving both in-app preview AND the rendered PNG share image, `CodexCertificateSheet` with `ImageRenderer` → `ShareLink` integration shipping the PNG through the system share sheet. `ProgressTabView` gains a secondary-action toolbar "Share my codex" button. Trauma-informed: zero-discovery framed as agency not absence; copy stoplist (`missed` / `failure` / `behind` / `should` / `must` / `haven't yet` / `fell short`) pinned by parameterized test across count `0...12`. 8 new `CodexCertificateTests`. |
| #76 | Mastery moments detector — ecology axis wired into MicrobiomeView. Closes (partial) FEATURE_PLAN.md § Delight & Polish "Mastery moments — Distinct screen ripple + chord when child internalizes microbiome ecology" on ecology axis. Ships `MasteryMomentDetector` (Services/Engagement/, pure nonisolated `Sendable` value type) tracking per-session mastery thresholds for `.ecologyMaster` (≥ 15 consecutive stable ticks under fiber feeding) / `.defenseMaster` (perfect 5-wave immune run) / `.codexMaster` (12/12 microbes) with `acknowledged: Set<MasteryKind>` so each kind fires once per session. `MicrobiomeView` carries the detector as `@State`; on a returned `Moment` it fires `CelebrationCoordinator.personalBest(metric:value:)` (`.epic` tier ripple + chord via `daily-complete` Lottie) + carries the moment's subline into the mentor bubble + fires `SensoryPaletteCoordinator.streakMilestone(threshold)` for distinct haptic vs the routine `.achievement` cue. Trauma-informed: copy stoplist (`finally` / `at last` / `you almost` / `failed` / `behind` / `should have` / `compared to` / `better than`) pinned by parameterized test across all 3 moment kinds. 12 new `MasteryMomentDetectorTests`. Defense + codex axes ship the detector surface ready for follow-up wiring into `ImmuneGameView` + `MicrobeCodexView`. |

Net additions: 2 new Services modules (`CodexCertificate` + `MasteryMomentDetector`, both under `Services/Engagement/` per the portfolio SPM folder convention) + 1 new AppFeature subdir (`HubContribution/` containing `MicrobeLabHubContribution` + `MicrobeLabHubRegistrar`) + 2 new AppFeature views (`CodexCertificateView` + `CodexCertificateSheet` under `Engagement/`) + 20 new unit tests across 2 suites (`CodexCertificateTests` 8 + `MasteryMomentDetectorTests` 12) + 1 cross-repo handoff to labsmith (`HANDOFF_FROM_APP_FORGEADVENTURE_LIFE_ZONE_PROPOSAL.md`) + 1 ForgeKit module promoted from declared-but-unused → actively consumed (`ForgeAdventure` → AppFeature). Build green at each merge.

**Previous round (2026-06-12, PRs #68 → #71, seventh-pass auto-cycle sweep)**: 4-PR sweep driven by the user-direct standing auto-cycle (`branch → commit → push → gh pr create → gh pr merge → verify`) paired with the **seventh-pass** Xcode-managed file safety reinforcement + the persisted maximize-ForgeKit-integration + close-FEATURE_PLAN-checkboxes + follow-technical-design-doc + SPM-folder-structure directive set. Each PR landed end-to-end before the next branched. Seven restatements in one calendar day is the all-time portfolio record for any single rule. Rollup:

| PR | Theme |
|---|---|
| #68 | **Seventh-pass** Xcode-managed file safety reinforcement — codifies the **seven-pass invariant** (supersedes the six-pass invariant). New rule: when the same safety rule has been restated seven times in a single calendar day, the agent re-affirms the rule (verbatim user-direct quote + scope table reference) at the top of every round-document it emits (PR descriptions, every new `HANDOFF_TO_USER_<TOPIC>.md`, `Docs/IMPLEMENTATION_HANDOFF.md` rollup top, `Docs/FEATURE_PLAN.md` prologue). Markdown decay risk is now structurally mitigated by per-round re-affirmation rather than relying on a future session reading CLAUDE.md first. |
| #69 | SpriteView VoiceOver labels — closes the high-priority GAP 1 from `Docs/AUDIT_ACCESSIBILITY_PASS_2026-06-12.md` § Findings. `ExploreView` / `MicrobiomeView` / `ImmuneGameView` each carry a top-level `.accessibilityElement(children: .contain) + .accessibilityLabel(...) + .accessibilityValue(...)` envelope so VoiceOver users land on a named canvas + hear dynamic state on each state change (tier snap / feeding-mode change + tick / wave + score + pathogens-remaining). FEATURE_PLAN § Quality "Accessibility audit" GAP 1 follow-up closed. |
| #70 | ForgeReporting wire-up — parent-facing standards-mapped progress report (engagement skeleton). `ProgressReportService` (Services/Engagement/, pure nonisolated value type wrapping `ForgeReporting.ForgeReportGenerator`) + `ProgressReportView` (AppFeature/Settings/) under a new "For parents" section gated behind the existing parental-gate math gate. Plumbing: `AppRootView` builds a `ProgressReportSnapshot` from live engagement signals + threads through `ProfileView(progressReportSnapshot:)` → `SettingsView(progressReportSnapshot:)`. Per-standard proficiency (strengths / growth areas) follows when per-question attempt logs land; today's surface is the engagement skeleton. 7 new `ProgressReportServiceTests`. Closes (partial) FEATURE_PLAN § Parent Integration → "Progress dashboard". |
| #71 | ForgeSensory wire-up — juice layer (haptic + audio-ready axis). `SensoryPaletteCoordinator` (Services/Engagement/, MainActor `@Observable`) wraps `ForgeSensory.SensoryPalette`. `AppRootView` instantiates one coordinator + threads through MicrobeCodexView → QuizView and MicrobiomeView → ImmuneGameView. Per-event wiring: QuizView fires `.correctAnswer` / `.incorrectAnswer` / `.achievement` / `.challengeComplete`; MicrobiomeView fires `.achievement`; ImmuneGameView fires `.streakMilestone(wave)` / `.challengeComplete` / `.achievement`. Audio dispatch defaults nil until the SFX pack lands per `.claude/rules/forgekit.md` § Asset generation ownership; haptic axis fires immediately via `ForgeHapticEngine.shared`. Trauma-informed: incorrect haptic is the canonical soft tap, never punitive. 6 new `SensoryPaletteCoordinatorTests`. Closes (partial — visual + haptic axes) FEATURE_PLAN § Delight & Polish → "Juice layer". |

Net additions: 2 new Services modules (`ProgressReportService` + `SensoryPaletteCoordinator`, both under `Services/Engagement/` per the portfolio SPM folder convention) + 1 new AppFeature module (`ProgressReportView` under `AppFeature/Settings/`) + 13 new unit tests across 2 suites (`ProgressReportServiceTests` + `SensoryPaletteCoordinatorTests`) + 2 ForgeKit modules promoted from declared-but-unused → actively consumed (`ForgeReporting` → Services; `ForgeSensory` → Services). Build green at each merge.

**Previous round (2026-06-12, PRs #63 → #66, sixth-pass auto-cycle sweep)**: 4-PR sweep driven by the user-direct standing auto-cycle paired with the **sixth-pass** Xcode-managed file safety reinforcement + an explicit maximize-ForgeKit-integration + close-FEATURE_PLAN-checkboxes + follow-technical-design-doc + SPM-folder-structure directive set. Rollup:

| PR | Theme |
|---|---|
| #63 | **Sixth-pass** Xcode-managed file safety reinforcement — codifies the **six-pass invariant** (when a single safety rule is restated six times in one calendar day, every subsequent session treats it as an immutable pre-flight check + every subsequent handoff doc carries a rule-restatement summary at the top so the cadence is inherited without re-reading the entire CLAUDE.md). Adds two new 6th-pass interaction notes (SPM folder-structure + technical-design-doc) clarifying that neither directive extends to managed-file edits. Also commits the Xcode-regenerated diff to `MicrobeLab.xctestplan` that wires the `SharedUITests` SPM test target from PR #59 — per the rule, staging+committing the Xcode-regenerated diff IS fine. Closes `HANDOFF_TO_USER_XCODE_GUI_TASKS.md` § 6. |
| #64 | ForgeAnalytics on-device privacy-first event totals — closes the "still declared but unused" gap from § ForgeKit Integration Status. Adds `AnalyticsService` (MainActor `@Observable` wrapper around the `ForgeAnalytics.AnalyticsEngine` actor) under `Services/Engagement/` + a canonical `MicrobeLabAnalyticsEvent` enum with grep-able snake_case event names + PII-safe property bags (counts + slugs only). Wires `AppRootView` (session start/end on scenePhase), `ExploreView` (zoom_tier_reached on every microscope snap), `MicrobiomeView` (feeding_mode_changed + microbiome_milestone every 5th tick + achievement_earned per ForgeGamification unlock), and `ImmuneGameView` (immune_wave_cleared per wave + immune_run_completed on full-run clear). 12 new `@Test` covering init defaults + session lifecycle + per-event name mapping + accumulation + activeDays plumbing + ZoomTier slug stability + property-bag PII discipline. |
| #65 | ForgeSpotlight indexing for the 12-microbe codex — closes the "still declared but unused" ForgeSpotlight gap. Adds `MicrobeSpotlightItem` (pure `SpotlightIndexable` adapter; stable slug-based `spotlightID` so UUID churn doesn't fragment the index) + `MicrobeSpotlightIndex` (MainActor `@Observable` wrapper around `ForgeSpotlightIndexer`). `AppRootView.loadCatalog()` indexes the catalog on every cold launch; ForgeSpotlight dedupes via `spotlightID`. Trauma-informed posture mirrors `MicrobeCodexView`: entire catalog indexed (the codex already shows all 12 cards from launch — locked entries render as "???"); locked microbes still gate the fact card behind discovery once landed. Privacy: `CSSearchableIndex` is on-device per `.claude/rules/age-assurance.md`; keywords ship stable enum slugs only (pinned by `spotlightKeywordsContainOnlyStableEnumSlugs` invariant). 7 new `@Test`. |
| #66 | Mentor personality callbacks — closes the FEATURE_PLAN § Delight & Polish → "Character personality" item. Adds `MentorRecallStore` (UserDefaults-persisted ring buffer of recently-met microbe slugs; cap 5; FIFO recency dedup) + `VeeMentor.recallCue(for:daysSinceLastSeen:)` (warm trauma-informed callback copy with day-bucket pivots 0 / 1 / 2-6 / 7+ — "still hanging around" / "still here when you're ready" — never "you abandoned us" or loss-aversion). `ExploreView.init()` prefers a recall callback over the variable-reward / default cold-open copy when the store has entries; `ExploreView.onAppear` records rare-microbe-sighting slugs into the store so subsequent cold opens can quote them. 8 new `@Test` for the store + 6 new `@Test` for the cue (pinning the trauma-safe no-abandonment-words discipline). |

Net additions: 2 new Services modules (`AnalyticsService` + `MicrobeSpotlightIndex` + `MentorRecallStore` — the last two land flat at the Services root + under `Services/Engagement/` respectively per the SPM folder convention) + 1 new SharedUITests xctestplan wiring (Xcode GUI) + 33 new unit tests across 4 suites + 3 ForgeKit modules promoted from declared-but-unused → actively consumed (`ForgeAnalytics` → Services, `ForgeSpotlight` → Services; the recall surface extends the existing `ForgeAI`-fronted `VeeMentor`). Build green at each merge.

**Previous round (2026-06-12, PRs #58 → #61, fifth-pass auto-cycle)**: 4-PR sweep driven by the user-direct standing auto-cycle paired with the fifth-pass Xcode-managed file safety reinforcement + maximize-ForgeKit-integration + close-FEATURE_PLAN-checkboxes directive. Rollup:

| PR | Theme |
|---|---|
| #58 | Xcode-managed file safety — **fifth-pass** reinforcement (CLAUDE.md / `xcode-agent-safety.md` / `HANDOFF_TO_USER_XCODE_GUI_TASKS.md`). Codifies the five-pass invariant (when a single safety rule is restated five times in one day it becomes a pre-flight check, not a guideline) + the maximize-ForgeKit-integration interaction note (ForgeKit SPM-only wiring stays safe; modules needing entitlements still route through `HANDOFF_TO_USER_XCODE_GUI_TASKS.md`). |
| #59 | ForgePedagogy hint scaffolding in QuizView — first kid-facing consumer of the previously-declared `ForgePedagogy` ForgeKit module. `QuestionHintStrategy` (SharedUI/, pure `nonisolated` value-type) derives per-tier hint text from a `Question`: vague leans on the curriculum-standard tag; medium returns the first sentence of the explanation with the correct-choice phrase elided; specific returns the full explanation. `QuizMachine` extended with `requestedHintTier` + `hintsUsedCount` + `nextRequestableHintTier` + `requestNextHint()`. `QuizView` adds a "Hint" button next to "Check" + an inline yellow-tinted hint card + a trauma-informed completion summary ("Asked for help on N — that's how learning works"). New `SharedUITests` SPM target ships 11 `@Test` covering the progression + per-tier text invariants (Xcode scheme-wiring step queued in `HANDOFF_TO_USER_XCODE_GUI_TASKS.md` § 6). |
| #60 | ForgeKnowledgeGraph cross-microbe ecology surfacing — `MicrobeKnowledgeGraph` (Services/, pure `nonisolated` value-type wrapper) builds a shared-habitat graph from the bundled catalog: every pair of microbes with the same `preferredEnvironment` gets a symmetric pair of `.recommended` edges. `MicrobeCodexCard` gains an optional `livesNearDisplayNames` param; discovered cards render a small "Lives near: A, B" caption. `MicrobeCodexView` builds the graph once on appear + threads neighbors through, filtering to the kid's discovered set so the codex NEVER hints at undiscovered microbes (trauma-informed posture). 9 `@Test` units in `ServicesTests/MicrobeKnowledgeGraphTests` pin construction + ordering + limit + exclude-self + unknown-slug. |
| #61 | Auto-surface SessionSummarySheet on app background — closes the FEATURE_PLAN.md § Parent Integration → "Session closer" follow-up. `AppRootView` stamps `sessionStartedAt` + `sessionStartXP` on cold launch + every `.background → .active` resume; on `.background` the productivity gate (`captureSessionSummaryIfProductive`) requires elapsed ≥ 60s AND XP earned > 0; the next `.active` surfaces `pendingSessionSummary` via `.sheet(isPresented:)` UNLESS the daily-cap / welcome-back / streak-rescue overlays hold centered-overlay precedence. Trauma-informed: short backgrounds (notification check) NEVER fire the summary; quiet sessions (0 XP) NEVER fire it. |

Net additions: 1 new Services module (`MicrobeKnowledgeGraph`) + 1 new SharedUI module (`QuestionHintStrategy`) + 1 new SharedUITests SPM test target (scheme wiring queued in handoff doc) + 20 new unit tests (11 `QuizMachineHintTests` + 9 `MicrobeKnowledgeGraphTests`) + 2 ForgeKit modules promoted from declared-but-unused → actively consumed (`ForgePedagogy` → SharedUI; `ForgeKnowledgeGraph` → Services). Build green at each merge.

**Previous round (2026-06-12, PRs #52 → #56, fourth-pass safety + Phase-1 polish)**: 5-PR sweep — Xcode-managed file safety 4th-pass + ForgeCelebration coordinator + DailyTimeCoordinator + Declared Age Range API gate scaffold + OSSignposter perf probes. See git log for the full per-PR notes; cross-references preserved in CLAUDE.md + FEATURE_PLAN.md.

**Pre-2026-06-12 PR #51**: Read-only accessibility audit (`Docs/AUDIT_ACCESSIBILITY_PASS_2026-06-12.md`) — covered every SwiftUI surface shipped through PR #50, verdict PASS WITH GAPS (Dynamic Type + color contrast portfolio-wide PASS; 3 prioritized gaps remain).

**Earlier 2026-06-12 (PRs #28 + #29 + #30)**: Phase-1 question kits 02 (microbiome), 03 (immune defense, trauma-informed register), and 04 (beneficial microbes) shipped — each as its own auto-cycle PR with bundled JSON in `Services/Resources/`, `QuestionKitService.phase1KitSlugs` extended in canonical order, and new `QuestionKitServiceTests` coverage (9/9 pass). FEATURE_PLAN.md § Gamification kit-bundling work item closed. CLAUDE.md Xcode-managed-file safety table (workspace + scheme + test plan) is the canonical reference — the user reinforced this guard at session start.

**Earlier this day (PRs #25 + #26)**: Mentor (Cilia) cue refreshes on `MicrobiomeView` feeding-mode change + every-5th-tick milestone + on `ImmuneGameView` wave-clear / run-complete. Phase-1 achievements `fiberPioneer` / `sugarTrial` / `microbiomeSteady` / `immuneRookie` / `immuneRunner` auto-evaluate as the kid hits criteria; XP awards flow through `GamificationService.evaluateAchievements`. Doc-path drift (`Libraries/Package.swift` → `Packages/Libraries/Package.swift`) corrected across CLAUDE.md + TECHNICAL_DESIGN.md + FEATURE_PLAN.md + APP_SPECIFIC_NOTES.md. Pre-existing `MacrophagePacmanSceneTests.spawnIsReproducibleAcrossSeeds()` failure (UUID-identity drift) fixed by comparing the deterministic `(kind, position, velocity)` projection.

## Read First

1. **`Docs/TECHNICAL_DESIGN.md`** — architecture, state machines, domain model, trauma-informed posture
2. **`Docs/FEATURE_PLAN.md`** — phased roadmap with checkbox status per work item
3. **This repo's CLAUDE.md** — portfolio tech stack + reference doc index + Xcode-managed file constraints
4. **Portfolio patterns**: `labsmith/Docs/PORTFOLIO_PATTERNS.md` § Implementation Prep

## ForgeKit Integration Status (post-2026-06-13 maximize-integration sweep, thirteenth pass)

Modules now actively consumed (vs the `Package.swift` declared list):

| Module | Where it's wired | Notes |
|---|---|---|
| `ForgeModels` | Models, Services, AppFeature | `StudentProfile`, `BloomLevel`, `StandardAlignment`, base value types |
| `ForgePersistence` | Services | SwiftData helpers |
| `ForgeGamification` | Services (`GamificationService` wraps `XPEngine` + `StreakManager` + `AchievementEngine`) | Phase-1 achievements + XP curve |
| `ForgeAccessibility` | Services (`DailyTimeCoordinator` wraps `SessionTimerService`) | Daily-cap pipeline (PR #54) |
| `ForgeUI` | AppFeature (`MicrobeLabOnboardingFlow` wraps `ForgeOnboardingFlow`) | Onboarding glass buttons + flow scaffolding |
| `ForgeCelebration` | AppFeature (`CelebrationCoordinator` at `AppRootView` + `.celebrationOverlay`) | Proportional milestone celebrations (PR #53) |
| `ForgeAvatar` | AppFeature (`AvatarStudioSheet` hosts `AvatarStudioView(.lite)`) | Avatar studio + ForgeID seeding |
| `ForgeSync` | AppFeature (`AvatarStudioSheet` / `ProfileView` read/write the canonical avatar via `AppGroupStore`) | Cross-portfolio identity |
| `ForgeGameEngine` | GameEngine (SpriteKit scenes import for base helpers) | LOD swap infrastructure pending portrait pack |
| `ForgePedagogy` | SharedUI (`QuestionHintStrategy` derives per-tier hint text; `QuizMachine` exposes `HintTier`-driven progression) | Hint scaffolding in QuizView (PR #59) |
| `ForgeKnowledgeGraph` | Services (`MicrobeKnowledgeGraph` wraps `KnowledgeGraph` with shared-habitat edges; `MicrobeCodexView` surfaces "Lives near" hints) | Cross-microbe ecology surfacing (PR #60) |
| `ForgeAnalytics` | Services (`AnalyticsService` wraps the actor-based `AnalyticsEngine`; `MicrobeLabAnalyticsEvent` enum carries grep-able event names + PII-safe property bags) | On-device event totals + session lifecycle (PR #64) |
| `ForgeSpotlight` | Services (`MicrobeSpotlightIndex` wraps `ForgeSpotlightIndexer`; `MicrobeSpotlightItem` adapts `MicrobeCharacter` to `SpotlightIndexable`) | 12-microbe codex Spotlight indexing (PR #65) |
| `ForgeReporting` | Services (`ProgressReportService` wraps `ForgeReportGenerator`; `AppFeature/Settings/ProgressReportView` renders the parent-conference text + standards-covered list under the parental-gate "For parents" section) | Parent-facing standards-mapped progress report (PR #70) |
| `ForgeSensory` | Services (`SensoryPaletteCoordinator` wraps `SensoryPalette`; QuizView / MicrobiomeView / ImmuneGameView fire `.correctAnswer` / `.incorrectAnswer` / `.achievement` / `.streakMilestone(wave)` / `.challengeComplete` events) | Juice layer haptic axis (PR #71); audio dispatch defaults nil until SFX pack lands |
| `ForgeAdventure` | AppFeature (`MicrobeLabHubContribution` Level 2 overlay conforms to `HubContribution`; `MicrobeLabHubRegistrar.register(into:)` async helper for AdventureHub-side integration; `MicrobeLabHubChallengeAdapter` renders the per-engine hub placeholder surface) | Phase-1 hub contribution targets `.scienceLabs` (PR #74); cross-repo handoff requests labsmith add canonical `lifeZone` case |
| Apple `AppIntents` (system) | AppFeature (`MicrobeLabIntents.swift` declares `OpenMicroscopeIntent` / `OpenCodexIntent` / `OpenMicrobiomeIntent`; `MicrobeLabAppShortcuts` declares 6 Siri-callable phrases); Services (`NavigationCoordinator` brokers App Intent → tab-switch via `requestTab(_:)`); Models (`MicrobeLabTab` canonical 5-tab enum); AppRootView observes `navigationCoordinator.requestedTab` via `.onChange(of:)` | Siri / Spotlight / Shortcuts entry points natively wired (PR #83); ForgeKit `ForgeIntents` helper API can layer on top in a follow-up |
| `ForgeNavigation` | AppFeature (`MicrobeLabPhase: AppPhase` mirrors the AppRootView Group block 1:1 with 5 cases — `.parentHandoff` / `.kidOnboarding` / `.loadingCatalog` / `.tabShell` / `.catalogFailure`; `MicrobeLabStartupGateID` exposes canonical gate IDs); AppRootView emits `.onChange(of: currentPhase)` → `DebugLog.lifecycle` + `AnalyticsService.track(.appPhaseReached(phaseSlug:))` per transition | Last declared-but-unused ForgeKit module — promoted to actively consumed (PR #87). Future ForgePhaseRouter consumers can adopt the phase enum verbatim |
| `ForgeReporting` (extended) | Services (`ProgressReportService` now derives `[StandardProficiency]` from `QuestionAttemptStore.proficiencies(matching:)` + threads through `ProgressReportSnapshot.standardProficiencies`; `averageScore` derives from the proficiency mean) | Per-standard proficiency surface CLOSED (PR #88); parent-conference report now renders real Strengths / Growth Areas sections when the kid has tagged-question attempts |
| `ForgeIllustrations` | AppFeature (`ChapterReaderSheet` renders `ChapterPortraitView` opener variant; bundle resolved via `MicrobeChapterStore.resourcesBundle` static accessor) + Services (`MicrobeChapterStore` loads 6 canonical DN-S chapter MDs from `Bundle.module`; per-microbe markdown content surfaces as opt-in reader sheet on discovered codex cards) | Chapter content axis CLOSED (PR #94); `IllustrationRegistry` per-microbe portrait + per-chapter WebP registration items still asset-blocked on labsmith portrait pack distribution. `ChapterPortraitView` gracefully renders `Color.clear` when chapter WebPs are absent so the surface is correct today + auto-upgrades when assets ship |

**All ForgeKit modules declared in `Package.swift` are now actively consumed by Swift code** — zero remaining declared-but-unused gaps post-PR #87 (PR #94 adds `ForgeIllustrations` to the declared + consumed set in one motion).

## What's Shipped (Phase 1)

### Scaffold + SPM

- 6-target SPM package (`Packages/Libraries`) — `Models` / `Services` / `SharedUI` / `GameEngine` / `AIMentor` / `AppFeature`
- ForgeKit pinned at `from: "0.99.0"` via remote GitHub URL
- Per-target `swiftSettings` matches portfolio default-isolation MainActor + InferIsolatedConformances
- `Bundle.module` resources for the bundled microbe catalog + question kits

### Data Layer (Models)

- Value types: `MicrobeCharacter` (v2 with `voiceLines`) / `MicrobiomeState` / `ZoomTier` / `GutSlot` / `FeedingMode` / `AntibioticState` / `GrowthRate` / `MicrobeKingdom` / `MicrobeRole`
- Immune-game value types: `PathogenKind` / `PathogenState` / `MacrophageState` / `Vec2`
- Question-kit value types: `Question` / `QuestionKit`
- SwiftData `@Model`s: `PersistentMicrobeSession` / `PlayerProgress` / `EncounterLog` / `JournalEntry`
- `SchemaV1` + `MicrobeLabMigrationPlan` (versioned-schema from day one per `.claude/rules/swiftdata.md`)
- Per-`@Model` value-type cache structs in `CacheStructs.swift`

### Services

- `MicrobeCatalogService` — loads bundled `microbes.json` (v2 schema, 12 entries)
- `QuestionKitService` — bundled kit loader; ships kits 01-04 (microbiology basics 5q + microbiome 6q + immune defense 6q + beneficial microbes 6q, all NGSS / NHES tagged) via `phase1KitSlugs`
- `AppSettings` + `AppSettingsStore` — UserDefaults-backed, dependency-injected per `.claude/rules/testing.md`
- `GamificationService` — `@Observable` MainActor wrapper around `XPEngine` + `StreakManager` + `AchievementEngine`
- `MicrobeLabAchievements` — 10 Phase 1 achievement definitions
- `MicrobeKnowledgeGraph` — pure value-type wrapper around `ForgeKnowledgeGraph.KnowledgeGraph` with shared-habitat edges (PR #60)
- `AnalyticsService` — MainActor `@Observable` wrapper around the `ForgeAnalytics.AnalyticsEngine` actor; on-device only (PR #64)
- `MicrobeSpotlightIndex` — MainActor `@Observable` wrapper around `ForgeSpotlightIndexer`; indexes the 12-microbe catalog for Spotlight search (PR #65)
- `MentorRecallStore` — UserDefaults-persisted ring buffer of recently-met microbe slugs; powers Cilia's callback line surface (PR #66)
- `ProgressReportService` — pure nonisolated value type wrapping `ForgeReporting.ForgeReportGenerator`; produces parent-conference report text + standards-covered list from a `ProgressReportSnapshot` (PR #70)
- `SensoryPaletteCoordinator` — MainActor `@Observable` wrapper around `ForgeSensory.SensoryPalette`; routes haptic (and audio when SFX pack lands) feedback events across QuizView / MicrobiomeView / ImmuneGameView (PR #71)
- `CodexCertificate` — pure nonisolated `Sendable` value type snapshotting the kid's discovery progress with warm tiered headline; trauma-informed copy stoplist pinned by test (PR #75)
- `MasteryMomentDetector` — pure nonisolated `Sendable` value type tracking per-session mastery thresholds for `.ecologyMaster` / `.defenseMaster` / `.codexMaster`; once-per-session via `acknowledged: Set<MasteryKind>` (PR #76)
- `ImmuneDefenseTrophy` — pure nonisolated `Sendable` value type snapshotting the kid's macrophage-minigame run with warm tiered headline; perfect-run flag gated on same criteria as `MasteryMomentDetector.recordDefenseRunComplete`; trauma-informed copy stoplist pinned by parameterized test (PR #82)
- `NavigationCoordinator` — `@MainActor @Observable` singleton brokering App Intent → tab-switch requests via `requestTab(_:)` / `clearRequest()` (PR #83)
- `DiscoveryStore` — MainActor `@Observable` UserDefaults-backed persistence of microbe slugs the kid has met; pure-additive (PR #86)
- `QuestionAttemptStore` — MainActor `@Observable` UserDefaults-backed log of per-question quiz attempts; `proficiencies(matching:)` derives `[ForgeReporting.StandardProficiency]` for the parent-facing progress report (PR #88)
- `ParentalConsentService` — MainActor `@Observable` UserDefaults-backed store of per-feature `ParentalConsentRecord` rows with 365-day expiry per FTC 2026 COPPA Rule § Data retention limits; `ParentalConsentKind` enum carries 4 feature classes (disease story arcs / weekly summary / external links / classroom mode); `hasValidConsent(for:)` is the canonical gate-side check (PR #90)
- `WeeklySummaryService` — MainActor `@Observable` coordinator for the opt-in local weekly summary notification; wraps a `UserNotificationScheduler` adapter (production `SystemNotificationScheduler` calls `UNUserNotificationCenter.current()`; tests inject a recording fake); `WeeklySummaryNotificationContent.compose(from:)` is the pure `nonisolated` body composer with trauma-safe quiet-week framing (PR #91)
- `DebugLog` — single-seam emitter with 7 categories per `.claude/rules/debug-logging.md`

### Game Engine

- `MicroscopeScene` + `ZoomMachine` — pinch-to-zoom + tier-boundary snap; lazy visual setup
- `MicrobiomePuzzleScene` + `MicrobiomeSimulator` + `SimulationMachine` — feeding modes + antibiotic shock + recovery
- `SeededRNG` (splitmix64) — drives reproducible simulator jitter + immune-game spawn
- `MacrophagePacmanScene` — pure-logic surface (`spawnCurrentWave` / `advancePathogens` / `moveMacrophage` / `consumePathogensInRadius`) + lazy visuals
- `SimulationMachine` undo stack + `ZoomMachine` tier-state transitions

### AI Mentor

- `VeeMentor` (Cilia) — lazy `LanguageModelSession` reuse, availability gating, static fallbacks
- `@Generable` types: `MicrobeFact` / `ZoomCue` / `EcologyHypothesis`
- Per-character voice-line rotation via `VeeMentor.voiceLine(for:rotation:)`

### SharedUI

- `TierBadge` (Liquid Glass Category B button)
- `MicroscopeHUD` (tier-badge row + magnification chip)
- `FeedingModePicker`
- `MentorBubble` (Cilia speech bubble)
- `MicrobeCodexCard` (nav-grid Category C card)
- `QuizMachine` (view-local FSM per `.claude/rules/state-machines.md`)

### AppFeature

- 5-tab `TabView` shell (Explore / Codex / Microbiome / Progress / Profile)
- `ExploreView` (microscope SpriteView + HUD + mentor cue refresh on snap)
- `MicrobeCodexView` (12-microbe grid + toolbar Menu → kit picker → QuizView sheet)
- `MicrobiomeView` (puzzle scene + feeding-mode picker + antibiotic alert + NavigationStack → ImmuneGameView)
- `ImmuneGameView` (Pac-Man scene + trauma-safe off-ramp + score HUD)
- `ProgressTabView` (level + XP bar + streak chips + achievement gallery)
- `ProfileView` (placeholder for `AvatarStudioView`) → NavigationLink → `SettingsView`
- `SettingsView` (sensory toggles kid-accessible; content gate + session cap parent-gated via `ParentalGateView`)
- `QuizView` (5-question flow + reveal explanation + completion panel; awards XP + achievements on completion)
- `ParentalGateView` (math-problem gate; 3-strike 30s cooldown)

### Tests

- ModelsTests, GameEngineTests, ServicesTests, AIMentorTests targets each ship per-feature `@Suite` test coverage
- Notable coverage: `SeededRNG` reproducibility, simulator determinism + jitter, `MacrophagePacmanScene` spawn/move/consume logic, `AppSettings` roundtrip, `QuestionKitService` load, `GamificationService` XP + achievement idempotency, `VeeMentor` voice-line rotation

## Outstanding (Phase 1 trail)

These remain unchecked in `Docs/FEATURE_PLAN.md`:

| Item | Blocker / Plan |
|---|---|
| Microscope LOD sprite atlas + per-tier sprite swap | Asset-blocked on 12-microbe portrait pack — labsmith handoff `HANDOFF_FROM_APP_MICROBE_ILLUSTRATIONS.md` queued per `.claude/rules/forgekit.md` § Asset generation ownership |
| 12 cast portrait WebPs + `ForgeIllustrations.IllustrationRegistry` wiring | Same as above |
| ~~Question kits 02-04~~ | ✅ SHIPPED 2026-06-12 via PRs #28 / #29 / #30 |
| ~~Onboarding flow (5-step)~~ | ✅ SHIPPED via `MicrobeLabOnboardingFlow` (AppFeature/Onboarding/) |
| ~~Celebration system~~ | ✅ SHIPPED via PR #53 — `CelebrationCoordinator` proportional juice layer |
| ~~Parental controls (daily cap)~~ | ✅ SHIPPED via PR #54 — `DailyTimeCoordinator` + `DailyCapOverlay` |
| ~~Accessibility audit GAP 1 (SpriteView VoiceOver labels)~~ | ✅ SHIPPED via PR #69 — 3 SpriteView hosts carry top-level `.accessibilityLabel` + dynamic `.accessibilityValue` |
| ~~Progress dashboard (engagement skeleton)~~ | ✅ SHIPPED via PR #70 — `ProgressReportService` + `ProgressReportView` under SettingsView's "For parents" section; per-standard proficiency (strengths / growth areas) follows when per-question attempt logs land |
| ~~Juice layer (haptic axis)~~ | ✅ SHIPPED via PR #71 — `SensoryPaletteCoordinator` routes haptic events across QuizView / MicrobiomeView / ImmuneGameView; audio dispatch defaults nil until SFX pack lands |
| ~~Adventure Mode Level 2 Swift overlay~~ | ✅ SHIPPED via PR #74 — `MicrobeLabHubContribution` conforms to `ForgeAdventure.HubContribution`; `MicrobeLabHubRegistrar.register(into:)` ships the async helper AdventureHub-side integration calls at hub startup. Targets `.scienceLabs` until labsmith ships canonical `lifeZone` case per `Docs/HANDOFF_FROM_APP_FORGEADVENTURE_LIFE_ZONE_PROPOSAL.md` |
| ~~Share-worthy moments (codex completion axis)~~ | ✅ SHIPPED via PR #75 — `CodexCertificate` + `CodexCertificateView` + `CodexCertificateSheet` (`ImageRenderer` → `ShareLink`); Progress tab gains "Share my codex" toolbar button |
| ~~Share-worthy moments (immune defense trophy axis)~~ | ✅ SHIPPED via PR #82 — `ImmuneDefenseTrophy` + `ImmuneDefenseTrophyView` + `ImmuneDefenseTrophySheet` mirror the codex pattern. ImmuneGameView gains "Share trophy" icon button in the score HUD that surfaces when the run reaches `isComplete`. Perfect-run gate (Defense Master tier) matches `MasteryMomentDetector.recordDefenseRunComplete` perfect-run criteria. **Both share-worthy axes now CLOSED.** |
| ~~App Intents (Siri / Spotlight / Shortcuts)~~ | ✅ SHIPPED via PR #83 — `OpenMicroscopeIntent` + `OpenCodexIntent` + `OpenMicrobiomeIntent` + `MicrobeLabAppShortcuts` (6 Siri phrases) + `MicrobeLabTab` (Models) + `NavigationCoordinator` (Services) + AppRootView `.onChange(of:)` observer with progressive-disclosure guard. No Info.plist / entitlement edits required |
| ~~Mastery moments (ecology axis)~~ | ✅ SHIPPED via PR #76 — `MasteryMomentDetector` fires `.epic` tier celebration + distinct haptic when the kid maintains stable ecology ≥ 15 ticks under fiber feeding |
| ~~Mastery moments (defense axis)~~ | ✅ SHIPPED via PR #79 — `ImmuneGameView.evaluateDefenseMastery()` fires `.epic` `personalBest` ripple via `daily-complete` Lottie + distinct `.streakMilestone(scene.wave)` haptic + trauma-informed mentor bubble subline when the kid clears all 5 waves with zero pathogens remaining (perfect run) |
| ~~Mastery moments (codex axis)~~ | ✅ SHIPPED via PR #86 — `DiscoveryStore` persists microbe-slug discoveries; `MicrobeCodexView` taps mark microbes discovered + sync from `ExploreView`'s rare-sighting + curious-explorer cues; `MasteryMomentDetector.recordCodexDiscovery` fires `.codexMaster` via the canonical trifecta at 12/12. **MasteryMomentDetector now wired across all 3 of 3 axes.** |
| ~~ForgeNavigation wire-up~~ | ✅ SHIPPED via PR #87 — `MicrobeLabPhase: AppPhase` value type + `MicrobeLabStartupGateID` canonical gate IDs + `AppRootView.onChange(of: currentPhase)` observer fires `DebugLog.lifecycle` + `AnalyticsService.track(.appPhaseReached(phaseSlug:))` per transition. **Zero remaining declared-but-unused ForgeKit modules.** |
| ~~Parent dashboard per-standard proficiency~~ | ✅ SHIPPED via PR #88 — `QuestionAttemptStore` persists per-question attempts; `proficiencies(matching:)` derives `[ForgeReporting.StandardProficiency]`; `ProgressReportSnapshot.standardProficiencies` threads through the existing plumbing; `ProgressReportService.reportData` averageScore now derives from the proficiency mean. Strengths / Growth Areas sections render once the kid has answered tagged questions |
| Declared Age Range API gate (iOS 26.2+) | **Scaffold landed PR #55**. Live `await requestAgeRange(...)` blocked on (a) entitlement provisioning via Xcode GUI per `HANDOFF_TO_USER_XCODE_GUI_TASKS.md` § 6b, (b) COPPA consent + retention surface (receiving "Under 13" creates actual knowledge) |
| Adventure Mode (Life Zone hub config + ProgressionManager gating) | Awaits AdventureHub Level 1 JSON + ZoneID.lifeZone case + ForgeProgressionManager hub-side integration. Cross-repo handoff filed `Docs/HANDOFF_FROM_APP_FORGEADVENTURE_LIFE_ZONE_PROPOSAL.md` |
| ~~Performance profiling (16ms tier transition / 8ms sim tick)~~ | ✅ SHIPPED via PR #56 (signposts) + PR #92 (bench harness). `PerformanceBudgetTests` (6 `@Test`) gates the algorithmic regression class at the SPM-test layer. Real-device 60fps Instruments capture still wants a real device — that's the only remaining axis |
| ~~Parental consent service~~ | ✅ SHIPPED via PR #90 — `ParentalConsentService` (Services/Engagement/) + `ParentalConsentManagerView` (AppFeature/Settings/). 4 distinct consent kinds + 365-day re-consent window per FTC 2026 |
| ~~Parental gates~~ | ✅ SHIPPED via PR #90 — the consent service IS the canonical gate-side check; `hasValidConsent(for:)` is what every external-link / data-sharing surface MUST call. Existing `ParentalGateView` math gate stays the adult-verifier |
| ~~Weekly summary opt-in notification~~ | ✅ SHIPPED via PR #91 — `WeeklySummaryService` + `WeeklySummaryNotificationContent.compose(from:)`. Saturday 9am `UNCalendarNotificationTrigger`; trauma-safe quiet-week framing pinned by parameterized test. Local-only |
| UI tests / accessibility XCUITests | Best landed once portrait pack ships (UI tests rely on rendered assets) — accessibility audit already PASS WITH GAPS per `Docs/AUDIT_ACCESSIBILITY_PASS_2026-06-12.md`; GAP 1 closed PR #69 |
| Juice layer audio axis (SFX pack) | Asset-blocked on labsmith SFX bundle handoff per `.claude/rules/forgekit.md` § Asset generation ownership; coordinator is wired and ready (SFX dispatch closure parameter defaults nil) |

## Sequencing Constraints

- **AvatarStudio integration** awaits the ForgeKit 0.99-series `AvatarStudioView` portrait pack distribution per `.claude/rules/forgekit.md` § Avatar Edit Authority
- **Microscope portrait pack** is a single asset wave per `.claude/rules/forgekit.md` § Asset generation ownership; labsmith generates + ships via cross-repo PR
- **Trauma-informed sensitivity review** for Phase 3 disease-story arcs per `Docs/TECHNICAL_DESIGN.md` § Trauma-Informed Design Posture — gated R0 reviewer signoff per portfolio ADR-012 / ADR-016 (MicrobeLab is trauma-AWARE not trauma-gated, so the gate applies at Phase 3 only)

## Cross-references

- `@Docs/TECHNICAL_DESIGN.md` — architecture
- `@Docs/FEATURE_PLAN.md` — phase checklist
- `@Docs/HANDOFF_FROM_LABSMITH_DISTRIBUTED_NARRATIVE_RETROFIT.md` — 6-char canonical cast
- `@Docs/HANDOFF_FROM_LABSMITH_DN_S_STORY_PER_CHARACTER.md` — DN-S chapter spec
- `@.claude/rules/forgekit.md` — module catalog + asset ownership
- `@.claude/rules/spm-architecture.md` — package conventions
- `@.claude/rules/xcode-agent-safety.md` — Xcode-managed file constraints (the agent operates in-IDE; never write `.pbxproj` / `.xcscheme` / `.xctestplan` from disk)
